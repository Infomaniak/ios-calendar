#import <Foundation/NSArray.h>
#import <Foundation/NSDictionary.h>
#import <Foundation/NSError.h>
#import <Foundation/NSObject.h>
#import <Foundation/NSSet.h>
#import <Foundation/NSString.h>
#import <Foundation/NSValue.h>

@class NSString, NSSet<ObjectType>, NSObject, NSNumber, NSMutableSet<ObjectType>, NSMutableDictionary<KeyType, ObjectType>, NSMutableArray<ObjectType>, NSError, NSDictionary<KeyType, ObjectType>, NSArray<ObjectType>, KmpCalendar__SkieTypeExportsKt, KmpCalendar__SkieSuspendWrappersKt, KmpCalendarWeekDayNumCompanion, KmpCalendarWeekDayNum, KmpCalendarUShort, KmpCalendarULong, KmpCalendarUInt, KmpCalendarUByte, KmpCalendarSkie_SuspendResultSuccess, KmpCalendarSkie_SuspendResultError, KmpCalendarSkie_SuspendResultCanceled, KmpCalendarSkie_SuspendResult, KmpCalendarSkie_SuspendHandler, KmpCalendarSkie_CancellationHandler, KmpCalendarSkieKotlinStateFlow<T>, KmpCalendarSkieKotlinSharedFlow<T>, KmpCalendarSkieKotlinOptionalStateFlow<T>, KmpCalendarSkieKotlinOptionalSharedFlow<T>, KmpCalendarSkieKotlinOptionalMutableStateFlow<T>, KmpCalendarSkieKotlinOptionalMutableSharedFlow<T>, KmpCalendarSkieKotlinOptionalFlow<T>, KmpCalendarSkieKotlinMutableStateFlow<T>, KmpCalendarSkieKotlinMutableSharedFlow<T>, KmpCalendarSkieKotlinFlow<T>, KmpCalendarSkieColdFlowIterator<E>, KmpCalendarShort, KmpCalendarRecurrenceRule, KmpCalendarParticipationStatus, KmpCalendarNumber, KmpCalendarMutableSet<ObjectType>, KmpCalendarMutableDictionary<KeyType, ObjectType>, KmpCalendarLong, KmpCalendarKotlinx_serialization_coreStructureKindOBJECT, KmpCalendarKotlinx_serialization_coreStructureKindMAP, KmpCalendarKotlinx_serialization_coreStructureKindLIST, KmpCalendarKotlinx_serialization_coreStructureKindCLASS, KmpCalendarKotlinx_serialization_coreStructureKind, KmpCalendarKotlinx_serialization_coreSerializersModule, KmpCalendarKotlinx_serialization_coreSerialKindENUM, KmpCalendarKotlinx_serialization_coreSerialKindCONTEXTUAL, KmpCalendarKotlinx_serialization_coreSerialKind, KmpCalendarKotlinx_serialization_corePrimitiveKindSTRING, KmpCalendarKotlinx_serialization_corePrimitiveKindSHORT, KmpCalendarKotlinx_serialization_corePrimitiveKindLONG, KmpCalendarKotlinx_serialization_corePrimitiveKindINT, KmpCalendarKotlinx_serialization_corePrimitiveKindFLOAT, KmpCalendarKotlinx_serialization_corePrimitiveKindDOUBLE, KmpCalendarKotlinx_serialization_corePrimitiveKindCHAR, KmpCalendarKotlinx_serialization_corePrimitiveKindBYTE, KmpCalendarKotlinx_serialization_corePrimitiveKindBOOLEAN, KmpCalendarKotlinx_serialization_corePrimitiveKind, KmpCalendarKotlinx_serialization_corePolymorphicKindSEALED, KmpCalendarKotlinx_serialization_corePolymorphicKindOPEN, KmpCalendarKotlinx_serialization_corePolymorphicKind, KmpCalendarKotlinx_datetimePadding, KmpCalendarKotlinx_datetimeMonthNamesCompanion, KmpCalendarKotlinx_datetimeMonthNames, KmpCalendarKotlinx_datetimeMonth, KmpCalendarKotlinx_datetimeLocalDateRangeCompanion, KmpCalendarKotlinx_datetimeLocalDateRange, KmpCalendarKotlinx_datetimeLocalDateProgressionCompanion, KmpCalendarKotlinx_datetimeLocalDateProgression, KmpCalendarKotlinx_datetimeLocalDateCompanion, KmpCalendarKotlinx_datetimeLocalDate, KmpCalendarKotlinx_datetimeDayOfWeekNamesCompanion, KmpCalendarKotlinx_datetimeDayOfWeekNames, KmpCalendarKotlinx_datetimeDayOfWeek, KmpCalendarKotlinThrowable, KmpCalendarKotlinRuntimeException, KmpCalendarKotlinNothing, KmpCalendarKotlinInstantCompanion, KmpCalendarKotlinInstant, KmpCalendarKotlinIllegalStateException, KmpCalendarKotlinException, KmpCalendarKotlinEnumCompanion, KmpCalendarKotlinEnum<E>, KmpCalendarKotlinCancellationException, KmpCalendarKotlinArray<T>, KmpCalendarInt, KmpCalendarFrequency, KmpCalendarFloat, KmpCalendarEventTimingTimed, KmpCalendarEventTimingAllDay, KmpCalendarDouble, KmpCalendarDavCredentials, KmpCalendarCalendarSdkException, KmpCalendarCalendarSDKProvider, KmpCalendarCalendarManager, KmpCalendarCalendarAccessLevel, KmpCalendarCalendar, KmpCalendarByte, KmpCalendarBoolean, KmpCalendarBase, KmpCalendarAttendeeRole, KmpCalendarAttendee, KmpCalendarAppleIdAccessorsKt, KmpCalendarAccountManager;

@protocol NSCopying, KmpCalendarSkie_DispatcherDelegate, KmpCalendarKtorClientProvider, KmpCalendarKotlinx_serialization_coreSerializersModuleCollector, KmpCalendarKotlinx_serialization_coreSerializationStrategy, KmpCalendarKotlinx_serialization_coreSerialDescriptor, KmpCalendarKotlinx_serialization_coreKSerializer, KmpCalendarKotlinx_serialization_coreEncoder, KmpCalendarKotlinx_serialization_coreDeserializationStrategy, KmpCalendarKotlinx_serialization_coreDecoder, KmpCalendarKotlinx_serialization_coreCompositeEncoder, KmpCalendarKotlinx_serialization_coreCompositeDecoder, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithYearMonth, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithUtcOffset, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithTime, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTimeComponents, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTime, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDate, KmpCalendarKotlinx_datetimeDateTimeFormatBuilder, KmpCalendarKotlinx_datetimeDateTimeFormat, KmpCalendarKotlinx_coroutines_coreStateFlow, KmpCalendarKotlinx_coroutines_coreSharedFlow, KmpCalendarKotlinx_coroutines_coreRunnable, KmpCalendarKotlinx_coroutines_coreMutableStateFlow, KmpCalendarKotlinx_coroutines_coreMutableSharedFlow, KmpCalendarKotlinx_coroutines_coreFlowCollector, KmpCalendarKotlinx_coroutines_coreFlow, KmpCalendarKotlinOpenEndRange, KmpCalendarKotlinKDeclarationContainer, KmpCalendarKotlinKClassifier, KmpCalendarKotlinKClass, KmpCalendarKotlinKAnnotatedElement, KmpCalendarKotlinIterator, KmpCalendarKotlinIterable, KmpCalendarKotlinComparable, KmpCalendarKotlinCollection, KmpCalendarKotlinClosedRange, KmpCalendarKotlinAppendable, KmpCalendarKotlinAnnotation, KmpCalendarEventTiming, KmpCalendarEvent, KmpCalendarDatabaseModule, KmpCalendarCalendarCoreGraph;

// Due to an Obj-C/Swift interop limitation, SKIE cannot generate Swift types with a lambda type argument.
// Example of such type is: A<() -> Unit> where A<T> is a generic class.
// To avoid compilation errors SKIE replaces these type arguments with __SkieLambdaErrorType, resulting in A<__SkieLambdaErrorType>.
// Generated declarations that reference __SkieLambdaErrorType cannot be called in any way and the __SkieLambdaErrorType class cannot be used.
// The original declarations can still be used in the same way as other declarations hidden by SKIE (and with the same limitations as without SKIE).
@interface __SkieLambdaErrorType : NSObject
- (instancetype _Nonnull)init __attribute__((unavailable));
+ (instancetype _Nonnull)new __attribute__((unavailable));
@end

// Due to an Obj-C/Swift interop limitation, SKIE cannot generate Swift code that uses external Obj-C types for which SKIE doesn't know a fully qualified name.
// This problem occurs when custom Cinterop bindings are used because those do not contain the name of the Framework that provides implementation for those binding.
// The name can be configured manually using the SKIE Gradle configuration key 'ClassInterop.CInteropFrameworkName' in the same way as other SKIE features.
// To avoid compilation errors SKIE replaces types with unknown Framework name with __SkieUnknownCInteropFrameworkErrorType.
// Generated declarations that reference __SkieUnknownCInteropFrameworkErrorType cannot be called in any way and the __SkieUnknownCInteropFrameworkErrorType class cannot be used.
@interface __SkieUnknownCInteropFrameworkErrorType : NSObject
- (instancetype _Nonnull)init __attribute__((unavailable));
+ (instancetype _Nonnull)new __attribute__((unavailable));
@end

typedef id _Nonnull Skie__TypeDef__0__id __attribute__((__swift_private__));

NS_ASSUME_NONNULL_BEGIN
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wunknown-warning-option"
#pragma clang diagnostic ignored "-Wincompatible-property-type"
#pragma clang diagnostic ignored "-Wnullability"

#pragma push_macro("_Nullable_result")
#if !__has_feature(nullability_nullable_result)
#undef _Nullable_result
#define _Nullable_result _Nullable
#endif

__attribute__((swift_name("KotlinBase")))
@interface KmpCalendarBase : NSObject
- (instancetype)init __attribute__((unavailable));
+ (instancetype)new __attribute__((unavailable));
+ (void)initialize __attribute__((objc_requires_super));
@end

@interface KmpCalendarBase (KmpCalendarBaseCopying) <NSCopying>
@end

__attribute__((swift_name("KotlinMutableSet")))
@interface KmpCalendarMutableSet<ObjectType> : NSMutableSet<ObjectType>
@end

__attribute__((swift_name("KotlinMutableDictionary")))
@interface KmpCalendarMutableDictionary<KeyType, ObjectType> : NSMutableDictionary<KeyType, ObjectType>
@end

@interface NSError (NSErrorKmpCalendarKotlinException)
@property (readonly) id _Nullable kotlinException;
@end

__attribute__((swift_name("KotlinNumber")))
@interface KmpCalendarNumber : NSNumber
- (instancetype)initWithChar:(char)value __attribute__((unavailable));
- (instancetype)initWithUnsignedChar:(unsigned char)value __attribute__((unavailable));
- (instancetype)initWithShort:(short)value __attribute__((unavailable));
- (instancetype)initWithUnsignedShort:(unsigned short)value __attribute__((unavailable));
- (instancetype)initWithInt:(int)value __attribute__((unavailable));
- (instancetype)initWithUnsignedInt:(unsigned int)value __attribute__((unavailable));
- (instancetype)initWithLong:(long)value __attribute__((unavailable));
- (instancetype)initWithUnsignedLong:(unsigned long)value __attribute__((unavailable));
- (instancetype)initWithLongLong:(long long)value __attribute__((unavailable));
- (instancetype)initWithUnsignedLongLong:(unsigned long long)value __attribute__((unavailable));
- (instancetype)initWithFloat:(float)value __attribute__((unavailable));
- (instancetype)initWithDouble:(double)value __attribute__((unavailable));
- (instancetype)initWithBool:(BOOL)value __attribute__((unavailable));
- (instancetype)initWithInteger:(NSInteger)value __attribute__((unavailable));
- (instancetype)initWithUnsignedInteger:(NSUInteger)value __attribute__((unavailable));
+ (instancetype)numberWithChar:(char)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedChar:(unsigned char)value __attribute__((unavailable));
+ (instancetype)numberWithShort:(short)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedShort:(unsigned short)value __attribute__((unavailable));
+ (instancetype)numberWithInt:(int)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedInt:(unsigned int)value __attribute__((unavailable));
+ (instancetype)numberWithLong:(long)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedLong:(unsigned long)value __attribute__((unavailable));
+ (instancetype)numberWithLongLong:(long long)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedLongLong:(unsigned long long)value __attribute__((unavailable));
+ (instancetype)numberWithFloat:(float)value __attribute__((unavailable));
+ (instancetype)numberWithDouble:(double)value __attribute__((unavailable));
+ (instancetype)numberWithBool:(BOOL)value __attribute__((unavailable));
+ (instancetype)numberWithInteger:(NSInteger)value __attribute__((unavailable));
+ (instancetype)numberWithUnsignedInteger:(NSUInteger)value __attribute__((unavailable));
@end

__attribute__((swift_name("KotlinByte")))
@interface KmpCalendarByte : KmpCalendarNumber
- (instancetype)initWithChar:(char)value;
+ (instancetype)numberWithChar:(char)value;
@end

__attribute__((swift_name("KotlinUByte")))
@interface KmpCalendarUByte : KmpCalendarNumber
- (instancetype)initWithUnsignedChar:(unsigned char)value;
+ (instancetype)numberWithUnsignedChar:(unsigned char)value;
@end

__attribute__((swift_name("KotlinShort")))
@interface KmpCalendarShort : KmpCalendarNumber
- (instancetype)initWithShort:(short)value;
+ (instancetype)numberWithShort:(short)value;
@end

__attribute__((swift_name("KotlinUShort")))
@interface KmpCalendarUShort : KmpCalendarNumber
- (instancetype)initWithUnsignedShort:(unsigned short)value;
+ (instancetype)numberWithUnsignedShort:(unsigned short)value;
@end

__attribute__((swift_name("KotlinInt")))
@interface KmpCalendarInt : KmpCalendarNumber
- (instancetype)initWithInt:(int)value;
+ (instancetype)numberWithInt:(int)value;
@end

__attribute__((swift_name("KotlinUInt")))
@interface KmpCalendarUInt : KmpCalendarNumber
- (instancetype)initWithUnsignedInt:(unsigned int)value;
+ (instancetype)numberWithUnsignedInt:(unsigned int)value;
@end

__attribute__((swift_name("KotlinLong")))
@interface KmpCalendarLong : KmpCalendarNumber
- (instancetype)initWithLongLong:(long long)value;
+ (instancetype)numberWithLongLong:(long long)value;
@end

__attribute__((swift_name("KotlinULong")))
@interface KmpCalendarULong : KmpCalendarNumber
- (instancetype)initWithUnsignedLongLong:(unsigned long long)value;
+ (instancetype)numberWithUnsignedLongLong:(unsigned long long)value;
@end

__attribute__((swift_name("KotlinFloat")))
@interface KmpCalendarFloat : KmpCalendarNumber
- (instancetype)initWithFloat:(float)value;
+ (instancetype)numberWithFloat:(float)value;
@end

__attribute__((swift_name("KotlinDouble")))
@interface KmpCalendarDouble : KmpCalendarNumber
- (instancetype)initWithDouble:(double)value;
+ (instancetype)numberWithDouble:(double)value;
@end

__attribute__((swift_name("KotlinBoolean")))
@interface KmpCalendarBoolean : KmpCalendarNumber
- (instancetype)initWithBool:(BOOL)value;
+ (instancetype)numberWithBool:(BOOL)value;
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieColdFlowIterator")))
@interface KmpCalendarSkieColdFlowIterator<E> : KmpCalendarBase
- (instancetype)initWithFlow:(id<KmpCalendarKotlinx_coroutines_coreFlow>)flow __attribute__((swift_name("init(flow:)"))) __attribute__((objc_designated_initializer));
- (void)cancel __attribute__((swift_name("cancel()")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)hasNextWithCompletionHandler:(void (^)(KmpCalendarBoolean * _Nullable, NSError * _Nullable))completionHandler __attribute__((swift_name("hasNext(completionHandler:)")));
- (E _Nullable)next __attribute__((swift_name("next()")));
@end


/**
 * An asynchronous data stream that sequentially emits values and completes normally or with an exception.
 *
 * _Intermediate operators_ on the flow such as [map], [filter], [take], [zip], etc are functions that are
 * applied to the _upstream_ flow or flows and return a _downstream_ flow where further operators can be applied to.
 * Intermediate operations do not execute any code in the flow and are not suspending functions themselves.
 * They only set up a chain of operations for future execution and quickly return.
 * This is known as a _cold flow_ property.
 *
 * _Terminal operators_ on the flow are either suspending functions such as [collect], [single], [reduce], [toList], etc.
 * or [launchIn] operator that starts collection of the flow in the given scope.
 * They are applied to the upstream flow and trigger execution of all operations.
 * Execution of the flow is also called _collecting the flow_  and is always performed in a suspending manner
 * without actual blocking. Terminal operators complete normally or exceptionally depending on successful or failed
 * execution of all the flow operations in the upstream. The most basic terminal operator is [collect], for example:
 *
 * ```
 * try {
 *     flow.collect { value ->
 *         println("Received $value")
 *     }
 * } catch (e: Exception) {
 *     println("The flow has thrown an exception: $e")
 * }
 * ```
 *
 * By default, flows are _sequential_ and all flow operations are executed sequentially in the same coroutine,
 * with an exception for a few operations specifically designed to introduce concurrency into flow
 * execution such as [buffer] and [flatMapMerge]. See their documentation for details.
 *
 * The `Flow` interface does not carry information whether a flow is a _cold_ stream that can be collected repeatedly and
 * triggers execution of the same code every time it is collected, or if it is a _hot_ stream that emits different
 * values from the same running source on each collection. Usually flows represent _cold_ streams, but
 * there is a [SharedFlow] subtype that represents _hot_ streams. In addition to that, any flow can be turned
 * into a _hot_ one by the [stateIn] and [shareIn] operators, or by converting the flow into a hot channel
 * via the [produceIn] operator.
 *
 * ### Flow builders
 *
 * There are the following basic ways to create a flow:
 *
 * - [flowOf(...)][flowOf] functions to create a flow from a fixed set of values.
 * - [asFlow()][asFlow] extension functions on various types to convert them into flows.
 * - [flow { ... }][flow] builder function to construct arbitrary flows from
 *   sequential calls to [emit][FlowCollector.emit] function.
 * - [channelFlow { ... }][channelFlow] builder function to construct arbitrary flows from
 *   potentially concurrent calls to the [send][kotlinx.coroutines.channels.SendChannel.send] function.
 * - [MutableStateFlow] and [MutableSharedFlow] define the corresponding constructor functions to create
 *   a _hot_ flow that can be directly updated.
 *
 * ### Flow constraints
 *
 * All implementations of the `Flow` interface must adhere to two key properties described in detail below:
 *
 * - Context preservation.
 * - Exception transparency.
 *
 * These properties ensure the ability to perform local reasoning about the code with flows and modularize the code
 * in such a way that upstream flow emitters can be developed separately from downstream flow collectors.
 * A user of a flow does not need to be aware of implementation details of the upstream flows it uses.
 *
 * ### Context preservation
 *
 * The flow has a context preservation property: it encapsulates its own execution context and never propagates or leaks
 * it downstream, thus making reasoning about the execution context of particular transformations or terminal
 * operations trivial.
 *
 * There is only one way to change the context of a flow: the [flowOn][Flow.flowOn] operator
 * that changes the upstream context ("everything above the `flowOn` operator").
 * For additional information refer to its documentation.
 *
 * This reasoning can be demonstrated in practice:
 *
 * ```
 * val flowA = flowOf(1, 2, 3)
 *     .map { it + 1 } // Will be executed in ctxA
 *     .flowOn(ctxA) // Changes the upstream context: flowOf and map
 *
 * // Now we have a context-preserving flow: it is executed somewhere but this information is encapsulated in the flow itself
 *
 * val filtered = flowA // ctxA is encapsulated in flowA
 *    .filter { it == 3 } // Pure operator without a context yet
 *
 * withContext(Dispatchers.Main) {
 *     // All non-encapsulated operators will be executed in Main: filter and single
 *     val result = filtered.single()
 *     myUi.text = result
 * }
 * ```
 *
 * From the implementation point of view, it means that all flow implementations should
 * only emit from the same coroutine context.
 * This constraint is efficiently enforced by the default [flow] builder.
 * The [flow] builder should be used if the flow implementation does not start any coroutines.
 * Its implementation prevents most of the development mistakes:
 *
 * ```
 * val myFlow = flow {
 *     // GlobalScope.launch { // is prohibited
 *     // launch(Dispatchers.IO) { // is prohibited
 *     // withContext(CoroutineName("myFlow")) { // is prohibited
 *     emit(1) // OK
 *     coroutineScope {
 *         emit(2) // OK -- still the same coroutine
 *     }
 * }
 * ```
 *
 * Use [channelFlow] if the collection and emission of a flow are to be separated into multiple coroutines.
 * It encapsulates all the context preservation work and allows you to focus on your
 * domain-specific problem, rather than invariant implementation details.
 * It is possible to use any combination of coroutine builders from within [channelFlow].
 *
 * If you are looking for performance and are sure that no concurrent emits and context jumps will happen,
 * the [flow] builder can be used alongside a [coroutineScope] or [supervisorScope] instead:
 * - Scoped primitive should be used to provide a [CoroutineScope].
 * - Changing the context of emission is prohibited, no matter whether it is `withContext(ctx)` or
 *   a builder argument (e.g. `launch(ctx)`).
 * - Collecting another flow from a separate context is allowed, but it has the same effect as
 *   applying the [flowOn] operator to that flow, which is more efficient.
 *
 * ### Exception transparency
 *
 * When `emit` or `emitAll` throws, the Flow implementations must immediately stop emitting new values and finish with an exception.
 * For diagnostics or application-specific purposes, the exception may be different from the one thrown by the emit operation,
 * suppressing the original exception as discussed below.
 * If there is a need to emit values after the downstream failed, please use the [catch][Flow.catch] operator.
 *
 * The [catch][Flow.catch] operator only catches upstream exceptions, but passes
 * all downstream exceptions. Similarly, terminal operators like [collect][Flow.collect]
 * throw any unhandled exceptions that occur in their code or in upstream flows, for example:
 *
 * ```
 * flow { emitData() }
 *     .map { computeOne(it) }
 *     .catch { ... } // catches exceptions in emitData and computeOne
 *     .map { computeTwo(it) }
 *     .collect { process(it) } // throws exceptions from process and computeTwo
 * ```
 * The same reasoning can be applied to the [onCompletion] operator that is a declarative replacement for the `finally` block.
 *
 * All exception-handling Flow operators follow the principle of exception suppression:
 *
 * If the upstream flow throws an exception during its completion when the downstream exception has been thrown,
 * the downstream exception becomes superseded and suppressed by the upstream exception, being a semantic
 * equivalent of throwing from `finally` block. However, this doesn't affect the operation of the exception-handling operators,
 * which consider the downstream exception to be the root cause and behave as if the upstream didn't throw anything.
 *
 * Failure to adhere to the exception transparency requirement can lead to strange behaviors which make
 * it hard to reason about the code because an exception in the `collect { ... }` could be somehow "caught"
 * by an upstream flow, limiting the ability of local reasoning about the code.
 *
 * Flow machinery enforces exception transparency at runtime and throws [IllegalStateException] on any attempt to emit a value,
 * if an exception has been thrown on previous attempt.
 *
 * ### Reactive streams
 *
 * Flow is [Reactive Streams](http://www.reactive-streams.org/) compliant, you can safely interop it with
 * reactive streams using `Flow.asPublisher` and `Publisher.asFlow` from `kotlinx-coroutines-reactive` module.
 *
 * ### Not stable for inheritance
 *
 * **The `Flow` interface is not stable for inheritance in 3rd party libraries**, as new methods
 * might be added to this interface in the future, but is stable for use.
 *
 * Use the `flow { ... }` builder function to create an implementation, or extend [AbstractFlow].
 * These implementations ensure that the context preservation property is not violated, and prevent most
 * of the developer mistakes related to concurrency, inconsistent flow dispatchers, and cancellation.
 */
__attribute__((swift_name("Kotlinx_coroutines_coreFlow")))
@protocol KmpCalendarKotlinx_coroutines_coreFlow
@required

/**
 * Accepts the given [collector] and [emits][FlowCollector.emit] values into it.
 *
 * This method can be used along with SAM-conversion of [FlowCollector]:
 * ```
 * myFlow.collect { value -> println("Collected $value") }
 * ```
 *
 * ### Method inheritance
 *
 * To ensure the context preservation property, it is not recommended implementing this method directly.
 * Instead, [AbstractFlow] can be used as the base type to properly ensure flow's properties.
 *
 * All default flow implementations ensure context preservation and exception transparency properties on a best-effort basis
 * and throw [IllegalStateException] if a violation was detected.
 *
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinFlow")))
@interface KmpCalendarSkieKotlinFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreFlow>
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end


/**
 * A _hot_ [Flow] that shares emitted values among all its collectors in a broadcast fashion, so that all collectors
 * get all emitted values. A shared flow is called _hot_ because its active instance exists independently of the
 * presence of collectors. This is opposed to a regular [Flow], such as defined by the [`flow { ... }`][flow] function,
 * which is _cold_ and is started separately for each collector.
 *
 * **Shared flow never completes**. A call to [Flow.collect] on a shared flow never completes normally, and
 * neither does a coroutine started by the [Flow.launchIn] function. An active collector of a shared flow is called a _subscriber_.
 *
 * A subscriber of a shared flow can be cancelled. This usually happens when the scope in which the coroutine is running
 * is cancelled. A subscriber to a shared flow is always [cancellable][Flow.cancellable], and checks for
 * cancellation before each emission. Note that most terminal operators like [Flow.toList] would also not complete,
 * when applied to a shared flow, but flow-truncating operators like [Flow.take] and [Flow.takeWhile] can be used on a
 * shared flow to turn it into a completing one.
 *
 * A [mutable shared flow][MutableSharedFlow] is created using the [MutableSharedFlow(...)] constructor function.
 * Its state can be updated by [emitting][MutableSharedFlow.emit] values to it and performing other operations.
 * See the [MutableSharedFlow] documentation for details.
 *
 * [SharedFlow] is useful for broadcasting events that happen inside an application to subscribers that can come and go.
 * For example, the following class encapsulates an event bus that distributes events to all subscribers
 * in a _rendezvous_ manner, suspending until all subscribers receive emitted event:
 *
 * ```
 * class EventBus {
 *     private val _events = MutableSharedFlow<Event>() // private mutable shared flow
 *     val events = _events.asSharedFlow() // publicly exposed as read-only shared flow
 *
 *     suspend fun produceEvent(event: Event) {
 *         _events.emit(event) // suspends until all subscribers receive it
 *     }
 * }
 * ```
 *
 * As an alternative to the above usage with the `MutableSharedFlow(...)` constructor function,
 * any _cold_ [Flow] can be converted to a shared flow using the [shareIn] operator.
 *
 * There is a specialized implementation of shared flow for the case where the most recent state value needs
 * to be shared. See [StateFlow] for details.
 *
 * ### Replay cache and buffer
 *
 * A shared flow keeps a specific number of the most recent values in its _replay cache_. Every new subscriber first
 * gets the values from the replay cache and then gets new emitted values. The maximum size of the replay cache is
 * specified when the shared flow is created by the `replay` parameter. A snapshot of the current replay cache
 * is available via the [replayCache] property and it can be reset with the [MutableSharedFlow.resetReplayCache] function.
 *
 * A replay cache also provides buffer for emissions to the shared flow, allowing slow subscribers to
 * get values from the buffer without suspending emitters. The buffer space determines how much slow subscribers
 * can lag from the fast ones. When creating a shared flow, additional buffer capacity beyond replay can be reserved
 * using the `extraBufferCapacity` parameter.
 *
 * A shared flow with a buffer can be configured to avoid suspension of emitters on buffer overflow using
 * the `onBufferOverflow` parameter, which is equal to one of the entries of the [BufferOverflow] enum. When a strategy other
 * than [SUSPENDED][BufferOverflow.SUSPEND] is configured, emissions to the shared flow never suspend.
 *
 * **Buffer overflow condition can happen only when there is at least one subscriber that is not ready to accept
 * the new value.**  In the absence of subscribers only the most recent `replay` values are stored and the buffer
 * overflow behavior is never triggered and has no effect. In particular, in the absence of subscribers emitter never
 * suspends despite [BufferOverflow.SUSPEND] option and [BufferOverflow.DROP_LATEST] option does not have effect either.
 * Essentially, the behavior in the absence of subscribers is always similar to [BufferOverflow.DROP_OLDEST],
 * but the buffer is just of `replay` size (without any `extraBufferCapacity`).
 *
 * ### Unbuffered shared flow
 *
 * A default implementation of a shared flow that is created with `MutableSharedFlow()` constructor function
 * without parameters has no replay cache nor additional buffer.
 * [emit][MutableSharedFlow.emit] call to such a shared flow suspends until all subscribers receive the emitted value
 * and returns immediately if there are no subscribers.
 * Thus, [tryEmit][MutableSharedFlow.tryEmit] call succeeds and returns `true` only if
 * there are no subscribers (in which case the emitted value is immediately lost).
 *
 * ### SharedFlow vs BroadcastChannel
 *
 * Conceptually shared flow is similar to [BroadcastChannel][BroadcastChannel]
 * and is designed to completely replace it.
 * It has the following important differences:
 *
 * - `SharedFlow` is simpler, because it does not have to implement all the [Channel] APIs, which allows
 *   for faster and simpler implementation.
 * - `SharedFlow` supports configurable replay and buffer overflow strategy.
 * - `SharedFlow` has a clear separation into a read-only `SharedFlow` interface and a [MutableSharedFlow].
 * - `SharedFlow` cannot be closed like `BroadcastChannel` and can never represent a failure.
 *   All errors and completion signals should be explicitly _materialized_ if needed.
 *
 * To migrate [BroadcastChannel] usage to [SharedFlow], start by replacing usages of the `BroadcastChannel(capacity)`
 * constructor with `MutableSharedFlow(0, extraBufferCapacity=capacity)` (broadcast channel does not replay
 * values to new subscribers). Replace [send][BroadcastChannel.send] and [trySend][BroadcastChannel.trySend] calls
 * with [emit][MutableStateFlow.emit] and [tryEmit][MutableStateFlow.tryEmit], and convert subscribers' code to flow operators.
 *
 * ### Concurrency
 *
 * All methods of shared flow are **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 *
 * ### Operator fusion
 *
 * Application of [flowOn][Flow.flowOn], [buffer] with [RENDEZVOUS][Channel.RENDEZVOUS] capacity,
 * or [cancellable] operators to a shared flow has no effect.
 *
 * ### Implementation notes
 *
 * Shared flow implementation uses a lock to ensure thread-safety, but suspending collector and emitter coroutines are
 * resumed outside of this lock to avoid deadlocks when using unconfined coroutines. Adding new subscribers
 * has `O(1)` amortized cost, but emitting has `O(N)` cost, where `N` is the number of subscribers.
 *
 * ### Not stable for inheritance
 *
 * **The `SharedFlow` interface is not stable for inheritance in 3rd party libraries**, as new methods
 * might be added to this interface in the future, but is stable for use.
 * Use the `MutableSharedFlow(replay, ...)` constructor function to create an implementation.
 *
 * @note annotations
 *   kotlin.SubclassOptInRequired(markerClass=[NormalClass(value=kotlinx/coroutines/ExperimentalForInheritanceCoroutinesApi)])
*/
__attribute__((swift_name("Kotlinx_coroutines_coreSharedFlow")))
@protocol KmpCalendarKotlinx_coroutines_coreSharedFlow <KmpCalendarKotlinx_coroutines_coreFlow>
@required

/**
 * A snapshot of the replay cache.
 */
@property (readonly) NSArray<id> *replayCache __attribute__((swift_name("replayCache")));
@end


/**
 * [FlowCollector] is used as an intermediate or a terminal collector of the flow and represents
 * an entity that accepts values emitted by the [Flow].
 *
 * This interface should usually not be implemented directly, but rather used as a receiver in a [flow] builder when implementing a custom operator,
 * or with SAM-conversion.
 * Implementations of this interface are not thread-safe.
 *
 * Example of usage:
 *
 * ```
 * val flow = getMyEvents()
 * try {
 *     flow.collect { value ->
 *         println("Received $value")
 *     }
 *     println("My events are consumed successfully")
 * } catch (e: Throwable) {
 *     println("Exception from the flow: $e")
 * }
 * ```
 */
__attribute__((swift_name("Kotlinx_coroutines_coreFlowCollector")))
@protocol KmpCalendarKotlinx_coroutines_coreFlowCollector
@required

/**
 * Collects the value emitted by the upstream.
 * This method is not thread-safe and should not be invoked concurrently.
 *
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)emitValue:(id _Nullable)value completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("emit(value:completionHandler:)")));
@end


/**
 * A mutable [SharedFlow] that provides functions to [emit] values to the flow.
 * An instance of `MutableSharedFlow` with the given configuration parameters can be created using `MutableSharedFlow(...)`
 * constructor function.
 *
 * See the [SharedFlow] documentation for details on shared flows.
 *
 * `MutableSharedFlow` is a [SharedFlow] that also provides the abilities to [emit] a value,
 * to [tryEmit] without suspension if possible, to track the [subscriptionCount],
 * and to [resetReplayCache].
 *
 * ### Concurrency
 *
 * All methods of shared flow are **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 *
 * ### Not stable for inheritance
 *
 * **The `MutableSharedFlow` interface is not stable for inheritance in 3rd party libraries**, as new methods
 * might be added to this interface in the future, but is stable for use.
 * Use the `MutableSharedFlow(...)` constructor function to create an implementation.
 *
 * @note annotations
 *   kotlin.SubclassOptInRequired(markerClass=[NormalClass(value=kotlinx/coroutines/ExperimentalForInheritanceCoroutinesApi)])
*/
__attribute__((swift_name("Kotlinx_coroutines_coreMutableSharedFlow")))
@protocol KmpCalendarKotlinx_coroutines_coreMutableSharedFlow <KmpCalendarKotlinx_coroutines_coreSharedFlow, KmpCalendarKotlinx_coroutines_coreFlowCollector>
@required

/**
 * Resets the [replayCache] of this shared flow to an empty state.
 * New subscribers will be receiving only the values that were emitted after this call,
 * while old subscribers will still be receiving previously buffered values.
 * To reset a shared flow to an initial value, emit the value after this call.
 *
 * On a [MutableStateFlow], which always contains a single value, this function is not
 * supported, and throws an [UnsupportedOperationException]. To reset a [MutableStateFlow]
 * to an initial value, just update its [value][MutableStateFlow.value].
 *
 * This method is **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 *
 * **Note: This is an experimental api.** This function may be removed or renamed in the future.
 *
 * @note annotations
 *   kotlinx.coroutines.ExperimentalCoroutinesApi
*/
- (void)resetReplayCache __attribute__((swift_name("resetReplayCache()")));

/**
 * Tries to emit a [value] to this shared flow without suspending. It returns `true` if the value was
 * emitted successfully (see below). When this function returns `false`, it means that a call to a plain [emit]
 * function would suspend until there is buffer space available.
 *
 * This call can return `false` only when the [BufferOverflow] strategy is
 * [SUSPEND][BufferOverflow.SUSPEND] **and** there are subscribers collecting this shared flow.
 *
 * If there are no subscribers, the buffer is not used.
 * Instead, the most recently emitted value is simply stored into
 * the replay cache if one was configured, displacing the older elements there,
 * or dropped if no replay cache was configured. In any case, `tryEmit` returns `true`.
 *
 * This method is **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 */
- (BOOL)tryEmitValue:(id _Nullable)value __attribute__((swift_name("tryEmit(value:)")));

/**
 * The number of subscribers (active collectors) to this shared flow.
 *
 * The integer in the resulting [StateFlow] is not negative and starts with zero for a freshly created
 * shared flow.
 *
 * This state can be used to react to changes in the number of subscriptions to this shared flow.
 * For example, if you need to call `onActive` when the first subscriber appears and `onInactive`
 * when the last one disappears, you can set it up like this:
 *
 * ```
 * sharedFlow.subscriptionCount
 *     .map { count -> count > 0 } // map count into active/inactive flag
 *     .distinctUntilChanged() // only react to true<->false changes
 *     .onEach { isActive -> // configure an action
 *         if (isActive) onActive() else onInactive()
 *     }
 *     .launchIn(scope) // launch it
 * ```
 *
 * Usually, [StateFlow] conflates values, but [subscriptionCount] is not conflated.
 * This is done so that any subscribers that need to be notified when subscribers appear do
 * reliably observe it. With conflation, if a single subscriber appeared and immediately left, those
 * collecting [subscriptionCount] could fail to notice it due to `0` immediately conflating the
 * subscription count.
 */
@property (readonly) id<KmpCalendarKotlinx_coroutines_coreStateFlow> subscriptionCount __attribute__((swift_name("subscriptionCount")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinMutableSharedFlow")))
@interface KmpCalendarSkieKotlinMutableSharedFlow<T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreMutableSharedFlow>
@property (readonly) NSArray<T> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) id<KmpCalendarKotlinx_coroutines_coreStateFlow> subscriptionCount __attribute__((swift_name("subscriptionCount")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreMutableSharedFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)emitValue:(T)value completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("emit(value:completionHandler:)")));

/**
 * @note annotations
 *   kotlinx.coroutines.ExperimentalCoroutinesApi
*/
- (void)resetReplayCache __attribute__((swift_name("resetReplayCache()")));
- (BOOL)tryEmitValue:(T)value __attribute__((swift_name("tryEmit(value:)")));
@end


/**
 * A [SharedFlow] that represents a read-only state with a single updatable data [value] that emits updates
 * to the value to its collectors. A state flow is a _hot_ flow because its active instance exists independently
 * of the presence of collectors. Its current value can be retrieved via the [value] property.
 *
 * **State flow never completes**. A call to [Flow.collect] on a state flow never completes normally, and
 * neither does a coroutine started by the [Flow.launchIn] function. An active collector of a state flow is called a _subscriber_.
 *
 * A [mutable state flow][MutableStateFlow] is created using `MutableStateFlow(value)` constructor function with
 * the initial value. The value of mutable state flow can be updated by setting its [value] property.
 * Updates to the [value] are always [conflated][Flow.conflate]. So a slow collector skips fast updates,
 * but always collects the most recently emitted value.
 *
 * [StateFlow] is useful as a data-model class to represent any kind of state.
 * Derived values can be defined using various operators on the flows, with [combine] operator being especially
 * useful to combine values from multiple state flows using arbitrary functions.
 *
 * For example, the following class encapsulates an integer state and increments its value on each call to `inc`:
 *
 * ```
 * class CounterModel {
 *     private val _counter = MutableStateFlow(0) // private mutable state flow
 *     val counter = _counter.asStateFlow() // publicly exposed as read-only state flow
 *
 *     fun inc() {
 *         _counter.update { count -> count + 1 } // atomic, safe for concurrent use
 *     }
 * }
 * ```
 *
 * Having two instances of the above `CounterModel` class one can define the sum of their counters like this:
 *
 * ```
 * val aModel = CounterModel()
 * val bModel = CounterModel()
 * val sumFlow: Flow<Int> = aModel.counter.combine(bModel.counter) { a, b -> a + b }
 * ```
 *
 * As an alternative to the above usage with the `MutableStateFlow(...)` constructor function,
 * any _cold_ [Flow] can be converted to a state flow using the [stateIn] operator.
 *
 * ### Strong equality-based conflation
 *
 * Values in state flow are conflated using [Any.equals] comparison in a similar way to
 * [distinctUntilChanged] operator. It is used to conflate incoming updates
 * to [value][MutableStateFlow.value] in [MutableStateFlow] and to suppress emission of the values to collectors
 * when new value is equal to the previously emitted one. State flow behavior with classes that violate
 * the contract for [Any.equals] is unspecified.
 *
 * ### State flow is a shared flow
 *
 * State flow is a special-purpose, high-performance, and efficient implementation of [SharedFlow] for the narrow,
 * but widely used case of sharing a state. See the [SharedFlow] documentation for the basic rules,
 * constraints, and operators that are applicable to all shared flows.
 *
 * State flow always has an initial value, replays one most recent value to new subscribers, does not buffer any
 * more values, but keeps the last emitted one, and does not support [resetReplayCache][MutableSharedFlow.resetReplayCache].
 * A state flow behaves identically to a shared flow when it is created
 * with the following parameters and the [distinctUntilChanged] operator is applied to it:
 *
 * ```
 * // MutableStateFlow(initialValue) is a shared flow with the following parameters:
 * val shared = MutableSharedFlow(
 *     replay = 1,
 *     onBufferOverflow = BufferOverflow.DROP_OLDEST
 * )
 * shared.tryEmit(initialValue) // emit the initial value
 * val state = shared.distinctUntilChanged() // get StateFlow-like behavior
 * ```
 *
 * Use [SharedFlow] when you need a [StateFlow] with tweaks in its behavior such as extra buffering, replaying more
 * values, or omitting the initial value.
 *
 * ### StateFlow vs ConflatedBroadcastChannel
 *
 * Conceptually, state flow is similar to [ConflatedBroadcastChannel]
 * and is designed to completely replace it.
 * It has the following important differences:
 *
 * - `StateFlow` is simpler, because it does not have to implement all the [Channel] APIs, which allows
 *   for faster, garbage-free implementation, unlike `ConflatedBroadcastChannel` implementation that
 *   allocates objects on each emitted value.
 * - `StateFlow` always has a value which can be safely read at any time via [value] property.
 *   Unlike `ConflatedBroadcastChannel`, there is no way to create a state flow without a value.
 * - `StateFlow` has a clear separation into a read-only `StateFlow` interface and a [MutableStateFlow].
 * - `StateFlow` conflation is based on equality like [distinctUntilChanged] operator,
 *   unlike conflation in `ConflatedBroadcastChannel` that is based on reference identity.
 * - `StateFlow` cannot be closed like `ConflatedBroadcastChannel` and can never represent a failure.
 *   All errors and completion signals should be explicitly _materialized_ if needed.
 *
 * `StateFlow` is designed to better cover typical use-cases of keeping track of state changes in time, taking
 * more pragmatic design choices for the sake of convenience.
 *
 * To migrate [ConflatedBroadcastChannel] usage to [StateFlow], start by replacing usages of the `ConflatedBroadcastChannel()`
 * constructor with `MutableStateFlow(initialValue)`, using `null` as an initial value if you don't have one.
 * Replace [send][ConflatedBroadcastChannel.send] and [trySend][ConflatedBroadcastChannel.trySend] calls
 * with updates to the state flow's [MutableStateFlow.value], and convert subscribers' code to flow operators.
 * You can use the [filterNotNull] operator to mimic behavior of a `ConflatedBroadcastChannel` without initial value.
 *
 * ### Concurrency
 *
 * All methods of state flow are **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 *
 * ### Operator fusion
 *
 * Application of [flowOn][Flow.flowOn], [conflate][Flow.conflate],
 * [buffer] with [CONFLATED][Channel.CONFLATED] or [RENDEZVOUS][Channel.RENDEZVOUS] capacity,
 * [distinctUntilChanged][Flow.distinctUntilChanged], or [cancellable] operators to a state flow has no effect.
 *
 * ### Implementation notes
 *
 * State flow implementation is optimized for memory consumption and allocation-freedom. It uses a lock to ensure
 * thread-safety, but suspending collector coroutines are resumed outside of this lock to avoid dead-locks when
 * using unconfined coroutines. Adding new subscribers has `O(1)` amortized cost, but updating a [value] has `O(N)`
 * cost, where `N` is the number of active subscribers.
 *
 * ### Not stable for inheritance
 *
 * **`The StateFlow` interface is not stable for inheritance in 3rd party libraries**, as new methods
 * might be added to this interface in the future, but is stable for use.
 * Use the `MutableStateFlow(value)` constructor function to create an implementation.
 *
 * @note annotations
 *   kotlin.SubclassOptInRequired(markerClass=[NormalClass(value=kotlinx/coroutines/ExperimentalForInheritanceCoroutinesApi)])
*/
__attribute__((swift_name("Kotlinx_coroutines_coreStateFlow")))
@protocol KmpCalendarKotlinx_coroutines_coreStateFlow <KmpCalendarKotlinx_coroutines_coreSharedFlow>
@required

/**
 * The current value of this state flow.
 */
@property (readonly) id _Nullable value __attribute__((swift_name("value")));
@end


/**
 * A mutable [StateFlow] that provides a setter for [value].
 * An instance of `MutableStateFlow` with the given initial `value` can be created using
 * `MutableStateFlow(value)` constructor function.
 * See the [StateFlow] documentation for details on state flows.
 * Note that all emission-related operators, such as [value]'s setter, [emit], and [tryEmit], are conflated using [Any.equals].
 *
 * ### Not stable for inheritance
 *
 * **The `MutableStateFlow` interface is not stable for inheritance in 3rd party libraries**, as new methods
 * might be added to this interface in the future, but is stable for use.
 * Use the `MutableStateFlow()` constructor function to create an implementation.
 *
 * @note annotations
 *   kotlin.SubclassOptInRequired(markerClass=[NormalClass(value=kotlinx/coroutines/ExperimentalForInheritanceCoroutinesApi)])
*/
__attribute__((swift_name("Kotlinx_coroutines_coreMutableStateFlow")))
@protocol KmpCalendarKotlinx_coroutines_coreMutableStateFlow <KmpCalendarKotlinx_coroutines_coreStateFlow, KmpCalendarKotlinx_coroutines_coreMutableSharedFlow>
@required
- (void)setValue:(id _Nullable)value __attribute__((swift_name("setValue(_:)")));

/**
 * Atomically compares the current [value] with [expect] and sets it to [update] if it is equal to [expect].
 * The result is `true` if the [value] was set to [update] and `false` otherwise.
 *
 * This function use a regular comparison using [Any.equals]. If both [expect] and [update] are equal to the
 * current [value], this function returns `true`, but it does not actually change the reference that is
 * stored in the [value].
 *
 * This method is **thread-safe** and can be safely invoked from concurrent coroutines without
 * external synchronization.
 */
- (BOOL)compareAndSetExpect:(id _Nullable)expect update:(id _Nullable)update __attribute__((swift_name("compareAndSet(expect:update:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinMutableStateFlow")))
@interface KmpCalendarSkieKotlinMutableStateFlow<T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreMutableStateFlow>
@property (readonly) NSArray<T> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) id<KmpCalendarKotlinx_coroutines_coreStateFlow> subscriptionCount __attribute__((swift_name("subscriptionCount")));
@property T value __attribute__((swift_name("value")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreMutableStateFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
- (BOOL)compareAndSetExpect:(T)expect update:(T)update __attribute__((swift_name("compareAndSet(expect:update:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)emitValue:(T)value completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("emit(value:completionHandler:)")));

/**
 * @note annotations
 *   kotlinx.coroutines.ExperimentalCoroutinesApi
*/
- (void)resetReplayCache __attribute__((swift_name("resetReplayCache()")));
- (BOOL)tryEmitValue:(T)value __attribute__((swift_name("tryEmit(value:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinOptionalFlow")))
@interface KmpCalendarSkieKotlinOptionalFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreFlow>
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinOptionalMutableSharedFlow")))
@interface KmpCalendarSkieKotlinOptionalMutableSharedFlow<T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreMutableSharedFlow>
@property (readonly) NSArray<id> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) id<KmpCalendarKotlinx_coroutines_coreStateFlow> subscriptionCount __attribute__((swift_name("subscriptionCount")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreMutableSharedFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)emitValue:(T _Nullable)value completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("emit(value:completionHandler:)")));

/**
 * @note annotations
 *   kotlinx.coroutines.ExperimentalCoroutinesApi
*/
- (void)resetReplayCache __attribute__((swift_name("resetReplayCache()")));
- (BOOL)tryEmitValue:(T _Nullable)value __attribute__((swift_name("tryEmit(value:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinOptionalMutableStateFlow")))
@interface KmpCalendarSkieKotlinOptionalMutableStateFlow<T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreMutableStateFlow>
@property (readonly) NSArray<id> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) id<KmpCalendarKotlinx_coroutines_coreStateFlow> subscriptionCount __attribute__((swift_name("subscriptionCount")));
@property T _Nullable value __attribute__((swift_name("value")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreMutableStateFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
- (BOOL)compareAndSetExpect:(T _Nullable)expect update:(T _Nullable)update __attribute__((swift_name("compareAndSet(expect:update:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)emitValue:(T _Nullable)value completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("emit(value:completionHandler:)")));

/**
 * @note annotations
 *   kotlinx.coroutines.ExperimentalCoroutinesApi
*/
- (void)resetReplayCache __attribute__((swift_name("resetReplayCache()")));
- (BOOL)tryEmitValue:(T _Nullable)value __attribute__((swift_name("tryEmit(value:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinOptionalSharedFlow")))
@interface KmpCalendarSkieKotlinOptionalSharedFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreSharedFlow>
@property (readonly) NSArray<id> *replayCache __attribute__((swift_name("replayCache")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreSharedFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinOptionalStateFlow")))
@interface KmpCalendarSkieKotlinOptionalStateFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreStateFlow>
@property (readonly) NSArray<id> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) T _Nullable value __attribute__((swift_name("value")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreStateFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinSharedFlow")))
@interface KmpCalendarSkieKotlinSharedFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreSharedFlow>
@property (readonly) NSArray<T> *replayCache __attribute__((swift_name("replayCache")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreSharedFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("SkieKotlinStateFlow")))
@interface KmpCalendarSkieKotlinStateFlow<__covariant T> : KmpCalendarBase <KmpCalendarKotlinx_coroutines_coreStateFlow>
@property (readonly) NSArray<T> *replayCache __attribute__((swift_name("replayCache")));
@property (readonly) T value __attribute__((swift_name("value")));
- (instancetype)initWithDelegate:(id<KmpCalendarKotlinx_coroutines_coreStateFlow>)delegate __attribute__((swift_name("init(_:)"))) __attribute__((objc_designated_initializer));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)collectCollector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("collect(collector:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Skie_CancellationHandler")))
@interface KmpCalendarSkie_CancellationHandler : KmpCalendarBase
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (void)cancel __attribute__((swift_name("cancel()")));
@end

__attribute__((swift_name("Skie_DispatcherDelegate")))
@protocol KmpCalendarSkie_DispatcherDelegate
@required
- (void)dispatchBlock:(id<KmpCalendarKotlinx_coroutines_coreRunnable>)block __attribute__((swift_name("dispatch(block:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Skie_SuspendHandler")))
@interface KmpCalendarSkie_SuspendHandler : KmpCalendarBase
- (instancetype)initWithCancellationHandler:(KmpCalendarSkie_CancellationHandler *)cancellationHandler dispatcherDelegate:(id<KmpCalendarSkie_DispatcherDelegate>)dispatcherDelegate onResult:(void (^)(KmpCalendarSkie_SuspendResult *))onResult __attribute__((swift_name("init(cancellationHandler:dispatcherDelegate:onResult:)"))) __attribute__((objc_designated_initializer));
@end

__attribute__((swift_name("Skie_SuspendResult")))
@interface KmpCalendarSkie_SuspendResult : KmpCalendarBase
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Skie_SuspendResult.Canceled")))
@interface KmpCalendarSkie_SuspendResultCanceled : KmpCalendarSkie_SuspendResult
@property (class, readonly, getter=shared) KmpCalendarSkie_SuspendResultCanceled *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)canceled __attribute__((swift_name("init()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Skie_SuspendResult.Error")))
@interface KmpCalendarSkie_SuspendResultError : KmpCalendarSkie_SuspendResult
@property (readonly) NSError *error __attribute__((swift_name("error")));
- (instancetype)initWithError:(NSError *)error __attribute__((swift_name("init(error:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarSkie_SuspendResultError *)doCopyError:(NSError *)error __attribute__((swift_name("doCopy(error:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Skie_SuspendResult.Success")))
@interface KmpCalendarSkie_SuspendResultSuccess : KmpCalendarSkie_SuspendResult
@property (readonly) id _Nullable value __attribute__((swift_name("value")));
- (instancetype)initWithValue:(id _Nullable)value __attribute__((swift_name("init(value:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarSkie_SuspendResultSuccess *)doCopyValue:(id _Nullable)value __attribute__((swift_name("doCopy(value:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((swift_name("KtorClientProvider")))
@protocol KmpCalendarKtorClientProvider
@required
@end


/**
 * Shared contract for the Calendar dependency graph.
 *
 * Automatically merged as a supertype of any `@DependencyGraph(AppScope::class)`:
 * - On Android: merged into the app's `AppGraph`
 * - On Apple: merged into `CalendarSDK`
 *
 * Platform-specific graphs only need to provide the [android.content.Context] (Android)
 * or the database path (Apple) — everything else is wired through `@Inject` classes
 * and `@ContributesTo` modules (`DatabaseModule`, `AndroidDatabaseModule`, `CaldavClientModule`).
 */
__attribute__((swift_name("CalendarCoreGraph")))
@protocol KmpCalendarCalendarCoreGraph
@required
@property (readonly) KmpCalendarAccountManager *accountManager __attribute__((swift_name("accountManager")));
@property (readonly) KmpCalendarCalendarManager *calendarManager __attribute__((swift_name("calendarManager")));
@end

__attribute__((swift_name("DatabaseModule")))
@protocol KmpCalendarDatabaseModule
@required
@end


/**
 * CalDAV account credentials, as seen by public consumers of the library.
 *
 * This is the Core-owned counterpart of the internal `:kmpdav` `DavAccount`
 * (mapped at the repository boundary): keeping a dedicated domain type here lets
 * the bridge module stay an implementation detail that is never exported into
 * the public Apple framework.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("DavCredentials")))
@interface KmpCalendarDavCredentials : KmpCalendarBase
@property (readonly) NSString *password __attribute__((swift_name("password")));
@property (readonly) NSString *username __attribute__((swift_name("username")));
- (instancetype)initWithUsername:(NSString *)username password:(NSString *)password __attribute__((swift_name("init(username:password:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarDavCredentials *)doCopyUsername:(NSString *)username password:(NSString *)password __attribute__((swift_name("doCopy(username:password:)")));
- (KmpCalendarDavCredentials *)doCopy__Skie_DefaultArguments__130 __attribute__((swift_name("doCopy__Skie_DefaultArguments__130()")));
- (KmpCalendarDavCredentials *)doCopy__Skie_DefaultArguments__131Username:(NSString *)username __attribute__((swift_name("doCopy__Skie_DefaultArguments__131(username:)")));
- (KmpCalendarDavCredentials *)doCopy__Skie_DefaultArguments__132Password:(NSString *)password __attribute__((swift_name("doCopy__Skie_DefaultArguments__132(password:)")));

/**
 * CalDAV account credentials, as seen by public consumers of the library.
 *
 * This is the Core-owned counterpart of the internal `:kmpdav` `DavAccount`
 * (mapped at the repository boundary): keeping a dedicated domain type here lets
 * the bridge module stay an implementation detail that is never exported into
 * the public Apple framework.
 */
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));

/**
 * CalDAV account credentials, as seen by public consumers of the library.
 *
 * This is the Core-owned counterpart of the internal `:kmpdav` `DavAccount`
 * (mapped at the repository boundary): keeping a dedicated domain type here lets
 * the bridge module stay an implementation detail that is never exported into
 * the public Apple framework.
 */
- (NSUInteger)hash __attribute__((swift_name("hash()")));

/**
 * CalDAV account credentials, as seen by public consumers of the library.
 *
 * This is the Core-owned counterpart of the internal `:kmpdav` `DavAccount`
 * (mapped at the repository boundary): keeping a dedicated domain type here lets
 * the bridge module stay an implementation detail that is never exported into
 * the public Apple framework.
 */
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Calendar")))
@interface KmpCalendarCalendar : KmpCalendarBase
@property (readonly) KmpCalendarCalendarAccessLevel *accessLevel __attribute__((swift_name("accessLevel")));
@property (readonly) int32_t color __attribute__((swift_name("color")));
@property (readonly) NSString *displayName __attribute__((swift_name("displayName")));
@property (readonly) BOOL isVisible __attribute__((swift_name("isVisible")));
- (instancetype)initWithId:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("init(id:accountId:displayName:color:isVisible:accessLevel:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithId:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible__Skie_DefaultArguments__147:(BOOL)isVisible__Skie_DefaultArguments__147 __attribute__((swift_name("init(id:accountId:displayName:color:isVisible__Skie_DefaultArguments__147:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarCalendar *)doCopyId:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy(id:accountId:displayName:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__0 __attribute__((swift_name("doCopy__Skie_DefaultArguments__0()")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__1Id:(id)id __attribute__((swift_name("doCopy__Skie_DefaultArguments__1(id:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__10AccountId:(int64_t)accountId color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__10(accountId:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__11Id:(id)id accountId:(int64_t)accountId color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__11(id:accountId:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__12DisplayName:(NSString *)displayName color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__12(displayName:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__13Id:(id)id displayName:(NSString *)displayName color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__13(id:displayName:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__14AccountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__14(accountId:displayName:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__15Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__15(id:accountId:displayName:color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__16IsVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__16(isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__17Id:(id)id isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__17(id:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__18AccountId:(int64_t)accountId isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__18(accountId:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__19Id:(id)id accountId:(int64_t)accountId isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__19(id:accountId:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__2AccountId:(int64_t)accountId __attribute__((swift_name("doCopy__Skie_DefaultArguments__2(accountId:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__20DisplayName:(NSString *)displayName isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__20(displayName:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__21Id:(id)id displayName:(NSString *)displayName isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__21(id:displayName:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__22AccountId:(int64_t)accountId displayName:(NSString *)displayName isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__22(accountId:displayName:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__23Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__23(id:accountId:displayName:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__24Color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__24(color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__25Id:(id)id color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__25(id:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__26AccountId:(int64_t)accountId color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__26(accountId:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__27Id:(id)id accountId:(int64_t)accountId color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__27(id:accountId:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__28DisplayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__28(displayName:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__29Id:(id)id displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__29(id:displayName:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__3Id:(id)id accountId:(int64_t)accountId __attribute__((swift_name("doCopy__Skie_DefaultArguments__3(id:accountId:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__30AccountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__30(accountId:displayName:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__31Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible __attribute__((swift_name("doCopy__Skie_DefaultArguments__31(id:accountId:displayName:color:isVisible:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__32AccessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__32(accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__33Id:(id)id accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__33(id:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__34AccountId:(int64_t)accountId accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__34(accountId:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__35Id:(id)id accountId:(int64_t)accountId accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__35(id:accountId:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__36DisplayName:(NSString *)displayName accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__36(displayName:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__37Id:(id)id displayName:(NSString *)displayName accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__37(id:displayName:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__38AccountId:(int64_t)accountId displayName:(NSString *)displayName accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__38(accountId:displayName:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__39Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__39(id:accountId:displayName:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__4DisplayName:(NSString *)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__4(displayName:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__40Color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__40(color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__41Id:(id)id color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__41(id:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__42AccountId:(int64_t)accountId color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__42(accountId:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__43Id:(id)id accountId:(int64_t)accountId color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__43(id:accountId:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__44DisplayName:(NSString *)displayName color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__44(displayName:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__45Id:(id)id displayName:(NSString *)displayName color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__45(id:displayName:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__46AccountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__46(accountId:displayName:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__47Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__47(id:accountId:displayName:color:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__48IsVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__48(isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__49Id:(id)id isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__49(id:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__5Id:(id)id displayName:(NSString *)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__5(id:displayName:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__50AccountId:(int64_t)accountId isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__50(accountId:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__51Id:(id)id accountId:(int64_t)accountId isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__51(id:accountId:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__52DisplayName:(NSString *)displayName isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__52(displayName:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__53Id:(id)id displayName:(NSString *)displayName isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__53(id:displayName:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__54AccountId:(int64_t)accountId displayName:(NSString *)displayName isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__54(accountId:displayName:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__55Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__55(id:accountId:displayName:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__56Color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__56(color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__57Id:(id)id color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__57(id:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__58AccountId:(int64_t)accountId color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__58(accountId:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__59Id:(id)id accountId:(int64_t)accountId color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__59(id:accountId:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__6AccountId:(int64_t)accountId displayName:(NSString *)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__6(accountId:displayName:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__60DisplayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__60(displayName:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__61Id:(id)id displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__61(id:displayName:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__62AccountId:(int64_t)accountId displayName:(NSString *)displayName color:(int32_t)color isVisible:(BOOL)isVisible accessLevel:(KmpCalendarCalendarAccessLevel *)accessLevel __attribute__((swift_name("doCopy__Skie_DefaultArguments__62(accountId:displayName:color:isVisible:accessLevel:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__7Id:(id)id accountId:(int64_t)accountId displayName:(NSString *)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__7(id:accountId:displayName:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__8Color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__8(color:)")));
- (KmpCalendarCalendar *)doCopy__Skie_DefaultArguments__9Id:(id)id color:(int32_t)color __attribute__((swift_name("doCopy__Skie_DefaultArguments__9(id:color:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((swift_name("KotlinComparable")))
@protocol KmpCalendarKotlinComparable
@required
- (int32_t)compareToOther:(id _Nullable)other __attribute__((swift_name("compareTo(other:)")));
@end

__attribute__((swift_name("KotlinEnum")))
@interface KmpCalendarKotlinEnum<E> : KmpCalendarBase <KmpCalendarKotlinComparable>
@property (class, readonly, getter=companion) KmpCalendarKotlinEnumCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) NSString *name __attribute__((swift_name("name")));
@property (readonly) int32_t ordinal __attribute__((swift_name("ordinal")));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer));
- (int32_t)compareToOther:(E)other __attribute__((swift_name("compareTo(other:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * Access level the current user holds on a [Calendar], resolved from the
 * CalDAV `current-user-privilege-set` (RFC 3744).
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("CalendarAccessLevel")))
@interface KmpCalendarCalendarAccessLevel : KmpCalendarKotlinEnum<KmpCalendarCalendarAccessLevel *>
@property (class, readonly) KmpCalendarCalendarAccessLevel *none __attribute__((swift_name("none")));
@property (class, readonly) KmpCalendarCalendarAccessLevel *read __attribute__((swift_name("read")));
@property (class, readonly) KmpCalendarCalendarAccessLevel *readWrite __attribute__((swift_name("readWrite")));
@property (class, readonly) KmpCalendarCalendarAccessLevel *owner __attribute__((swift_name("owner")));
@property (class, readonly) NSArray<KmpCalendarCalendarAccessLevel *> *entries __attribute__((swift_name("entries")));
@property (readonly) BOOL canWrite __attribute__((swift_name("canWrite")));
@property (readonly) BOOL isReadOnly __attribute__((swift_name("isReadOnly")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Access level the current user holds on a [Calendar], resolved from the
 * CalDAV `current-user-privilege-set` (RFC 3744).
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarCalendarAccessLevel *> *)values __attribute__((swift_name("values()")));

/** Whether the current user can create/update/delete events in this calendar. */

/** Whether the calendar is visible but not editable by the current user. */
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Attendee")))
@interface KmpCalendarAttendee : KmpCalendarBase
@property (readonly) NSString * _Nullable displayName __attribute__((swift_name("displayName")));
@property (readonly) NSString *email __attribute__((swift_name("email")));
@property (readonly) BOOL isOrganizer __attribute__((swift_name("isOrganizer")));
@property (readonly) BOOL responseNeeded __attribute__((swift_name("responseNeeded")));
@property (readonly) KmpCalendarAttendeeRole *role __attribute__((swift_name("role")));
@property (readonly) KmpCalendarParticipationStatus *status __attribute__((swift_name("status")));
- (instancetype)initWithEmail:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("init(email:displayName:status:role:isOrganizer:responseNeeded:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email status:(KmpCalendarParticipationStatus *)status role__Skie_DefaultArguments__149:(KmpCalendarAttendeeRole *)role__Skie_DefaultArguments__149 __attribute__((swift_name("init(email:status:role__Skie_DefaultArguments__149:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role__Skie_DefaultArguments__150:(KmpCalendarAttendeeRole *)role__Skie_DefaultArguments__150 __attribute__((swift_name("init(email:displayName:status:role__Skie_DefaultArguments__150:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer__Skie_DefaultArguments__151:(BOOL)isOrganizer__Skie_DefaultArguments__151 __attribute__((swift_name("init(email:status:role:isOrganizer__Skie_DefaultArguments__151:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer__Skie_DefaultArguments__152:(BOOL)isOrganizer__Skie_DefaultArguments__152 __attribute__((swift_name("init(email:displayName:status:role:isOrganizer__Skie_DefaultArguments__152:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded__Skie_DefaultArguments__153:(BOOL)responseNeeded__Skie_DefaultArguments__153 __attribute__((swift_name("init(email:status:role:responseNeeded__Skie_DefaultArguments__153:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded__Skie_DefaultArguments__154:(BOOL)responseNeeded__Skie_DefaultArguments__154 __attribute__((swift_name("init(email:displayName:status:role:responseNeeded__Skie_DefaultArguments__154:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithEmail:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded__Skie_DefaultArguments__155:(BOOL)responseNeeded__Skie_DefaultArguments__155 __attribute__((swift_name("init(email:status:role:isOrganizer:responseNeeded__Skie_DefaultArguments__155:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarAttendee *)doCopyEmail:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy(email:displayName:status:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__100DisplayName:(NSString * _Nullable)displayName responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__100(displayName:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__101Email:(NSString *)email displayName:(NSString * _Nullable)displayName responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__101(email:displayName:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__102Status:(KmpCalendarParticipationStatus *)status responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__102(status:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__103Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__103(email:status:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__104DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__104(displayName:status:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__105Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__105(email:displayName:status:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__106Role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__106(role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__107Email:(NSString *)email role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__107(email:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__108DisplayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__108(displayName:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__109Email:(NSString *)email displayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__109(email:displayName:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__110Status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__110(status:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__111Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__111(email:status:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__112DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__112(displayName:status:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__113Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__113(email:displayName:status:role:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__114IsOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__114(isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__115Email:(NSString *)email isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__115(email:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__116DisplayName:(NSString * _Nullable)displayName isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__116(displayName:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__117Email:(NSString *)email displayName:(NSString * _Nullable)displayName isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__117(email:displayName:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__118Status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__118(status:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__119Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__119(email:status:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__120DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__120(displayName:status:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__121Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__121(email:displayName:status:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__122Role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__122(role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__123Email:(NSString *)email role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__123(email:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__124DisplayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__124(displayName:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__125Email:(NSString *)email displayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__125(email:displayName:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__126Status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__126(status:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__127Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__127(email:status:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__128DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__128(displayName:status:role:isOrganizer:responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__66 __attribute__((swift_name("doCopy__Skie_DefaultArguments__66()")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__67Email:(NSString *)email __attribute__((swift_name("doCopy__Skie_DefaultArguments__67(email:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__68DisplayName:(NSString * _Nullable)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__68(displayName:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__69Email:(NSString *)email displayName:(NSString * _Nullable)displayName __attribute__((swift_name("doCopy__Skie_DefaultArguments__69(email:displayName:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__70Status:(KmpCalendarParticipationStatus *)status __attribute__((swift_name("doCopy__Skie_DefaultArguments__70(status:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__71Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status __attribute__((swift_name("doCopy__Skie_DefaultArguments__71(email:status:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__72DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status __attribute__((swift_name("doCopy__Skie_DefaultArguments__72(displayName:status:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__73Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status __attribute__((swift_name("doCopy__Skie_DefaultArguments__73(email:displayName:status:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__74Role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__74(role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__75Email:(NSString *)email role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__75(email:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__76DisplayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__76(displayName:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__77Email:(NSString *)email displayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__77(email:displayName:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__78Status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__78(status:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__79Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__79(email:status:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__80DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__80(displayName:status:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__81Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role __attribute__((swift_name("doCopy__Skie_DefaultArguments__81(email:displayName:status:role:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__82IsOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__82(isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__83Email:(NSString *)email isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__83(email:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__84DisplayName:(NSString * _Nullable)displayName isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__84(displayName:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__85Email:(NSString *)email displayName:(NSString * _Nullable)displayName isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__85(email:displayName:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__86Status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__86(status:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__87Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__87(email:status:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__88DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__88(displayName:status:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__89Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__89(email:displayName:status:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__90Role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__90(role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__91Email:(NSString *)email role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__91(email:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__92DisplayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__92(displayName:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__93Email:(NSString *)email displayName:(NSString * _Nullable)displayName role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__93(email:displayName:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__94Status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__94(status:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__95Email:(NSString *)email status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__95(email:status:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__96DisplayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__96(displayName:status:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__97Email:(NSString *)email displayName:(NSString * _Nullable)displayName status:(KmpCalendarParticipationStatus *)status role:(KmpCalendarAttendeeRole *)role isOrganizer:(BOOL)isOrganizer __attribute__((swift_name("doCopy__Skie_DefaultArguments__97(email:displayName:status:role:isOrganizer:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__98ResponseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__98(responseNeeded:)")));
- (KmpCalendarAttendee *)doCopy__Skie_DefaultArguments__99Email:(NSString *)email responseNeeded:(BOOL)responseNeeded __attribute__((swift_name("doCopy__Skie_DefaultArguments__99(email:responseNeeded:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("AttendeeRole")))
@interface KmpCalendarAttendeeRole : KmpCalendarKotlinEnum<KmpCalendarAttendeeRole *>
@property (class, readonly) KmpCalendarAttendeeRole *organizer __attribute__((swift_name("organizer")));
@property (class, readonly) KmpCalendarAttendeeRole *requested __attribute__((swift_name("requested")));
@property (class, readonly) KmpCalendarAttendeeRole *optional __attribute__((swift_name("optional")));
@property (class, readonly) KmpCalendarAttendeeRole *nonparticipant __attribute__((swift_name("nonparticipant")));
@property (class, readonly) NSArray<KmpCalendarAttendeeRole *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarAttendeeRole *> *)values __attribute__((swift_name("values()")));
@end

__attribute__((swift_name("Event")))
@protocol KmpCalendarEvent
@required
@property (readonly) NSArray<KmpCalendarAttendee *> *attendees __attribute__((swift_name("attendees")));
@property (readonly) BOOL canEdit __attribute__((swift_name("canEdit")));
@property (readonly) NSString * _Nullable categories __attribute__((swift_name("categories")));
@property (readonly) int32_t color __attribute__((swift_name("color")));
@property (readonly) NSString * _Nullable description_ __attribute__((swift_name("description_")));
@property (readonly) KmpCalendarKotlinInstant * _Nullable lastModified __attribute__((swift_name("lastModified")));
@property (readonly) NSString * _Nullable location __attribute__((swift_name("location")));
@property (readonly) KmpCalendarAttendee * _Nullable organizer __attribute__((swift_name("organizer")));
@property (readonly) NSString * _Nullable status __attribute__((swift_name("status")));
@property (readonly) id<KmpCalendarEventTiming> timing __attribute__((swift_name("timing")));
@property (readonly) NSString *title __attribute__((swift_name("title")));
@end


/**
 * When an event happens.
 *
 * RFC 5545 distinguishes two granularities by the value type of `DTSTART`, which is a structural
 * difference: a `DATE` start is a whole-day event ([AllDay], date-based, no time
 * nor timezone), while a `DATE-TIME` start is anchored to an instant ([Timed]).
 */
__attribute__((swift_name("EventTiming")))
@protocol KmpCalendarEventTiming
@required
@property (readonly) KmpCalendarRecurrenceRule * _Nullable recurrenceRule __attribute__((swift_name("recurrenceRule")));
@end


/**
 * Whole-day event (`DTSTART;VALUE=DATE`). Spans the half-open day range `[startDate, endDate)`.
 *
 * Per RFC 5545 §3.6.1, when no `DTEND`/`DURATION` is given the duration is one day, so [endDate]
 * defaults to the day after [startDate]. [endDate] is exclusive (as iCal `DTEND` is).
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("EventTimingAllDay")))
@interface KmpCalendarEventTimingAllDay : KmpCalendarBase <KmpCalendarEventTiming>
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *endDate __attribute__((swift_name("endDate")));
@property (readonly) KmpCalendarRecurrenceRule * _Nullable recurrenceRule __attribute__((swift_name("recurrenceRule")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *startDate __attribute__((swift_name("startDate")));
- (instancetype)initWithStartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate endDate:(KmpCalendarKotlinx_datetimeLocalDate *)endDate recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("init(startDate:endDate:recurrenceRule:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithStartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate endDate__Skie_DefaultArguments__159:(KmpCalendarKotlinx_datetimeLocalDate *)endDate__Skie_DefaultArguments__159 __attribute__((swift_name("init(startDate:endDate__Skie_DefaultArguments__159:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarEventTimingAllDay *)doCopyStartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate endDate:(KmpCalendarKotlinx_datetimeLocalDate *)endDate recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy(startDate:endDate:recurrenceRule:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__133 __attribute__((swift_name("doCopy__Skie_DefaultArguments__133()")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__134StartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate __attribute__((swift_name("doCopy__Skie_DefaultArguments__134(startDate:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__135EndDate:(KmpCalendarKotlinx_datetimeLocalDate *)endDate __attribute__((swift_name("doCopy__Skie_DefaultArguments__135(endDate:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__136StartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate endDate:(KmpCalendarKotlinx_datetimeLocalDate *)endDate __attribute__((swift_name("doCopy__Skie_DefaultArguments__136(startDate:endDate:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__137RecurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__137(recurrenceRule:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__138StartDate:(KmpCalendarKotlinx_datetimeLocalDate *)startDate recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__138(startDate:recurrenceRule:)")));
- (KmpCalendarEventTimingAllDay *)doCopy__Skie_DefaultArguments__139EndDate:(KmpCalendarKotlinx_datetimeLocalDate *)endDate recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__139(endDate:recurrenceRule:)")));

/**
 * Whole-day event (`DTSTART;VALUE=DATE`). Spans the half-open day range `[startDate, endDate)`.
 *
 * Per RFC 5545 §3.6.1, when no `DTEND`/`DURATION` is given the duration is one day, so [endDate]
 * defaults to the day after [startDate]. [endDate] is exclusive (as iCal `DTEND` is).
 */
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));

/**
 * Whole-day event (`DTSTART;VALUE=DATE`). Spans the half-open day range `[startDate, endDate)`.
 *
 * Per RFC 5545 §3.6.1, when no `DTEND`/`DURATION` is given the duration is one day, so [endDate]
 * defaults to the day after [startDate]. [endDate] is exclusive (as iCal `DTEND` is).
 */
- (NSUInteger)hash __attribute__((swift_name("hash()")));

/**
 * Whole-day event (`DTSTART;VALUE=DATE`). Spans the half-open day range `[startDate, endDate)`.
 *
 * Per RFC 5545 §3.6.1, when no `DTEND`/`DURATION` is given the duration is one day, so [endDate]
 * defaults to the day after [startDate]. [endDate] is exclusive (as iCal `DTEND` is).
 */
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * Instant-anchored event (`DTSTART;VALUE=DATE-TIME`). Spans `[start, end]`.
 *
 * [end] is the **resolved** end: it already accounts for `DTEND` / `DURATION` (and equals [start] for a
 * zero-length event with neither). The resolution rule lives in a single place — the write-side mapper that
 * persists `EventEntity.dtEndEffective` — so consumers never have to recompute it.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("EventTimingTimed")))
@interface KmpCalendarEventTimingTimed : KmpCalendarBase <KmpCalendarEventTiming>
@property (readonly) KmpCalendarKotlinInstant *end __attribute__((swift_name("end")));
@property (readonly) KmpCalendarRecurrenceRule * _Nullable recurrenceRule __attribute__((swift_name("recurrenceRule")));
@property (readonly) KmpCalendarKotlinInstant *start __attribute__((swift_name("start")));
- (instancetype)initWithStart:(KmpCalendarKotlinInstant *)start end:(KmpCalendarKotlinInstant *)end recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("init(start:end:recurrenceRule:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithStart:(KmpCalendarKotlinInstant *)start end__Skie_DefaultArguments__160:(KmpCalendarKotlinInstant *)end__Skie_DefaultArguments__160 __attribute__((swift_name("init(start:end__Skie_DefaultArguments__160:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarEventTimingTimed *)doCopyStart:(KmpCalendarKotlinInstant *)start end:(KmpCalendarKotlinInstant *)end recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy(start:end:recurrenceRule:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__140 __attribute__((swift_name("doCopy__Skie_DefaultArguments__140()")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__141Start:(KmpCalendarKotlinInstant *)start __attribute__((swift_name("doCopy__Skie_DefaultArguments__141(start:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__142End:(KmpCalendarKotlinInstant *)end __attribute__((swift_name("doCopy__Skie_DefaultArguments__142(end:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__143Start:(KmpCalendarKotlinInstant *)start end:(KmpCalendarKotlinInstant *)end __attribute__((swift_name("doCopy__Skie_DefaultArguments__143(start:end:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__144RecurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__144(recurrenceRule:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__145Start:(KmpCalendarKotlinInstant *)start recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__145(start:recurrenceRule:)")));
- (KmpCalendarEventTimingTimed *)doCopy__Skie_DefaultArguments__146End:(KmpCalendarKotlinInstant *)end recurrenceRule:(KmpCalendarRecurrenceRule * _Nullable)recurrenceRule __attribute__((swift_name("doCopy__Skie_DefaultArguments__146(end:recurrenceRule:)")));

/**
 * Instant-anchored event (`DTSTART;VALUE=DATE-TIME`). Spans `[start, end]`.
 *
 * [end] is the **resolved** end: it already accounts for `DTEND` / `DURATION` (and equals [start] for a
 * zero-length event with neither). The resolution rule lives in a single place — the write-side mapper that
 * persists `EventEntity.dtEndEffective` — so consumers never have to recompute it.
 */
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));

/**
 * Instant-anchored event (`DTSTART;VALUE=DATE-TIME`). Spans `[start, end]`.
 *
 * [end] is the **resolved** end: it already accounts for `DTEND` / `DURATION` (and equals [start] for a
 * zero-length event with neither). The resolution rule lives in a single place — the write-side mapper that
 * persists `EventEntity.dtEndEffective` — so consumers never have to recompute it.
 */
- (NSUInteger)hash __attribute__((swift_name("hash()")));

/**
 * Instant-anchored event (`DTSTART;VALUE=DATE-TIME`). Spans `[start, end]`.
 *
 * [end] is the **resolved** end: it already accounts for `DTEND` / `DURATION` (and equals [start] for a
 * zero-length event with neither). The resolution rule lives in a single place — the write-side mapper that
 * persists `EventEntity.dtEndEffective` — so consumers never have to recompute it.
 */
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("ParticipationStatus")))
@interface KmpCalendarParticipationStatus : KmpCalendarKotlinEnum<KmpCalendarParticipationStatus *>
@property (class, readonly) KmpCalendarParticipationStatus *accepted __attribute__((swift_name("accepted")));
@property (class, readonly) KmpCalendarParticipationStatus *declined __attribute__((swift_name("declined")));
@property (class, readonly) KmpCalendarParticipationStatus *tentative __attribute__((swift_name("tentative")));
@property (class, readonly) KmpCalendarParticipationStatus *needsaction __attribute__((swift_name("needsaction")));
@property (class, readonly) NSArray<KmpCalendarParticipationStatus *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarParticipationStatus *> *)values __attribute__((swift_name("values()")));
@end


/** RFC 5545 frequencies. */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Frequency")))
@interface KmpCalendarFrequency : KmpCalendarKotlinEnum<KmpCalendarFrequency *>
@property (class, readonly) KmpCalendarFrequency *secondly __attribute__((swift_name("secondly")));
@property (class, readonly) KmpCalendarFrequency *minutely __attribute__((swift_name("minutely")));
@property (class, readonly) KmpCalendarFrequency *hourly __attribute__((swift_name("hourly")));
@property (class, readonly) KmpCalendarFrequency *daily __attribute__((swift_name("daily")));
@property (class, readonly) KmpCalendarFrequency *weekly __attribute__((swift_name("weekly")));
@property (class, readonly) KmpCalendarFrequency *monthly __attribute__((swift_name("monthly")));
@property (class, readonly) KmpCalendarFrequency *yearly __attribute__((swift_name("yearly")));
@property (class, readonly) NSArray<KmpCalendarFrequency *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));

/** RFC 5545 frequencies. */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarFrequency *> *)values __attribute__((swift_name("values()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("RecurrenceRule")))
@interface KmpCalendarRecurrenceRule : KmpCalendarBase
@property (readonly) NSArray<KmpCalendarWeekDayNum *> *byDay __attribute__((swift_name("byDay")));
@property (readonly) NSArray<KmpCalendarInt *> *byMonth __attribute__((swift_name("byMonth")));
@property (readonly) NSArray<KmpCalendarInt *> *byMonthDay __attribute__((swift_name("byMonthDay")));
@property (readonly) NSArray<KmpCalendarInt *> *bySetPos __attribute__((swift_name("bySetPos")));
@property (readonly) KmpCalendarInt * _Nullable count __attribute__((swift_name("count")));
@property (readonly) KmpCalendarFrequency *freq __attribute__((swift_name("freq")));
@property (readonly) int32_t interval __attribute__((swift_name("interval")));
@property (readonly) KmpCalendarKotlinInstant * _Nullable until __attribute__((swift_name("until")));
@property (readonly) KmpCalendarKotlinx_datetimeDayOfWeek * _Nullable weekStart __attribute__((swift_name("weekStart")));
- (instancetype)initWithFreq:(KmpCalendarFrequency *)freq interval:(int32_t)interval count:(KmpCalendarInt * _Nullable)count until:(KmpCalendarKotlinInstant * _Nullable)until byDay:(NSArray<KmpCalendarWeekDayNum *> *)byDay byMonthDay:(NSArray<KmpCalendarInt *> *)byMonthDay byMonth:(NSArray<KmpCalendarInt *> *)byMonth bySetPos:(NSArray<KmpCalendarInt *> *)bySetPos weekStart:(KmpCalendarKotlinx_datetimeDayOfWeek * _Nullable)weekStart __attribute__((swift_name("init(freq:interval:count:until:byDay:byMonthDay:byMonth:bySetPos:weekStart:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarRecurrenceRule *)doCopyFreq:(KmpCalendarFrequency *)freq interval:(int32_t)interval count:(KmpCalendarInt * _Nullable)count until:(KmpCalendarKotlinInstant * _Nullable)until byDay:(NSArray<KmpCalendarWeekDayNum *> *)byDay byMonthDay:(NSArray<KmpCalendarInt *> *)byMonthDay byMonth:(NSArray<KmpCalendarInt *> *)byMonth bySetPos:(NSArray<KmpCalendarInt *> *)bySetPos weekStart:(KmpCalendarKotlinx_datetimeDayOfWeek * _Nullable)weekStart __attribute__((swift_name("doCopy(freq:interval:count:until:byDay:byMonthDay:byMonth:bySetPos:weekStart:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * A day-of-week optionally qualified by an ordinal position
 * (e.g. `2MO` = second Monday, `-1FR` = last Friday).
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("WeekDayNum")))
@interface KmpCalendarWeekDayNum : KmpCalendarBase
@property (class, readonly, getter=companion) KmpCalendarWeekDayNumCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) KmpCalendarKotlinx_datetimeDayOfWeek *dayOfWeek __attribute__((swift_name("dayOfWeek")));
@property (readonly) KmpCalendarInt * _Nullable ordinal __attribute__((swift_name("ordinal")));
- (instancetype)initWithOrdinal:(KmpCalendarInt * _Nullable)ordinal dayOfWeek:(KmpCalendarKotlinx_datetimeDayOfWeek *)dayOfWeek __attribute__((swift_name("init(ordinal:dayOfWeek:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithDayOfWeek__Skie_DefaultArguments__148:(KmpCalendarKotlinx_datetimeDayOfWeek *)dayOfWeek__Skie_DefaultArguments__148 __attribute__((swift_name("init(dayOfWeek__Skie_DefaultArguments__148:)"))) __attribute__((objc_designated_initializer));
- (KmpCalendarWeekDayNum *)doCopyOrdinal:(KmpCalendarInt * _Nullable)ordinal dayOfWeek:(KmpCalendarKotlinx_datetimeDayOfWeek *)dayOfWeek __attribute__((swift_name("doCopy(ordinal:dayOfWeek:)")));
- (KmpCalendarWeekDayNum *)doCopy__Skie_DefaultArguments__63 __attribute__((swift_name("doCopy__Skie_DefaultArguments__63()")));
- (KmpCalendarWeekDayNum *)doCopy__Skie_DefaultArguments__64Ordinal:(KmpCalendarInt * _Nullable)ordinal __attribute__((swift_name("doCopy__Skie_DefaultArguments__64(ordinal:)")));
- (KmpCalendarWeekDayNum *)doCopy__Skie_DefaultArguments__65DayOfWeek:(KmpCalendarKotlinx_datetimeDayOfWeek *)dayOfWeek __attribute__((swift_name("doCopy__Skie_DefaultArguments__65(dayOfWeek:)")));

/**
 * A day-of-week optionally qualified by an ordinal position
 * (e.g. `2MO` = second Monday, `-1FR` = last Friday).
 */
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));

/**
 * A day-of-week optionally qualified by an ordinal position
 * (e.g. `2MO` = second Monday, `-1FR` = last Friday).
 */
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)toRfc5545 __attribute__((swift_name("toRfc5545()")));

/**
 * A day-of-week optionally qualified by an ordinal position
 * (e.g. `2MO` = second Monday, `-1FR` = last Friday).
 */
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("WeekDayNum.Companion")))
@interface KmpCalendarWeekDayNumCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarWeekDayNumCompanion *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
- (KmpCalendarWeekDayNum * _Nullable)parseToken:(NSString *)token __attribute__((swift_name("parse(token:)")));
@end

__attribute__((swift_name("KotlinThrowable")))
@interface KmpCalendarKotlinThrowable : KmpCalendarBase
@property (readonly) KmpCalendarKotlinThrowable * _Nullable cause __attribute__((swift_name("cause")));
@property (readonly) NSString * _Nullable message __attribute__((swift_name("message")));
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));

/**
 * @note annotations
 *   kotlin.experimental.ExperimentalNativeApi
*/
- (KmpCalendarKotlinArray<NSString *> *)getStackTrace __attribute__((swift_name("getStackTrace()")));
- (void)printStackTrace __attribute__((swift_name("printStackTrace()")));
- (NSString *)description __attribute__((swift_name("description()")));
- (NSError *)asError __attribute__((swift_name("asError()")));
@end

__attribute__((swift_name("KotlinException")))
@interface KmpCalendarKotlinException : KmpCalendarKotlinThrowable
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("CalendarSdkException")))
@interface KmpCalendarCalendarSdkException : KmpCalendarKotlinException
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage__Skie_DefaultArguments__157:(NSString * _Nullable)message__Skie_DefaultArguments__157 __attribute__((swift_name("init(message__Skie_DefaultArguments__157:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause__Skie_DefaultArguments__158:(KmpCalendarKotlinThrowable * _Nullable)cause__Skie_DefaultArguments__158 __attribute__((swift_name("init(cause__Skie_DefaultArguments__158:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("AccountManager")))
@interface KmpCalendarAccountManager : KmpCalendarBase

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)doInitAccountAccountId:(int64_t)accountId credentials:(KmpCalendarDavCredentials *)credentials completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("doInitAccount(accountId:credentials:completionHandler:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)removeAccountAccountId:(int64_t)accountId completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("removeAccount(accountId:completionHandler:)")));

/**
 * @note This method converts instances of CalendarSdkException, CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)retrieveDavCredentialAuthToken:(NSString *)authToken login:(NSString * _Nullable)login completionHandler:(void (^)(KmpCalendarDavCredentials * _Nullable, NSError * _Nullable))completionHandler __attribute__((swift_name("retrieveDavCredential(authToken:login:completionHandler:)")));

/**
 * @note This method converts instances of CalendarSdkException, CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)retrieveDavCredential__Skie_DefaultArguments__129AuthToken:(NSString *)authToken completionHandler:(void (^)(KmpCalendarDavCredentials * _Nullable, NSError * _Nullable))completionHandler __attribute__((swift_name("retrieveDavCredential__Skie_DefaultArguments__129(authToken:completionHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("CalendarManager")))
@interface KmpCalendarCalendarManager : KmpCalendarBase

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)deleteEventEventId:(id)eventId completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("deleteEvent(eventId:completionHandler:)")));
- (id<KmpCalendarKotlinx_coroutines_coreFlow>)observeCalendars __attribute__((swift_name("observeCalendars()")));
- (id<KmpCalendarKotlinx_coroutines_coreFlow>)observeEventEventId:(id)eventId __attribute__((swift_name("observeEvent(eventId:)")));

/** Observe events from all *visible* calendars of the current account overlapping [start, end[. */
- (id<KmpCalendarKotlinx_coroutines_coreFlow>)observeEventsStart:(KmpCalendarKotlinInstant *)start end:(KmpCalendarKotlinInstant *)end __attribute__((swift_name("observeEvents(start:end:)")));

/**
 * @note This method converts instances of CancellationException to errors.
 * Other uncaught Kotlin exceptions are fatal.
*/
- (void)syncCalendarsAccountId:(int64_t)accountId completionHandler:(void (^)(NSError * _Nullable))completionHandler __attribute__((swift_name("syncCalendars(accountId:completionHandler:)")));
@end


/**
 * Entry point for Apple consumers.
 *
 * Usage from Swift:
 * ```swift
 * let sdk = CalendarSDKProvider.shared.sdk(databasePath: "/path/to/calendar.db")
 * sdk.accountManager.initAccount(...)
 * sdk.calendarManager.observeCalendars(...)
 * ```
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("CalendarSDKProvider")))
@interface KmpCalendarCalendarSDKProvider : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarCalendarSDKProvider *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Entry point for Apple consumers.
 *
 * Usage from Swift:
 * ```swift
 * let sdk = CalendarSDKProvider.shared.sdk(databasePath: "/path/to/calendar.db")
 * sdk.accountManager.initAccount(...)
 * sdk.calendarManager.observeCalendars(...)
 * ```
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)calendarSDKProvider __attribute__((swift_name("init()")));
- (id<KmpCalendarCalendarCoreGraph>)sdkDatabasePath:(NSString *)databasePath __attribute__((swift_name("sdk(databasePath:)")));
@end

@interface KmpCalendarCalendar (Extensions)
@property (readonly) int64_t accountIdValue __attribute__((swift_name("accountIdValue")));
@property (readonly) NSString *idValue __attribute__((swift_name("idValue")));

/**
 * Apple-only accessors that expose the underlying values of the `value class` ids as plain types.
 *
 * Kotlin value classes are boxed to `Any` when exported as *properties* to Swift/ObjC, which makes
 * `calendar.id` / `event.id` unusable from Swift. These extensions surface the wrapped value while
 * keeping the value classes intact in common code and on Android.
 */
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("AppleIdAccessorsKt")))
@interface KmpCalendarAppleIdAccessorsKt : KmpCalendarBase
+ (NSString *)calendarIdValue:(id<KmpCalendarEvent>)receiver __attribute__((swift_name("calendarIdValue(_:)")));
+ (NSString *)idValue:(id<KmpCalendarEvent>)receiver __attribute__((swift_name("idValue(_:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("__SkieSuspendWrappersKt")))
@interface KmpCalendar__SkieSuspendWrappersKt : KmpCalendarBase
+ (void)Skie_Suspend__0__hasNextDispatchReceiver:(KmpCalendarSkieColdFlowIterator<id> *)dispatchReceiver suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__0__hasNext(dispatchReceiver:suspendHandler:)")));
+ (void)Skie_Suspend__1__collectDispatchReceiver:(id<KmpCalendarKotlinx_coroutines_coreFlow>)dispatchReceiver collector:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)collector suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__1__collect(dispatchReceiver:collector:suspendHandler:)")));
+ (void)Skie_Suspend__2__emitDispatchReceiver:(id<KmpCalendarKotlinx_coroutines_coreFlowCollector>)dispatchReceiver value:(id _Nullable)value suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__2__emit(dispatchReceiver:value:suspendHandler:)")));
+ (void)Skie_Suspend__3__deleteEventDispatchReceiver:(KmpCalendarCalendarManager *)dispatchReceiver eventId:(id)eventId suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__3__deleteEvent(dispatchReceiver:eventId:suspendHandler:)")));
+ (void)Skie_Suspend__4__syncCalendarsDispatchReceiver:(KmpCalendarCalendarManager *)dispatchReceiver accountId:(int64_t)accountId suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__4__syncCalendars(dispatchReceiver:accountId:suspendHandler:)")));
+ (void)Skie_Suspend__5__initAccountDispatchReceiver:(KmpCalendarAccountManager *)dispatchReceiver accountId:(int64_t)accountId credentials:(KmpCalendarDavCredentials *)credentials suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__5__initAccount(dispatchReceiver:accountId:credentials:suspendHandler:)")));
+ (void)Skie_Suspend__6__removeAccountDispatchReceiver:(KmpCalendarAccountManager *)dispatchReceiver accountId:(int64_t)accountId suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__6__removeAccount(dispatchReceiver:accountId:suspendHandler:)")));
+ (void)Skie_Suspend__7__retrieveDavCredentialDispatchReceiver:(KmpCalendarAccountManager *)dispatchReceiver authToken:(NSString *)authToken login:(NSString * _Nullable)login suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__7__retrieveDavCredential(dispatchReceiver:authToken:login:suspendHandler:)")));
+ (void)Skie_Suspend__8__retrieveDavCredential__Skie_DefaultArguments__129DispatchReceiver:(KmpCalendarAccountManager *)dispatchReceiver authToken:(NSString *)authToken suspendHandler:(KmpCalendarSkie_SuspendHandler *)suspendHandler __attribute__((swift_name("Skie_Suspend__8__retrieveDavCredential__Skie_DefaultArguments__129(dispatchReceiver:authToken:suspendHandler:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("__SkieTypeExportsKt")))
@interface KmpCalendar__SkieTypeExportsKt : KmpCalendarBase
+ (void)skieTypeExports_0P0:(id<KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTime>)p0 p1:(id<KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTimeComponents>)p1 p2:(id<KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithTime>)p2 p3:(id<KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithUtcOffset>)p3 p4:(KmpCalendarKotlinx_serialization_corePolymorphicKind *)p4 p5:(KmpCalendarKotlinx_serialization_corePolymorphicKindOPEN *)p5 p6:(KmpCalendarKotlinx_serialization_corePolymorphicKindSEALED *)p6 p7:(KmpCalendarKotlinx_serialization_corePrimitiveKind *)p7 p8:(KmpCalendarKotlinx_serialization_corePrimitiveKindBOOLEAN *)p8 p9:(KmpCalendarKotlinx_serialization_corePrimitiveKindBYTE *)p9 p10:(KmpCalendarKotlinx_serialization_corePrimitiveKindCHAR *)p10 p11:(KmpCalendarKotlinx_serialization_corePrimitiveKindDOUBLE *)p11 p12:(KmpCalendarKotlinx_serialization_corePrimitiveKindFLOAT *)p12 p13:(KmpCalendarKotlinx_serialization_corePrimitiveKindINT *)p13 p14:(KmpCalendarKotlinx_serialization_corePrimitiveKindLONG *)p14 p15:(KmpCalendarKotlinx_serialization_corePrimitiveKindSHORT *)p15 p16:(KmpCalendarKotlinx_serialization_corePrimitiveKindSTRING *)p16 p17:(KmpCalendarKotlinx_serialization_coreSerialKindCONTEXTUAL *)p17 p18:(KmpCalendarKotlinx_serialization_coreSerialKindENUM *)p18 p19:(KmpCalendarKotlinx_serialization_coreStructureKind *)p19 p20:(KmpCalendarKotlinx_serialization_coreStructureKindCLASS *)p20 p21:(KmpCalendarKotlinx_serialization_coreStructureKindLIST *)p21 p22:(KmpCalendarKotlinx_serialization_coreStructureKindMAP *)p22 p23:(KmpCalendarKotlinx_serialization_coreStructureKindOBJECT *)p23 __attribute__((swift_name("skieTypeExports_0(p0:p1:p2:p3:p4:p5:p6:p7:p8:p9:p10:p11:p12:p13:p14:p15:p16:p17:p18:p19:p20:p21:p22:p23:)")));
@end

__attribute__((swift_name("KotlinRuntimeException")))
@interface KmpCalendarKotlinRuntimeException : KmpCalendarKotlinException
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));
@end

__attribute__((swift_name("KotlinIllegalStateException")))
@interface KmpCalendarKotlinIllegalStateException : KmpCalendarKotlinRuntimeException
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));
@end


/**
 * @note annotations
 *   kotlin.SinceKotlin(version="1.4")
*/
__attribute__((swift_name("KotlinCancellationException")))
@interface KmpCalendarKotlinCancellationException : KmpCalendarKotlinIllegalStateException
- (instancetype)init __attribute__((swift_name("init()"))) __attribute__((objc_designated_initializer));
+ (instancetype)new __attribute__((availability(swift, unavailable, message="use object initializers instead")));
- (instancetype)initWithMessage:(NSString * _Nullable)message __attribute__((swift_name("init(message:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithCause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(cause:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMessage:(NSString * _Nullable)message cause:(KmpCalendarKotlinThrowable * _Nullable)cause __attribute__((swift_name("init(message:cause:)"))) __attribute__((objc_designated_initializer));
@end


/**
 * A runnable task for [CoroutineDispatcher.dispatch].
 *
 * Equivalent to the type `() -> Unit`.
 */
__attribute__((swift_name("Kotlinx_coroutines_coreRunnable")))
@protocol KmpCalendarKotlinx_coroutines_coreRunnable
@required

/**
 * @suppress
 */
- (void)run __attribute__((swift_name("run()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("KotlinEnumCompanion")))
@interface KmpCalendarKotlinEnumCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinEnumCompanion *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("KotlinArray")))
@interface KmpCalendarKotlinArray<T> : KmpCalendarBase
@property (readonly) int32_t size __attribute__((swift_name("size")));
+ (instancetype)arrayWithSize:(int32_t)size init:(T _Nullable (^)(KmpCalendarInt *))init __attribute__((swift_name("init(size:init:)")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (T _Nullable)getIndex:(int32_t)index __attribute__((swift_name("get(index:)")));
- (id<KmpCalendarKotlinIterator>)iterator __attribute__((swift_name("iterator()")));
- (void)setIndex:(int32_t)index value:(T _Nullable)value __attribute__((swift_name("set(index:value:)")));
@end


/**
 * @note annotations
 *   kotlin.SinceKotlin(version="2.3")
*/
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("KotlinInstant")))
@interface KmpCalendarKotlinInstant : KmpCalendarBase <KmpCalendarKotlinComparable>
@property (class, readonly, getter=companion) KmpCalendarKotlinInstantCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) int64_t epochSeconds __attribute__((swift_name("epochSeconds")));
@property (readonly) int32_t nanosecondsOfSecond __attribute__((swift_name("nanosecondsOfSecond")));
- (int32_t)compareToOther:(KmpCalendarKotlinInstant *)other __attribute__((swift_name("compareTo(other:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (KmpCalendarKotlinInstant *)minusDuration:(int64_t)duration __attribute__((swift_name("minus(duration:)")));
- (int64_t)minusOther:(KmpCalendarKotlinInstant *)other __attribute__((swift_name("minus(other:)")));
- (KmpCalendarKotlinInstant *)plusDuration:(int64_t)duration __attribute__((swift_name("plus(duration:)")));
- (int64_t)toEpochMilliseconds __attribute__((swift_name("toEpochMilliseconds()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * @note annotations
 *   kotlinx.serialization.Serializable(with=NormalClass(value=kotlinx/datetime/serializers/LocalDateSerializer))
*/
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeLocalDate")))
@interface KmpCalendarKotlinx_datetimeLocalDate : KmpCalendarBase <KmpCalendarKotlinComparable>
@property (class, readonly, getter=companion) KmpCalendarKotlinx_datetimeLocalDateCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) int32_t day __attribute__((swift_name("day")));
@property (readonly) int32_t dayOfMonth __attribute__((swift_name("dayOfMonth"))) __attribute__((deprecated("Use the 'day' property instead")));
@property (readonly) KmpCalendarKotlinx_datetimeDayOfWeek *dayOfWeek __attribute__((swift_name("dayOfWeek")));
@property (readonly) int32_t dayOfYear __attribute__((swift_name("dayOfYear")));
@property (readonly) KmpCalendarKotlinx_datetimeMonth *month __attribute__((swift_name("month")));
@property (readonly) int32_t monthNumber __attribute__((swift_name("monthNumber"))) __attribute__((deprecated("Use the 'month' property instead")));
@property (readonly) int32_t year __attribute__((swift_name("year")));
- (instancetype)initWithYear:(int32_t)year month:(int32_t)month day:(int32_t)day __attribute__((swift_name("init(year:month:day:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithYear:(int32_t)year month:(KmpCalendarKotlinx_datetimeMonth *)month day_:(int32_t)day __attribute__((swift_name("init(year:month:day_:)"))) __attribute__((objc_designated_initializer));
- (int32_t)compareToOther:(KmpCalendarKotlinx_datetimeLocalDate *)other __attribute__((swift_name("compareTo(other:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (KmpCalendarKotlinx_datetimeLocalDateRange *)rangeToThat:(KmpCalendarKotlinx_datetimeLocalDate *)that __attribute__((swift_name("rangeTo(that:)")));
- (KmpCalendarKotlinx_datetimeLocalDateRange *)rangeUntilThat:(KmpCalendarKotlinx_datetimeLocalDate *)that __attribute__((swift_name("rangeUntil(that:)")));
- (int64_t)toEpochDays __attribute__((swift_name("toEpochDays()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeDayOfWeek")))
@interface KmpCalendarKotlinx_datetimeDayOfWeek : KmpCalendarKotlinEnum<KmpCalendarKotlinx_datetimeDayOfWeek *>
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *monday __attribute__((swift_name("monday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *tuesday __attribute__((swift_name("tuesday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *wednesday __attribute__((swift_name("wednesday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *thursday __attribute__((swift_name("thursday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *friday __attribute__((swift_name("friday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *saturday __attribute__((swift_name("saturday")));
@property (class, readonly) KmpCalendarKotlinx_datetimeDayOfWeek *sunday __attribute__((swift_name("sunday")));
@property (class, readonly) NSArray<KmpCalendarKotlinx_datetimeDayOfWeek *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarKotlinx_datetimeDayOfWeek *> *)values __attribute__((swift_name("values()")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilder")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilder
@required
- (void)charsValue:(NSString *)value __attribute__((swift_name("chars(value:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithYearMonth")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithYearMonth <KmpCalendarKotlinx_datetimeDateTimeFormatBuilder>
@required
- (void)monthNameNames:(KmpCalendarKotlinx_datetimeMonthNames *)names __attribute__((swift_name("monthName(names:)")));
- (void)monthNumberPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("monthNumber(padding:)")));
- (void)yearPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("year(padding:)")));
- (void)yearMonthFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("yearMonth(format:)")));
- (void)yearTwoDigitsBaseYear:(int32_t)baseYear __attribute__((swift_name("yearTwoDigits(baseYear:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithDate")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDate <KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithYearMonth>
@required
- (void)dateFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("date(format:)")));
- (void)dayPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("day(padding:)")));
- (void)dayOfMonthPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("dayOfMonth(padding:)"))) __attribute__((deprecated("Use 'day' instead")));
- (void)dayOfWeekNames:(KmpCalendarKotlinx_datetimeDayOfWeekNames *)names __attribute__((swift_name("dayOfWeek(names:)")));
- (void)dayOfYearPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("dayOfYear(padding:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithTime")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithTime <KmpCalendarKotlinx_datetimeDateTimeFormatBuilder>
@required
- (void)amPmHourPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("amPmHour(padding:)")));
- (void)amPmMarkerAm:(NSString *)am pm:(NSString *)pm __attribute__((swift_name("amPmMarker(am:pm:)")));
- (void)hourPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("hour(padding:)")));
- (void)minutePadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("minute(padding:)")));
- (void)secondPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("second(padding:)")));
- (void)secondFractionFixedLength:(int32_t)fixedLength __attribute__((swift_name("secondFraction(fixedLength:)")));
- (void)secondFractionMinLength:(int32_t)minLength maxLength:(int32_t)maxLength __attribute__((swift_name("secondFraction(minLength:maxLength:)")));
- (void)timeFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("time(format:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithDateTime")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTime <KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDate, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithTime>
@required
- (void)dateTimeFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("dateTime(format:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithUtcOffset")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithUtcOffset <KmpCalendarKotlinx_datetimeDateTimeFormatBuilder>
@required
- (void)offsetFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("offset(format:)")));
- (void)offsetHoursPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("offsetHours(padding:)")));
- (void)offsetMinutesOfHourPadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("offsetMinutesOfHour(padding:)")));
- (void)offsetSecondsOfMinutePadding:(KmpCalendarKotlinx_datetimePadding *)padding __attribute__((swift_name("offsetSecondsOfMinute(padding:)")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormatBuilderWithDateTimeComponents")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTimeComponents <KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDateTime, KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithUtcOffset>
@required
- (void)dateTimeComponentsFormat:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("dateTimeComponents(format:)")));
- (void)timeZoneId __attribute__((swift_name("timeZoneId()")));
@end


/**
 * Serial kind is an intrinsic property of [SerialDescriptor] that indicates how
 * the corresponding type is structurally represented by its serializer.
 *
 * Kind is used by serialization formats to determine how exactly the given type
 * should be serialized. For example, JSON format detects the kind of the value and,
 * depending on that, may write it as a plain value for primitive kinds, open a
 * curly brace '{' for class-like structures and square bracket '[' for list- and array- like structures.
 *
 * Kinds are used both during serialization, to serialize a value properly and statically, and
 * to introspect the type structure or build serialization schema.
 *
 * Kind should match the structure of the serialized form, not the structure of the corresponding Kotlin class.
 * Meaning that if serializable class `class IntPair(val left: Int, val right: Int)` is represented by the serializer
 * as a single `Long` value, its descriptor should have [PrimitiveKind.LONG] without nested elements even though the class itself
 * represents a structure with two primitive fields.
 */
__attribute__((swift_name("Kotlinx_serialization_coreSerialKind")))
@interface KmpCalendarKotlinx_serialization_coreSerialKind : KmpCalendarBase
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * Polymorphic kind represents a (bounded) polymorphic value, that is referred
 * by some base class or interface, but its structure is defined by one of the possible implementations.
 * Polymorphic kind is, by its definition, a union kind and is extracted to its own subtype to emphasize
 * bounded and sealed polymorphism common property: not knowing the actual type statically and requiring
 * formats to additionally encode it.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
__attribute__((swift_name("Kotlinx_serialization_corePolymorphicKind")))
@interface KmpCalendarKotlinx_serialization_corePolymorphicKind : KmpCalendarKotlinx_serialization_coreSerialKind
@end


/**
 * Open polymorphic kind represents statically unknown type that is hidden behind a given base class or interface.
 * [PolymorphicSerializer] can be used as an example of polymorphic serialization.
 *
 * Due to security concerns and typical mistakes that arises from polymorphic serialization, by default
 * `kotlinx.serialization` provides only bounded polymorphic serialization, forcing users to register all possible
 * serializers for a given base class or interface.
 *
 * To introspect descriptor of this kind (e.g. list possible subclasses), an instance of [SerializersModule] is required.
 * See [capturedKClass] extension property for more details.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePolymorphicKind.OPEN")))
@interface KmpCalendarKotlinx_serialization_corePolymorphicKindOPEN : KmpCalendarKotlinx_serialization_corePolymorphicKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePolymorphicKindOPEN *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Open polymorphic kind represents statically unknown type that is hidden behind a given base class or interface.
 * [PolymorphicSerializer] can be used as an example of polymorphic serialization.
 *
 * Due to security concerns and typical mistakes that arises from polymorphic serialization, by default
 * `kotlinx.serialization` provides only bounded polymorphic serialization, forcing users to register all possible
 * serializers for a given base class or interface.
 *
 * To introspect descriptor of this kind (e.g. list possible subclasses), an instance of [SerializersModule] is required.
 * See [capturedKClass] extension property for more details.
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)oPEN __attribute__((swift_name("init()")));
@end


/**
 * Sealed kind represents Kotlin sealed classes, where all subclasses are known statically at the moment of declaration.
 * [SealedClassSerializer] can be used as an example of sealed serialization.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePolymorphicKind.SEALED")))
@interface KmpCalendarKotlinx_serialization_corePolymorphicKindSEALED : KmpCalendarKotlinx_serialization_corePolymorphicKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePolymorphicKindSEALED *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Sealed kind represents Kotlin sealed classes, where all subclasses are known statically at the moment of declaration.
 * [SealedClassSerializer] can be used as an example of sealed serialization.
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)sEALED __attribute__((swift_name("init()")));
@end


/**
 * Values of primitive kinds usually are represented as a single value.
 * All default serializers for Kotlin [primitives types](https://kotlinlang.org/docs/tutorials/kotlin-for-py/primitive-data-types-and-their-limitations.html)
 * and [String] have primitive kind.
 *
 * ### Serializers interaction
 *
 * Serialization formats typically handle these kinds by calling a corresponding primitive method on encoder or decoder.
 * For example, if the following serializable class `class Color(val red: Byte, val green: Byte, val blue: Byte)` is represented by your serializer
 * as a single [Int] value, a typical serializer will serialize its value in the following manner:
 * ```
 * val intValue = color.rgbToInt()
 * encoder.encodeInt(intValue)
 * ```
 * and a corresponding [Decoder] counterpart.
 *
 * ### Implementation note
 *
 * Serial descriptors for primitive kinds are not expected to have any nested elements, thus its element count should be zero.
 * If a class is represented as a primitive value, its corresponding serial name *should not* be equal to the corresponding primitive type name.
 * For the `Color` example, represented as single [Int], its descriptor should have [INT] kind, zero elements and serial name **not equals**
 * to `kotlin.Int`: `PrimitiveDescriptor("my.package.ColorAsInt", PrimitiveKind.INT)`
 */
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKind : KmpCalendarKotlinx_serialization_coreSerialKind
@end


/**
 * Primitive kind that represents a boolean `true`/`false` value.
 * Corresponding Kotlin primitive is [Boolean].
 * Corresponding encoder and decoder methods are [Encoder.encodeBoolean] and [Decoder.decodeBoolean].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.BOOLEAN")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindBOOLEAN : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindBOOLEAN *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a boolean `true`/`false` value.
 * Corresponding Kotlin primitive is [Boolean].
 * Corresponding encoder and decoder methods are [Encoder.encodeBoolean] and [Decoder.decodeBoolean].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)bOOLEAN __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a single byte value.
 * Corresponding Kotlin primitive is [Byte].
 * Corresponding encoder and decoder methods are [Encoder.encodeByte] and [Decoder.decodeByte].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.BYTE")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindBYTE : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindBYTE *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a single byte value.
 * Corresponding Kotlin primitive is [Byte].
 * Corresponding encoder and decoder methods are [Encoder.encodeByte] and [Decoder.decodeByte].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)bYTE __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 16-bit unicode character value.
 * Corresponding Kotlin primitive is [Char].
 * Corresponding encoder and decoder methods are [Encoder.encodeChar] and [Decoder.decodeChar].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.CHAR")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindCHAR : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindCHAR *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 16-bit unicode character value.
 * Corresponding Kotlin primitive is [Char].
 * Corresponding encoder and decoder methods are [Encoder.encodeChar] and [Decoder.decodeChar].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)cHAR __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 64-bit IEEE 754 floating point value.
 * Corresponding Kotlin primitive is [Double].
 * Corresponding encoder and decoder methods are [Encoder.encodeDouble] and [Decoder.decodeDouble].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.DOUBLE")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindDOUBLE : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindDOUBLE *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 64-bit IEEE 754 floating point value.
 * Corresponding Kotlin primitive is [Double].
 * Corresponding encoder and decoder methods are [Encoder.encodeDouble] and [Decoder.decodeDouble].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)dOUBLE __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 32-bit IEEE 754 floating point value.
 * Corresponding Kotlin primitive is [Float].
 * Corresponding encoder and decoder methods are [Encoder.encodeFloat] and [Decoder.decodeFloat].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.FLOAT")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindFLOAT : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindFLOAT *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 32-bit IEEE 754 floating point value.
 * Corresponding Kotlin primitive is [Float].
 * Corresponding encoder and decoder methods are [Encoder.encodeFloat] and [Decoder.decodeFloat].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)fLOAT __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 32-bit int value.
 * Corresponding Kotlin primitive is [Int].
 * Corresponding encoder and decoder methods are [Encoder.encodeInt] and [Decoder.decodeInt].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.INT")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindINT : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindINT *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 32-bit int value.
 * Corresponding Kotlin primitive is [Int].
 * Corresponding encoder and decoder methods are [Encoder.encodeInt] and [Decoder.decodeInt].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)iNT __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 64-bit long value.
 * Corresponding Kotlin primitive is [Long].
 * Corresponding encoder and decoder methods are [Encoder.encodeLong] and [Decoder.decodeLong].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.LONG")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindLONG : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindLONG *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 64-bit long value.
 * Corresponding Kotlin primitive is [Long].
 * Corresponding encoder and decoder methods are [Encoder.encodeLong] and [Decoder.decodeLong].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)lONG __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a 16-bit short value.
 * Corresponding Kotlin primitive is [Short].
 * Corresponding encoder and decoder methods are [Encoder.encodeShort] and [Decoder.decodeShort].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.SHORT")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindSHORT : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindSHORT *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a 16-bit short value.
 * Corresponding Kotlin primitive is [Short].
 * Corresponding encoder and decoder methods are [Encoder.encodeShort] and [Decoder.decodeShort].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)sHORT __attribute__((swift_name("init()")));
@end


/**
 * Primitive kind that represents a string value.
 * Corresponding Kotlin primitive is [String].
 * Corresponding encoder and decoder methods are [Encoder.encodeString] and [Decoder.decodeString].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_corePrimitiveKind.STRING")))
@interface KmpCalendarKotlinx_serialization_corePrimitiveKindSTRING : KmpCalendarKotlinx_serialization_corePrimitiveKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_corePrimitiveKindSTRING *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Primitive kind that represents a string value.
 * Corresponding Kotlin primitive is [String].
 * Corresponding encoder and decoder methods are [Encoder.encodeString] and [Decoder.decodeString].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)sTRING __attribute__((swift_name("init()")));
@end


/**
 * Represents an "unknown" type that will be known only at the moment of the serialization.
 * Effectively it defers the choice of the serializer to a moment of the serialization, and can
 * be used for [contextual][Contextual] serialization.
 *
 * To introspect descriptor of this kind, an instance of [SerializersModule] is required.
 * See [capturedKClass] extension property for more details.
 * However, if possible options are known statically (e.g. for sealed classes), they can be
 * enumerated in child descriptors similarly to [ENUM].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreSerialKind.CONTEXTUAL")))
@interface KmpCalendarKotlinx_serialization_coreSerialKindCONTEXTUAL : KmpCalendarKotlinx_serialization_coreSerialKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreSerialKindCONTEXTUAL *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Represents an "unknown" type that will be known only at the moment of the serialization.
 * Effectively it defers the choice of the serializer to a moment of the serialization, and can
 * be used for [contextual][Contextual] serialization.
 *
 * To introspect descriptor of this kind, an instance of [SerializersModule] is required.
 * See [capturedKClass] extension property for more details.
 * However, if possible options are known statically (e.g. for sealed classes), they can be
 * enumerated in child descriptors similarly to [ENUM].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)cONTEXTUAL __attribute__((swift_name("init()")));
@end


/**
 * Represents a Kotlin [Enum] with statically known values.
 * All enum values should be enumerated in descriptor elements.
 * Each element descriptor of a [Enum] kind represents an instance of a particular enum
 * and has an [StructureKind.OBJECT] kind.
 * Each [positional name][SerialDescriptor.getElementName] contains a corresponding enum element [name][Enum.name].
 *
 * Corresponding encoder and decoder methods are [Encoder.encodeEnum] and [Decoder.decodeEnum].
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreSerialKind.ENUM")))
@interface KmpCalendarKotlinx_serialization_coreSerialKindENUM : KmpCalendarKotlinx_serialization_coreSerialKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreSerialKindENUM *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Represents a Kotlin [Enum] with statically known values.
 * All enum values should be enumerated in descriptor elements.
 * Each element descriptor of a [Enum] kind represents an instance of a particular enum
 * and has an [StructureKind.OBJECT] kind.
 * Each [positional name][SerialDescriptor.getElementName] contains a corresponding enum element [name][Enum.name].
 *
 * Corresponding encoder and decoder methods are [Encoder.encodeEnum] and [Decoder.decodeEnum].
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)eNUM __attribute__((swift_name("init()")));
@end


/**
 * Structure kind represents values with composite structure of nested elements of depth and arbitrary number.
 * We acknowledge following structured kinds:
 *
 * ### Regular classes
 * The most common case for serialization, that represents an arbitrary structure with fixed count of elements.
 * When the regular Kotlin class is marked as [Serializable], its descriptor kind will be [CLASS].
 *
 * ### Lists
 * [LIST] represent a structure with potentially unknown in advance number of elements of the same type.
 * All standard serializable [List] implementors and arrays are represented as [LIST] kind of the same type.
 *
 * ### Maps
 * [MAP] represent a structure with potentially unknown in advance number of key-value pairs of the same type.
 * All standard serializable [Map] implementors are represented as [Map] kind of the same type.
 *
 * ### Kotlin objects
 * A singleton object defined with `object` keyword with an [OBJECT] kind.
 * By default, objects are serialized as empty structures without any states and their identity is preserved
 * across serialization within the same process, so you always have the same instance of the object.
 *
 * ### Serializers interaction
 * Serialization formats typically handle these kinds by marking structure start and end.
 * E.g. the following serializable class `class IntHolder(myValue: Int)` of structure kind [CLASS] is handled by
 * serializer as the following call sequence:
 * ```
 * val composite = encoder.beginStructure(descriptor) // Denotes the start of the structure
 * composite.encodeIntElement(descriptor, index = 0, holder.myValue)
 * composite.endStructure(descriptor) // Denotes the end of the structure
 * ```
 * and its corresponding [Decoder] counterpart.
 *
 * ### Serial descriptor implementors note
 * These kinds can be used not only for collection and regular classes.
 * For example, provided serializer for [Map.Entry] represents it as [Map] type, so it is serialized
 * as `{"actualKey": "actualValue"}` map directly instead of `{"key": "actualKey", "value": "actualValue"}`
 */
__attribute__((swift_name("Kotlinx_serialization_coreStructureKind")))
@interface KmpCalendarKotlinx_serialization_coreStructureKind : KmpCalendarKotlinx_serialization_coreSerialKind
@end


/**
 * Structure kind for regular classes with an arbitrary, but known statically, structure.
 * Serializers typically encode classes with calls to [Encoder.beginStructure] and [CompositeEncoder.endStructure],
 * writing the elements of the class between these calls.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreStructureKind.CLASS")))
@interface KmpCalendarKotlinx_serialization_coreStructureKindCLASS : KmpCalendarKotlinx_serialization_coreStructureKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreStructureKindCLASS *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Structure kind for regular classes with an arbitrary, but known statically, structure.
 * Serializers typically encode classes with calls to [Encoder.beginStructure] and [CompositeEncoder.endStructure],
 * writing the elements of the class between these calls.
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)cLASS __attribute__((swift_name("init()")));
@end


/**
 * Structure kind for lists and arrays of an arbitrary length.
 * Serializers typically encode classes with calls to [Encoder.beginCollection] and [CompositeEncoder.endStructure],
 * writing the elements of the list between these calls.
 * Built-in list serializers treat elements as homogeneous, though application-specific serializers may impose
 * application-specific restrictions on specific [LIST] types.
 *
 * Example of such application-specific serialization may be class `class ListOfThreeElements() : List<Any>`,
 * for which an author of the serializer knows that while it is `List<Any>`, in fact, is always has three elements
 * of a known type (e.g. the first is always a string, the second one is always an int etc.)
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreStructureKind.LIST")))
@interface KmpCalendarKotlinx_serialization_coreStructureKindLIST : KmpCalendarKotlinx_serialization_coreStructureKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreStructureKindLIST *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Structure kind for lists and arrays of an arbitrary length.
 * Serializers typically encode classes with calls to [Encoder.beginCollection] and [CompositeEncoder.endStructure],
 * writing the elements of the list between these calls.
 * Built-in list serializers treat elements as homogeneous, though application-specific serializers may impose
 * application-specific restrictions on specific [LIST] types.
 *
 * Example of such application-specific serialization may be class `class ListOfThreeElements() : List<Any>`,
 * for which an author of the serializer knows that while it is `List<Any>`, in fact, is always has three elements
 * of a known type (e.g. the first is always a string, the second one is always an int etc.)
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)lIST __attribute__((swift_name("init()")));
@end


/**
 * Structure kind for maps of an arbitrary length.
 * Serializers typically encode classes with calls to [Encoder.beginCollection] and [CompositeEncoder.endStructure],
 * writing the elements of the map between these calls.
 *
 * Built-in map serializers treat elements as homogeneous, though application-specific serializers may impose
 * application-specific restrictions on specific [MAP] types.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreStructureKind.MAP")))
@interface KmpCalendarKotlinx_serialization_coreStructureKindMAP : KmpCalendarKotlinx_serialization_coreStructureKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreStructureKindMAP *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Structure kind for maps of an arbitrary length.
 * Serializers typically encode classes with calls to [Encoder.beginCollection] and [CompositeEncoder.endStructure],
 * writing the elements of the map between these calls.
 *
 * Built-in map serializers treat elements as homogeneous, though application-specific serializers may impose
 * application-specific restrictions on specific [MAP] types.
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)mAP __attribute__((swift_name("init()")));
@end


/**
 * Structure kind for singleton objects defined with `object` keyword.
 * By default, objects are serialized as empty structures without any state and their identity is preserved
 * across serialization within the same process, so you always have the same instance of the object.
 *
 * Empty structure is represented as a call to [Encoder.beginStructure] with the following [CompositeEncoder.endStructure]
 * without any intermediate encodings.
 */
__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_serialization_coreStructureKind.OBJECT")))
@interface KmpCalendarKotlinx_serialization_coreStructureKindOBJECT : KmpCalendarKotlinx_serialization_coreStructureKind
@property (class, readonly, getter=shared) KmpCalendarKotlinx_serialization_coreStructureKindOBJECT *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));

/**
 * Structure kind for singleton objects defined with `object` keyword.
 * By default, objects are serialized as empty structures without any state and their identity is preserved
 * across serialization within the same process, so you always have the same instance of the object.
 *
 * Empty structure is represented as a call to [Encoder.beginStructure] with the following [CompositeEncoder.endStructure]
 * without any intermediate encodings.
 */
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)oBJECT __attribute__((swift_name("init()")));
@end

__attribute__((swift_name("KotlinIterator")))
@protocol KmpCalendarKotlinIterator
@required
- (BOOL)hasNext __attribute__((swift_name("hasNext()")));
- (id _Nullable)next __attribute__((swift_name("next()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("KotlinInstant.Companion")))
@interface KmpCalendarKotlinInstantCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinInstantCompanion *shared __attribute__((swift_name("shared")));
@property (readonly) KmpCalendarKotlinInstant *DISTANT_FUTURE __attribute__((swift_name("DISTANT_FUTURE")));
@property (readonly) KmpCalendarKotlinInstant *DISTANT_PAST __attribute__((swift_name("DISTANT_PAST")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
- (KmpCalendarKotlinInstant *)fromEpochMillisecondsEpochMilliseconds:(int64_t)epochMilliseconds __attribute__((swift_name("fromEpochMilliseconds(epochMilliseconds:)")));
- (KmpCalendarKotlinInstant *)fromEpochSecondsEpochSeconds:(int64_t)epochSeconds nanosecondAdjustment:(int32_t)nanosecondAdjustment __attribute__((swift_name("fromEpochSeconds(epochSeconds:nanosecondAdjustment:)")));
- (KmpCalendarKotlinInstant *)fromEpochSecondsEpochSeconds:(int64_t)epochSeconds nanosecondAdjustment_:(int64_t)nanosecondAdjustment __attribute__((swift_name("fromEpochSeconds(epochSeconds:nanosecondAdjustment_:)")));
- (KmpCalendarKotlinInstant *)now __attribute__((swift_name("now()"))) __attribute__((unavailable("Use Clock.System.now() instead")));
- (KmpCalendarKotlinInstant *)parseInput:(id)input __attribute__((swift_name("parse(input:)")));
- (KmpCalendarKotlinInstant * _Nullable)parseOrNullInput:(id)input __attribute__((swift_name("parseOrNull(input:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeMonth")))
@interface KmpCalendarKotlinx_datetimeMonth : KmpCalendarKotlinEnum<KmpCalendarKotlinx_datetimeMonth *>
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *january __attribute__((swift_name("january")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *february __attribute__((swift_name("february")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *march __attribute__((swift_name("march")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *april __attribute__((swift_name("april")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *may __attribute__((swift_name("may")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *june __attribute__((swift_name("june")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *july __attribute__((swift_name("july")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *august __attribute__((swift_name("august")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *september __attribute__((swift_name("september")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *october __attribute__((swift_name("october")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *november __attribute__((swift_name("november")));
@property (class, readonly) KmpCalendarKotlinx_datetimeMonth *december __attribute__((swift_name("december")));
@property (class, readonly) NSArray<KmpCalendarKotlinx_datetimeMonth *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarKotlinx_datetimeMonth *> *)values __attribute__((swift_name("values()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeLocalDate.Companion")))
@interface KmpCalendarKotlinx_datetimeLocalDateCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinx_datetimeLocalDateCompanion *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
- (id<KmpCalendarKotlinx_datetimeDateTimeFormat>)FormatBlock:(void (^)(id<KmpCalendarKotlinx_datetimeDateTimeFormatBuilderWithDate>))block __attribute__((swift_name("Format(block:)")));
- (KmpCalendarKotlinx_datetimeLocalDate *)fromEpochDaysEpochDays:(int32_t)epochDays __attribute__((swift_name("fromEpochDays(epochDays:)")));
- (KmpCalendarKotlinx_datetimeLocalDate *)fromEpochDaysEpochDays_:(int64_t)epochDays __attribute__((swift_name("fromEpochDays(epochDays_:)")));
- (KmpCalendarKotlinx_datetimeLocalDate * _Nullable)orNullYear:(int32_t)year month:(int32_t)month day:(int32_t)day __attribute__((swift_name("orNull(year:month:day:)")));
- (KmpCalendarKotlinx_datetimeLocalDate * _Nullable)orNullYear:(int32_t)year month:(KmpCalendarKotlinx_datetimeMonth *)month day_:(int32_t)day __attribute__((swift_name("orNull(year:month:day_:)")));
- (KmpCalendarKotlinx_datetimeLocalDate *)parseInput:(id)input format:(id<KmpCalendarKotlinx_datetimeDateTimeFormat>)format __attribute__((swift_name("parse(input:format:)")));
- (id<KmpCalendarKotlinx_serialization_coreKSerializer>)serializer __attribute__((swift_name("serializer()")));
@end

__attribute__((swift_name("KotlinIterable")))
@protocol KmpCalendarKotlinIterable
@required
- (id<KmpCalendarKotlinIterator>)iterator __attribute__((swift_name("iterator()")));
@end

__attribute__((swift_name("KotlinCollection")))
@protocol KmpCalendarKotlinCollection <KmpCalendarKotlinIterable>
@required
- (BOOL)containsElement:(id _Nullable)element __attribute__((swift_name("contains(element:)")));
- (BOOL)containsAllElements:(id)elements __attribute__((swift_name("containsAll(elements:)")));
- (BOOL)isEmpty __attribute__((swift_name("isEmpty()")));
@property (readonly) int32_t size __attribute__((swift_name("size")));
@end

__attribute__((swift_name("Kotlinx_datetimeLocalDateProgression")))
@interface KmpCalendarKotlinx_datetimeLocalDateProgression : KmpCalendarBase <KmpCalendarKotlinCollection>
@property (class, readonly, getter=companion) KmpCalendarKotlinx_datetimeLocalDateProgressionCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *first __attribute__((swift_name("first")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *last __attribute__((swift_name("last")));
@property (readonly) int32_t size __attribute__((swift_name("size")));
- (BOOL)containsElement:(KmpCalendarKotlinx_datetimeLocalDate *)element __attribute__((swift_name("contains(element:)")));
- (BOOL)containsAllElements:(id)elements __attribute__((swift_name("containsAll(elements:)")));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (BOOL)isEmpty __attribute__((swift_name("isEmpty()")));
- (id<KmpCalendarKotlinIterator>)iterator __attribute__((swift_name("iterator()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((swift_name("KotlinClosedRange")))
@protocol KmpCalendarKotlinClosedRange
@required
- (BOOL)containsValue:(id)value __attribute__((swift_name("contains(value:)")));
- (BOOL)isEmpty __attribute__((swift_name("isEmpty()")));
@property (readonly) id endInclusive __attribute__((swift_name("endInclusive")));
@property (readonly) id start __attribute__((swift_name("start")));
@end


/**
 * @note annotations
 *   kotlin.SinceKotlin(version="1.9")
*/
__attribute__((swift_name("KotlinOpenEndRange")))
@protocol KmpCalendarKotlinOpenEndRange
@required
- (BOOL)containsValue_:(id)value __attribute__((swift_name("contains(value_:)")));
- (BOOL)isEmpty __attribute__((swift_name("isEmpty()")));
@property (readonly) id endExclusive __attribute__((swift_name("endExclusive")));
@property (readonly) id start __attribute__((swift_name("start")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeLocalDateRange")))
@interface KmpCalendarKotlinx_datetimeLocalDateRange : KmpCalendarKotlinx_datetimeLocalDateProgression <KmpCalendarKotlinClosedRange, KmpCalendarKotlinOpenEndRange>
@property (class, readonly, getter=companion) KmpCalendarKotlinx_datetimeLocalDateRangeCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *endExclusive __attribute__((swift_name("endExclusive"))) __attribute__((deprecated("This throws an exception if the exclusive end if not inside the platform-specific boundaries for LocalDate. The 'endInclusive' property does not throw and should be preferred.")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *endInclusive __attribute__((swift_name("endInclusive")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDate *start __attribute__((swift_name("start")));
- (instancetype)initWithStart:(KmpCalendarKotlinx_datetimeLocalDate *)start endInclusive:(KmpCalendarKotlinx_datetimeLocalDate *)endInclusive __attribute__((swift_name("init(start:endInclusive:)"))) __attribute__((objc_designated_initializer));
- (BOOL)containsElement:(KmpCalendarKotlinx_datetimeLocalDate *)element __attribute__((swift_name("contains(element:)")));
- (BOOL)containsValue:(KmpCalendarKotlinx_datetimeLocalDate *)element __attribute__((swift_name("contains(value:)")));
- (BOOL)containsValue_:(KmpCalendarKotlinx_datetimeLocalDate *)element __attribute__((swift_name("contains(value_:)")));
- (BOOL)isEmpty __attribute__((swift_name("isEmpty()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((swift_name("Kotlinx_datetimeDateTimeFormat")))
@protocol KmpCalendarKotlinx_datetimeDateTimeFormat
@required
- (NSString *)formatValue:(id _Nullable)value __attribute__((swift_name("format(value:)")));
- (id<KmpCalendarKotlinAppendable>)formatToAppendable:(id<KmpCalendarKotlinAppendable>)appendable value:(id _Nullable)value __attribute__((swift_name("formatTo(appendable:value:)")));
- (id _Nullable)parseInput:(id)input __attribute__((swift_name("parse(input:)")));
- (id _Nullable)parseOrNullInput:(id)input __attribute__((swift_name("parseOrNull(input:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimePadding")))
@interface KmpCalendarKotlinx_datetimePadding : KmpCalendarKotlinEnum<KmpCalendarKotlinx_datetimePadding *>
@property (class, readonly) KmpCalendarKotlinx_datetimePadding *none __attribute__((swift_name("none")));
@property (class, readonly) KmpCalendarKotlinx_datetimePadding *zero __attribute__((swift_name("zero")));
@property (class, readonly) KmpCalendarKotlinx_datetimePadding *space __attribute__((swift_name("space")));
@property (class, readonly) NSArray<KmpCalendarKotlinx_datetimePadding *> *entries __attribute__((swift_name("entries")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
- (instancetype)initWithName:(NSString *)name ordinal:(int32_t)ordinal __attribute__((swift_name("init(name:ordinal:)"))) __attribute__((objc_designated_initializer)) __attribute__((unavailable));
+ (KmpCalendarKotlinArray<KmpCalendarKotlinx_datetimePadding *> *)values __attribute__((swift_name("values()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeDayOfWeekNames")))
@interface KmpCalendarKotlinx_datetimeDayOfWeekNames : KmpCalendarBase
@property (class, readonly, getter=companion) KmpCalendarKotlinx_datetimeDayOfWeekNamesCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) NSArray<NSString *> *names __attribute__((swift_name("names")));
- (instancetype)initWithNames:(NSArray<NSString *> *)names __attribute__((swift_name("init(names:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithMonday:(NSString *)monday tuesday:(NSString *)tuesday wednesday:(NSString *)wednesday thursday:(NSString *)thursday friday:(NSString *)friday saturday:(NSString *)saturday sunday:(NSString *)sunday __attribute__((swift_name("init(monday:tuesday:wednesday:thursday:friday:saturday:sunday:)"))) __attribute__((objc_designated_initializer));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeMonthNames")))
@interface KmpCalendarKotlinx_datetimeMonthNames : KmpCalendarBase
@property (class, readonly, getter=companion) KmpCalendarKotlinx_datetimeMonthNamesCompanion *companion __attribute__((swift_name("companion")));
@property (readonly) NSArray<NSString *> *names __attribute__((swift_name("names")));
- (instancetype)initWithNames:(NSArray<NSString *> *)names __attribute__((swift_name("init(names:)"))) __attribute__((objc_designated_initializer));
- (instancetype)initWithJanuary:(NSString *)january february:(NSString *)february march:(NSString *)march april:(NSString *)april may:(NSString *)may june:(NSString *)june july:(NSString *)july august:(NSString *)august september:(NSString *)september october:(NSString *)october november:(NSString *)november december:(NSString *)december __attribute__((swift_name("init(january:february:march:april:may:june:july:august:september:october:november:december:)"))) __attribute__((objc_designated_initializer));
- (BOOL)isEqual:(id _Nullable)other __attribute__((swift_name("isEqual(_:)")));
- (NSUInteger)hash __attribute__((swift_name("hash()")));
- (NSString *)description __attribute__((swift_name("description()")));
@end


/**
 * Serialization strategy defines the serial form of a type [T], including its structural description,
 * declared by the [descriptor] and the actual serialization process, defined by the implementation
 * of the [serialize] method.
 *
 * [serialize] method takes an instance of [T] and transforms it into its serial form (a sequence of primitives),
 * calling the corresponding [Encoder] methods.
 *
 * A serial form of the type is a transformation of the concrete instance into a sequence of primitive values
 * and vice versa. The serial form is not required to completely mimic the structure of the class, for example,
 * a specific implementation may represent multiple integer values as a single string, omit or add some
 * values that are present in the type, but not in the instance.
 *
 * For a more detailed explanation of the serialization process, please refer to [KSerializer] documentation.
 */
__attribute__((swift_name("Kotlinx_serialization_coreSerializationStrategy")))
@protocol KmpCalendarKotlinx_serialization_coreSerializationStrategy
@required

/**
 * Serializes the [value] of type [T] using the format that is represented by the given [encoder].
 * [serialize] method is format-agnostic and operates with a high-level structured [Encoder] API.
 * Throws [SerializationException] if value cannot be serialized.
 *
 * Example of serialize method:
 * ```
 * class MyData(int: Int, stringList: List<String>, alwaysZero: Long)
 *
 * fun serialize(encoder: Encoder, value: MyData): Unit = encoder.encodeStructure(descriptor) {
 *     // encodeStructure encodes beginning and end of the structure
 *     // encode 'int' property as Int
 *     encodeIntElement(descriptor, index = 0, value.int)
 *     // encode 'stringList' property as List<String>
 *     encodeSerializableElement(descriptor, index = 1, serializer<List<String>>, value.stringList)
 *     // don't encode 'alwaysZero' property because we decided to do so
 * } // end of the structure
 * ```
 *
 * @throws SerializationException in case of any serialization-specific error
 * @throws IllegalArgumentException if the supplied input does not comply encoder's specification
 * @see KSerializer for additional information about general contracts and exception specifics
 */
- (void)serializeEncoder:(id<KmpCalendarKotlinx_serialization_coreEncoder>)encoder value:(id _Nullable)value __attribute__((swift_name("serialize(encoder:value:)")));

/**
 * Describes the structure of the serializable representation of [T], produced
 * by this serializer.
 */
@property (readonly) id<KmpCalendarKotlinx_serialization_coreSerialDescriptor> descriptor __attribute__((swift_name("descriptor")));
@end


/**
 * Deserialization strategy defines the serial form of a type [T], including its structural description,
 * declared by the [descriptor] and the actual deserialization process, defined by the implementation
 * of the [deserialize] method.
 *
 * [deserialize] method takes an instance of [Decoder], and, knowing the serial form of the [T],
 * invokes primitive retrieval methods on the decoder and then transforms the received primitives
 * to an instance of [T].
 *
 * A serial form of the type is a transformation of the concrete instance into a sequence of primitive values
 * and vice versa. The serial form is not required to completely mimic the structure of the class, for example,
 * a specific implementation may represent multiple integer values as a single string, omit or add some
 * values that are present in the type, but not in the instance.
 *
 * For a more detailed explanation of the serialization process, please refer to [KSerializer] documentation.
 */
__attribute__((swift_name("Kotlinx_serialization_coreDeserializationStrategy")))
@protocol KmpCalendarKotlinx_serialization_coreDeserializationStrategy
@required

/**
 * Deserializes the value of type [T] using the format that is represented by the given [decoder].
 * [deserialize] method is format-agnostic and operates with a high-level structured [Decoder] API.
 * As long as most of the formats imply an arbitrary order of properties, deserializer should be able
 * to decode these properties in an arbitrary order and in a format-agnostic way.
 * For that purposes, [CompositeDecoder.decodeElementIndex]-based loop is used: decoder firstly
 * signals property at which index it is ready to decode and then expects caller to decode
 * property with the given index.
 *
 * Throws [SerializationException] if value cannot be deserialized.
 *
 * Example of deserialize method:
 * ```
 * class MyData(int: Int, stringList: List<String>, alwaysZero: Long)
 *
 * fun deserialize(decoder: Decoder): MyData = decoder.decodeStructure(descriptor) {
 *     // decodeStructure decodes beginning and end of the structure
 *     var int: Int? = null
 *     var list: List<String>? = null
 *     loop@ while (true) {
 *         when (val index = decodeElementIndex(descriptor)) {
 *             DECODE_DONE -> break@loop
 *             0 -> {
 *                 // Decode 'int' property as Int
 *                 int = decodeIntElement(descriptor, index = 0)
 *             }
 *             1 -> {
 *                 // Decode 'stringList' property as List<String>
 *                 list = decodeSerializableElement(descriptor, index = 1, serializer<List<String>>())
 *             }
 *             else -> throw SerializationException("Unexpected index $index")
 *         }
 *      }
 *     if (int == null || list == null) throwMissingFieldException()
 *     // Always use 0 as a value for alwaysZero property because we decided to do so.
 *     return MyData(int, list, alwaysZero = 0L)
 * }
 * ```
 *
 * @throws MissingFieldException if non-optional fields were not found during deserialization
 * @throws SerializationException in case of any deserialization-specific error
 * @throws IllegalArgumentException if the decoded input is not a valid instance of [T]
 * @see KSerializer for additional information about general contracts and exception specifics
 */
- (id _Nullable)deserializeDecoder:(id<KmpCalendarKotlinx_serialization_coreDecoder>)decoder __attribute__((swift_name("deserialize(decoder:)")));

/**
 * Describes the structure of the serializable representation of [T], that current
 * deserializer is able to deserialize.
 */
@property (readonly) id<KmpCalendarKotlinx_serialization_coreSerialDescriptor> descriptor __attribute__((swift_name("descriptor")));
@end


/**
 * KSerializer is responsible for the representation of a serial form of a type [T]
 * in terms of [encoders][Encoder] and [decoders][Decoder] and for constructing and deconstructing [T]
 * from/to a sequence of encoding primitives. For classes marked with [@Serializable][Serializable], can be
 * obtained from generated companion extension `.serializer()` or from [serializer<T>()][serializer] function.
 *
 * Serialization is decoupled from the encoding process to make it completely format-agnostic.
 * Serialization represents a type as its serial form and is abstracted from the actual
 * format (whether its JSON, ProtoBuf or a hashing) and unaware of the underlying storage
 * (whether it is a string builder, byte array or a network socket), while
 * encoding/decoding is abstracted from a particular type and its serial form and is responsible
 * for transforming primitives ("here in an int property 'foo'" call from a serializer) into a particular
 * format-specific representation ("for a given int, append a property name in quotation marks,
 * then append a colon, then append an actual value" for JSON) and how to retrieve a primitive
 * ("give me an int that is 'foo' property") from the underlying representation ("expect the next string to be 'foo',
 * parse it, then parse colon, then parse a string until the next comma as an int and return it).
 *
 * Serial form consists of a structural description, declared by the [descriptor] and
 * actual serialization and deserialization processes, defined by the corresponding
 * [serialize] and [deserialize] methods implementation.
 *
 * Structural description specifies how the [T] is represented in the serial form:
 * its [kind][SerialKind] (e.g. whether it is represented as a primitive, a list or a class),
 * its [elements][SerialDescriptor.elementNames] and their [positional names][SerialDescriptor.getElementName].
 *
 * Serialization process is defined as a sequence of calls to an [Encoder], and transforms a type [T]
 * into a stream of format-agnostic primitives that represent [T], such as "here is an int, here is a double
 * and here is another nested object". It can be demonstrated by the example:
 * ```
 * class MyData(int: Int, stringList: List<String>, alwaysZero: Long)
 *
 * // .. serialize method of a corresponding serializer
 * fun serialize(encoder: Encoder, value: MyData): Unit = encoder.encodeStructure(descriptor) {
 *     // encodeStructure encodes beginning and end of the structure
 *     // encode 'int' property as Int
 *     encodeIntElement(descriptor, index = 0, value.int)
 *     // encode 'stringList' property as List<String>
 *     encodeSerializableElement(descriptor, index = 1, serializer<List<String>>, value.stringList)
 *     // don't encode 'alwaysZero' property because we decided to do so
 * } // end of the structure
 * ```
 *
 * Deserialization process is symmetric and uses [Decoder].
 *
 * ### Exception types for `KSerializer` implementation
 *
 * Implementations of [serialize] and [deserialize] methods are allowed to throw
 * any subtype of [IllegalArgumentException] in order to indicate serialization
 * and deserialization errors.
 *
 * For serializer implementations, it is recommended to throw subclasses of [SerializationException] for
 * any serialization-specific errors related to invalid or unsupported format of the data
 * and [IllegalStateException] for errors during validation of the data.
 */
__attribute__((swift_name("Kotlinx_serialization_coreKSerializer")))
@protocol KmpCalendarKotlinx_serialization_coreKSerializer <KmpCalendarKotlinx_serialization_coreSerializationStrategy, KmpCalendarKotlinx_serialization_coreDeserializationStrategy>
@required
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeLocalDateProgression.Companion")))
@interface KmpCalendarKotlinx_datetimeLocalDateProgressionCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinx_datetimeLocalDateProgressionCompanion *shared __attribute__((swift_name("shared")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeLocalDateRange.Companion")))
@interface KmpCalendarKotlinx_datetimeLocalDateRangeCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinx_datetimeLocalDateRangeCompanion *shared __attribute__((swift_name("shared")));
@property (readonly) KmpCalendarKotlinx_datetimeLocalDateRange *EMPTY __attribute__((swift_name("EMPTY")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
@end

__attribute__((swift_name("KotlinAppendable")))
@protocol KmpCalendarKotlinAppendable
@required

/**
 * @note annotations
 *   kotlin.IgnorableReturnValue
*/
- (id<KmpCalendarKotlinAppendable>)appendValue:(unichar)value __attribute__((swift_name("append(value:)")));

/**
 * @note annotations
 *   kotlin.IgnorableReturnValue
*/
- (id<KmpCalendarKotlinAppendable>)appendValue_:(id _Nullable)value __attribute__((swift_name("append(value_:)")));

/**
 * @note annotations
 *   kotlin.IgnorableReturnValue
*/
- (id<KmpCalendarKotlinAppendable>)appendValue:(id _Nullable)value startIndex:(int32_t)startIndex endIndex:(int32_t)endIndex __attribute__((swift_name("append(value:startIndex:endIndex:)")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeDayOfWeekNames.Companion")))
@interface KmpCalendarKotlinx_datetimeDayOfWeekNamesCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinx_datetimeDayOfWeekNamesCompanion *shared __attribute__((swift_name("shared")));
@property (readonly) KmpCalendarKotlinx_datetimeDayOfWeekNames *ENGLISH_ABBREVIATED __attribute__((swift_name("ENGLISH_ABBREVIATED")));
@property (readonly) KmpCalendarKotlinx_datetimeDayOfWeekNames *ENGLISH_FULL __attribute__((swift_name("ENGLISH_FULL")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("Kotlinx_datetimeMonthNames.Companion")))
@interface KmpCalendarKotlinx_datetimeMonthNamesCompanion : KmpCalendarBase
@property (class, readonly, getter=shared) KmpCalendarKotlinx_datetimeMonthNamesCompanion *shared __attribute__((swift_name("shared")));
@property (readonly) KmpCalendarKotlinx_datetimeMonthNames *ENGLISH_ABBREVIATED __attribute__((swift_name("ENGLISH_ABBREVIATED")));
@property (readonly) KmpCalendarKotlinx_datetimeMonthNames *ENGLISH_FULL __attribute__((swift_name("ENGLISH_FULL")));
+ (instancetype)alloc __attribute__((unavailable));
+ (instancetype)allocWithZone:(struct _NSZone *)zone __attribute__((unavailable));
+ (instancetype)companion __attribute__((swift_name("init()")));
@end


/**
 * Encoder is a core serialization primitive that encapsulates the knowledge of the underlying
 * format and its storage, exposing only structural methods to the serializer, making it completely
 * format-agnostic. Serialization process transforms a single value into the sequence of its
 * primitive elements, also called its serial form, while encoding transforms these primitive elements into an actual
 * format representation: JSON string, ProtoBuf ByteArray, in-memory map representation etc.
 *
 * Encoder provides high-level API that operates with basic primitive types, collections
 * and nested structures. Internally, encoder represents output storage and operates with its state
 * and lower level format-specific details.
 *
 * To be more specific, serialization transforms a value into a sequence of "here is an int, here is
 * a double, here a list of strings and here is another object that is a nested int", while encoding
 * transforms this sequence into a format-specific commands such as "insert opening curly bracket
 * for a nested object start, insert a name of the value, and the value separated with colon for an int etc."
 *
 * The symmetric interface for the deserialization process is [Decoder].
 *
 * ### Serialization. Primitives
 *
 * If a class is represented as a single [primitive][PrimitiveKind] value in its serialized form,
 * then one of the `encode*` methods (e.g. [encodeInt]) can be used directly.
 *
 * ### Serialization. Structured types.
 *
 * If a class is represented as a structure or has multiple values in its serialized form,
 * `encode*` methods are not that helpful, because they do not allow working with collection types or establish structure boundaries.
 * All these capabilities are delegated to the [CompositeEncoder] interface with a more specific API surface.
 * To denote a structure start, [beginStructure] should be used.
 * ```
 * // Denote the structure start,
 * val composite = encoder.beginStructure(descriptor)
 * // Encoding all elements within the structure using 'composite'
 * ...
 * // Denote the structure end
 * composite.endStructure(descriptor)
 * ```
 *
 * E.g. if the encoder belongs to JSON format, then [beginStructure] will write an opening bracket
 * (`{` or `[`, depending on the descriptor kind), returning the [CompositeEncoder] that is aware of colon separator,
 * that should be appended between each key-value pair, whilst [CompositeEncoder.endStructure] will write a closing bracket.
 *
 * ### Exception guarantees
 *
 * For the regular exceptions, such as invalid input, conflicting serial names,
 * [SerializationException] can be thrown by any encoder methods.
 * It is recommended to declare a format-specific subclass of [SerializationException] and throw it.
 *
 * ### Exception safety
 *
 * In general, catching [SerializationException] from any of `encode*` methods is not allowed and produces unspecified behaviour.
 * After thrown exception, the current encoder is left in an arbitrary state, no longer suitable for further encoding.
 *
 * ### Format encapsulation
 *
 * For example, for the following serializer:
 * ```
 * class StringHolder(val stringValue: String)
 *
 * object StringPairDeserializer : SerializationStrategy<StringHolder> {
 *    override val descriptor = ...
 *
 *    override fun serializer(encoder: Encoder, value: StringHolder) {
 *        // Denotes start of the structure, StringHolder is not a "plain" data type
 *        val composite = encoder.beginStructure(descriptor)
 *        // Encode the nested string value
 *        composite.encodeStringElement(descriptor, index = 0)
 *        // Denotes end of the structure
 *        composite.endStructure(descriptor)
 *    }
 * }
 * ```
 *
 * This serializer does not know anything about the underlying storage and will work with any properly-implemented encoder.
 * JSON, for example, writes an opening bracket `{` during the `beginStructure` call, writes `stringValue` key along
 * with its value in `encodeStringElement` and writes the closing bracket `}` during the `endStructure`.
 * XML would do roughly the same, but with different separators and structures, while ProtoBuf
 * machinery could be completely different.
 * In any case, all these parsing details are encapsulated by an encoder.
 *
 * ### Encoder implementation.
 *
 * While being strictly typed, an underlying format can transform actual types in the way it wants.
 * For example, a format can support only string types and encode/decode all primitives in a string form:
 * ```
 * StringFormatEncoder : Encoder {
 *
 *     ...
 *     override fun encodeDouble(value: Double) = encodeString(value.toString())
 *     override fun encodeInt(value: Int) = encodeString(value.toString())
 *     ...
 * }
 * ```
 *
 * ### Not stable for inheritance
 *
 * `Encoder` interface is not stable for inheritance in 3rd party libraries, as new methods
 * might be added to this interface or contracts of the existing methods can be changed.
 */
__attribute__((swift_name("Kotlinx_serialization_coreEncoder")))
@protocol KmpCalendarKotlinx_serialization_coreEncoder
@required

/**
 * Encodes the beginning of the collection with size [collectionSize] and the given serializer of its type parameters.
 * This method has to be implemented only if you need to know collection size in advance, otherwise, [beginStructure] can be used.
 */
- (id<KmpCalendarKotlinx_serialization_coreCompositeEncoder>)beginCollectionDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor collectionSize:(int32_t)collectionSize __attribute__((swift_name("beginCollection(descriptor:collectionSize:)")));

/**
 * Encodes the beginning of the nested structure in a serialized form
 * and returns [CompositeDecoder] responsible for encoding this very structure.
 * E.g the hierarchy:
 * ```
 * class StringHolder(val stringValue: String)
 * class Holder(val stringHolder: StringHolder)
 * ```
 *
 * with the following serialized form in JSON:
 * ```
 * {
 *   "stringHolder" : { "stringValue": "value" }
 * }
 * ```
 *
 * will be roughly represented as the following sequence of calls:
 * ```
 * // Holder serializer
 * fun serialize(encoder: Encoder, value: Holder) {
 *     val composite = encoder.beginStructure(descriptor) // the very first opening bracket '{'
 *     composite.encodeSerializableElement(descriptor, 0, value.stringHolder) // Serialize nested StringHolder
 *     composite.endStructure(descriptor) // The very last closing bracket
 * }
 *
 * // StringHolder serializer
 * fun serialize(encoder: Encoder, value: StringHolder) {
 *     val composite = encoder.beginStructure(descriptor) // One more '{' when the key "stringHolder" is already written
 *     composite.encodeStringElement(descriptor, 0, value.stringValue) // Serialize actual value
 *     composite.endStructure(descriptor) // Closing bracket
 * }
 * ```
 */
- (id<KmpCalendarKotlinx_serialization_coreCompositeEncoder>)beginStructureDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("beginStructure(descriptor:)")));

/**
 * Encodes a boolean value.
 * Corresponding kind is [PrimitiveKind.BOOLEAN].
 */
- (void)encodeBooleanValue:(BOOL)value __attribute__((swift_name("encodeBoolean(value:)")));

/**
 * Encodes a single byte value.
 * Corresponding kind is [PrimitiveKind.BYTE].
 */
- (void)encodeByteValue:(int8_t)value __attribute__((swift_name("encodeByte(value:)")));

/**
 * Encodes a 16-bit unicode character value.
 * Corresponding kind is [PrimitiveKind.CHAR].
 */
- (void)encodeCharValue:(unichar)value __attribute__((swift_name("encodeChar(value:)")));

/**
 * Encodes a 64-bit IEEE 754 floating point value.
 * Corresponding kind is [PrimitiveKind.DOUBLE].
 */
- (void)encodeDoubleValue:(double)value __attribute__((swift_name("encodeDouble(value:)")));

/**
 * Encodes a enum value that is stored at the [index] in [enumDescriptor] elements collection.
 * Corresponding kind is [SerialKind.ENUM].
 *
 * E.g. for the enum `enum class Letters { A, B, C, D }` and
 * serializable value "C", [encodeEnum] method should be called with `2` as am index.
 *
 * This method does not imply any restrictions on the output format,
 * the format is free to store the enum by its name, index, ordinal or any other
 */
- (void)encodeEnumEnumDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)enumDescriptor index:(int32_t)index __attribute__((swift_name("encodeEnum(enumDescriptor:index:)")));

/**
 * Encodes a 32-bit IEEE 754 floating point value.
 * Corresponding kind is [PrimitiveKind.FLOAT].
 */
- (void)encodeFloatValue:(float)value __attribute__((swift_name("encodeFloat(value:)")));

/**
 * Returns [Encoder] for encoding an underlying type of a value class in an inline manner.
 * [descriptor] describes a serializable value class.
 *
 * Namely, for the `@Serializable @JvmInline value class MyInt(val my: Int)`,
 * the following sequence is used:
 * ```
 * thisEncoder.encodeInline(MyInt.serializer().descriptor).encodeInt(my)
 * ```
 *
 * Current encoder may return any other instance of [Encoder] class, depending on the provided [descriptor].
 * For example, when this function is called on Json encoder with `UInt.serializer().descriptor`, the returned encoder is able
 * to encode unsigned integers.
 *
 * Note that this function returns [Encoder] instead of the [CompositeEncoder]
 * because value classes always have the single property.
 * Calling [Encoder.beginStructure] on returned instance leads to an unspecified behavior and, in general, is prohibited.
 */
- (id<KmpCalendarKotlinx_serialization_coreEncoder>)encodeInlineDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("encodeInline(descriptor:)")));

/**
 * Encodes a 32-bit integer value.
 * Corresponding kind is [PrimitiveKind.INT].
 */
- (void)encodeIntValue:(int32_t)value __attribute__((swift_name("encodeInt(value:)")));

/**
 * Encodes a 64-bit integer value.
 * Corresponding kind is [PrimitiveKind.LONG].
 */
- (void)encodeLongValue:(int64_t)value __attribute__((swift_name("encodeLong(value:)")));

/**
 * Notifies the encoder that value of a nullable type that is
 * being serialized is not null. It should be called before writing a non-null value
 * of nullable type:
 * ```
 * // Could be String? serialize method
 * if (value != null) {
 *     encoder.encodeNotNullMark()
 *     encoder.encodeStringValue(value)
 * } else {
 *     encoder.encodeNull()
 * }
 * ```
 *
 * This method has a use in highly-performant binary formats and can
 * be safely ignore by most of the regular formats.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (void)encodeNotNullMark __attribute__((swift_name("encodeNotNullMark()")));

/**
 * Encodes `null` value.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (void)encodeNull __attribute__((swift_name("encodeNull()")));

/**
 * Encodes the nullable [value] of type [T] by delegating the encoding process to the given [serializer].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (void)encodeNullableSerializableValueSerializer:(id<KmpCalendarKotlinx_serialization_coreSerializationStrategy>)serializer value:(id _Nullable)value __attribute__((swift_name("encodeNullableSerializableValue(serializer:value:)")));

/**
 * Encodes the [value] of type [T] by delegating the encoding process to the given [serializer].
 * For example, `encodeInt` call is equivalent to delegating integer encoding to [Int.serializer][Int.Companion.serializer]:
 * `encodeSerializableValue(Int.serializer())`
 */
- (void)encodeSerializableValueSerializer:(id<KmpCalendarKotlinx_serialization_coreSerializationStrategy>)serializer value:(id _Nullable)value __attribute__((swift_name("encodeSerializableValue(serializer:value:)")));

/**
 * Encodes a 16-bit short value.
 * Corresponding kind is [PrimitiveKind.SHORT].
 */
- (void)encodeShortValue:(int16_t)value __attribute__((swift_name("encodeShort(value:)")));

/**
 * Encodes a string value.
 * Corresponding kind is [PrimitiveKind.STRING].
 */
- (void)encodeStringValue:(NSString *)value __attribute__((swift_name("encodeString(value:)")));

/**
 * Context of the current serialization process, including contextual and polymorphic serialization and,
 * potentially, a format-specific configuration.
 */
@property (readonly) KmpCalendarKotlinx_serialization_coreSerializersModule *serializersModule __attribute__((swift_name("serializersModule")));
@end


/**
 * Serial descriptor is an inherent property of [KSerializer] that describes the structure of the serializable type.
 * The structure of the serializable type is not only the characteristic of the type itself, but also of the serializer as well,
 * meaning that one type can have multiple descriptors that have completely different structures.
 *
 * For example, the class `class Color(val rgb: Int)` can have multiple serializable representations,
 * such as `{"rgb": 255}`, `"#0000FF"`, `[0, 0, 255]` and `{"red": 0, "green": 0, "blue": 255}`.
 * Representations are determined by serializers, and each such serializer has its own descriptor that identifies
 * each structure in a distinguishable and format-agnostic manner.
 *
 * ### Structure
 * Serial descriptor is identified by its [name][serialName] and consists of a kind, potentially empty set of
 * children elements, and additional metadata.
 *
 * * [serialName] uniquely identifies the descriptor (and the corresponding serializer) for non-generic types.
 *   For generic types, the actual type substitution is omitted from the string representation, and the name
 *   identifies the family of the serializers without type substitutions. However, type substitution is accounted for
 *   in [equals] and [hashCode] operations, meaning that descriptors of generic classes with the same name but different type
 *   arguments are not equal to each other.
 *   [serialName] is typically used to specify the type of the target class during serialization of polymorphic and sealed
 *   classes, for observability and diagnostics.
 * * [Kind][SerialKind] defines what this descriptor represents: primitive, enum, object, collection, etc.
 * * Children elements are represented as serial descriptors as well and define the structure of the type's elements.
 * * Metadata carries additional information, such as [nullability][nullable], [optionality][isElementOptional]
 *   and [serial annotations][getElementAnnotations].
 *
 * ### Usages
 * There are two general usages of the descriptors: THE serialization process and serialization introspection.
 *
 * #### Serialization
 * Serial descriptor is used as a bridge between decoders/encoders and serializers.
 * When asking for a next element, the serializer provides an expected descriptor to the decoder, and,
 * based on the descriptor content, the decoder decides how to parse its input.
 * In JSON, for example, when the encoder is asked to encode the next element and this element
 * is a subtype of [List], the encoder receives a descriptor with [StructureKind.LIST] and, based on that,
 * first writes an opening square bracket before writing the content of the list.
 *
 * Serial descriptor _encapsulates_ the structure of the data, so serializers can be free from
 * format-specific details. `ListSerializer` knows nothing about JSON and square brackets, providing
 * only the structure of the data and delegating encoding decision to the format itself.
 *
 * #### Introspection
 * Another usage of a serial descriptor is type introspection without its serialization.
 * Introspection can be used to check whether the given serializable class complies the
 * corresponding scheme and to generate JSON or ProtoBuf schema from the given class.
 *
 * ### Indices
 * Serial descriptor API operates with children indices.
 * For the fixed-size structures, such as regular classes, index is represented by a value in
 * the range from zero to [elementsCount] and represent and index of the property in this class.
 * Consequently, primitives do not have children and their element count is zero.
 *
 * For collections and maps indices do not have a fixed bound. Regular collections descriptors usually
 * have one element (`T`, maps have two, one for keys and one for values), but potentially unlimited
 * number of actual children values. Valid indices range is not known statically,
 * and implementations of such a descriptor should provide consistent and unbounded names and indices.
 *
 * In practice, for regular classes it is allowed to invoke `getElement*(index)` methods
 * with an index from `0` to [elementsCount] range and the element at the particular index corresponds to the
 * serializable property at the given position.
 * For collections and maps, index parameter for `getElement*(index)` methods is effectively bounded
 * by the maximal number of collection/map elements.
 *
 * ### Thread-safety and mutability
 * Serial descriptor implementation should be immutable and, thus, thread-safe.
 *
 * ### Equality and caching
 * Serial descriptor can be used as a unique identifier for format-specific data or schemas and
 * this implies the following restrictions on its `equals` and `hashCode`:
 *
 * An [equals] implementation should use both [serialName] and elements structure.
 * Comparing [elementDescriptors] directly is discouraged,
 * because it may cause a stack overflow error, e.g., if a serializable class `T` contains elements of type `T`.
 * To avoid it, a serial descriptor implementation should compare only descriptors
 * of class' type parameters, in a way that `serializer<Box<Int>>().descriptor != serializer<Box<String>>().descriptor`.
 * If type parameters are equal, descriptor structure should be compared by using children elements
 * descriptors' [serialName]s, which correspond to class names
 * (do not confuse with elements' own names, which correspond to properties' names); and/or other [SerialDescriptor]
 * properties, such as [kind].
 * An example of [equals] implementation:
 * ```
 * if (this === other) return true
 * if (other::class != this::class) return false
 * if (serialName != other.serialName) return false
 * if (!typeParametersAreEqual(other)) return false
 * if (this.elementDescriptors().map { it.serialName } != other.elementDescriptors().map { it.serialName }) return false
 * return true
 * ```
 *
 * [hashCode] implementation should use the same properties for computing the result.
 *
 * ### User-defined serial descriptors
 * The best way to define a custom descriptor is to use [buildClassSerialDescriptor] builder function, where
 * for each serializable property the corresponding element is declared.
 *
 * Example:
 * ```
 * // Class with custom serializer and custom serial descriptor
 * class Data(
 *     val intField: Int, // This field is ignored by custom serializer
 *     val longField: Long, // This field is written as long, but in serialized form is named as "_longField"
 *     val stringList: List<String> // This field is written as regular list of strings
 * )
 *
 * // Descriptor for such class:
 * buildClassSerialDescriptor("my.package.Data") {
 *     // intField is deliberately ignored by serializer -- not present in the descriptor as well
 *     element<Long>("_longField") // longField is named as _longField
 *     element("stringField", listSerialDescriptor<String>())
 * }
 *
 * // Example of 'serialize' function for such descriptor
 * override fun serialize(encoder: Encoder, value: Data) {
 *     encoder.encodeStructure(descriptor) {
 *         encodeLongElement(descriptor, 0, value.longField) // Will be written as "_longField" because descriptor's child at index 0 says so
 *         encodeSerializableElement(descriptor, 1, ListSerializer(String.serializer()), value.stringList)
 *     }
 * }
 * ```
 *
 * For classes that are represented as a single primitive value, [PrimitiveSerialDescriptor] builder function can be used instead.
 *
 * ### Consistency violations
 * An implementation of [SerialDescriptor] should be consistent with the implementation of the corresponding [KSerializer].
 * Yet it is not type-checked statically, thus making it possible to declare a non-consistent implementation of descriptor and serializer.
 * In such cases, the behavior of an underlying format is unspecified and may lead to both runtime errors and encoding of
 * corrupted data that is impossible to decode back.
 *
 * ### Not for implementation
 *
 * `SerialDescriptor` interface should not be implemented in 3rd party libraries, as new methods
 * might be added to this interface when kotlinx.serialization adds support for new Kotlin features.
 * This interface is safe to use and construct via [buildClassSerialDescriptor], [PrimitiveSerialDescriptor], and `SerialDescriptor` factory function.
 *
 * @note annotations
 *   kotlin.SubclassOptInRequired(markerClass=[NormalClass(value=kotlinx/serialization/SealedSerializationApi)])
*/
__attribute__((swift_name("Kotlinx_serialization_coreSerialDescriptor")))
@protocol KmpCalendarKotlinx_serialization_coreSerialDescriptor
@required

/**
 * Returns serial annotations of the child element at the given [index].
 * This method differs from `getElementDescriptor(index).annotations` by reporting only
 * element-specific annotations:
 * ```
 * @Serializable
 * @OnClassSerialAnnotation
 * class Nested(...)
 *
 * @Serializable
 * class Outer(@OnPropertySerialAnnotation val nested: Nested)
 *
 * val outerDescriptor = Outer.serializer().descriptor
 *
 * outerDescriptor.getElementAnnotations(0) // Returns [@OnPropertySerialAnnotation]
 * outerDescriptor.getElementDescriptor(0).annotations // Returns [@OnClassSerialAnnotation]
 * ```
 * Only annotations marked with [SerialInfo] are added to the resulting list.
 *
 * @throws IndexOutOfBoundsException for an illegal [index] values.
 * @throws IllegalStateException if the current descriptor does not support children elements (e.g. is a primitive).
 */
- (NSArray<id<KmpCalendarKotlinAnnotation>> *)getElementAnnotationsIndex:(int32_t)index __attribute__((swift_name("getElementAnnotations(index:)")));

/**
 * Retrieves the descriptor of the child element for the given [index].
 * For the property of type `T` on the position `i`, `getElementDescriptor(i)` yields the same result
 * as for `T.serializer().descriptor`, if the serializer for this property is not explicitly overridden
 * with `@Serializable(with = ...`)`, [Polymorphic] or [Contextual].
 * This method can be used to completely introspect the type that the current descriptor describes.
 *
 * Example:
 * ```
 * @Serializable
 * @OnClassSerialAnnotation
 * class Nested(...)
 *
 * @Serializable
 * class Outer(val nested: Nested)
 *
 * val outerDescriptor = Outer.serializer().descriptor
 *
 * outerDescriptor.getElementDescriptor(0).serialName // Returns "Nested"
 * outerDescriptor.getElementDescriptor(0).annotations // Returns [@OnClassSerialAnnotation]
 * ```
 *
 * @throws IndexOutOfBoundsException for illegal [index] values.
 * @throws IllegalStateException if the current descriptor does not support children elements (e.g. is a primitive).
 */
- (id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)getElementDescriptorIndex:(int32_t)index __attribute__((swift_name("getElementDescriptor(index:)")));

/**
 * Returns an index in the children list of the given element by its name or [CompositeDecoder.UNKNOWN_NAME]
 * if there is no such element.
 * The resulting index, if it is not [CompositeDecoder.UNKNOWN_NAME], is guaranteed to be usable with [getElementName].
 *
 * Example:
 *
 * ```
 * @Serializable
 * class User(val name: String, val alias: String?)
 *
 * val userDescriptor = User.serializer().descriptor
 *
 * userDescriptor.getElementIndex("name") // Returns 0
 * userDescriptor.getElementIndex("alias") // Returns 1
 * userDescriptor.getElementIndex("lastName") // Returns CompositeDecoder.UNKNOWN_NAME = -3
 * ```
 */
- (int32_t)getElementIndexName:(NSString *)name __attribute__((swift_name("getElementIndex(name:)")));

/**
 * Returns a positional name of the child at the given [index].
 * Positional name represents a corresponding property name in the class, associated with
 * the current descriptor.
 *
 * Do not confuse with [serialName], which returns class name:
 *
 * ```
 * package my.app
 *
 * @Serializable
 * class User(val name: String)
 *
 * val userDescriptor = User.serializer().descriptor
 *
 * userDescriptor.serialName // Returns "my.app.User"
 * userDescriptor.getElementName(0) // Returns "name"
 * ```
 *
 * @throws IndexOutOfBoundsException for an illegal [index] values.
 * @throws IllegalStateException if the current descriptor does not support children elements (e.g. is a primitive)
 */
- (NSString *)getElementNameIndex:(int32_t)index __attribute__((swift_name("getElementName(index:)")));

/**
 * Whether the element at the given [index] is optional (can be absent in serialized form).
 * For generated descriptors, all elements that have a corresponding default parameter value are
 * marked as optional. Custom serializers can treat optional values in a serialization-specific manner
 * without a default parameters constraint.
 *
 * Example of optionality:
 * ```
 * @Serializable
 * class Holder(
 *     val a: Int, // isElementOptional(0) == false
 *     val b: Int?, // isElementOptional(1) == false
 *     val c: Int? = null, // isElementOptional(2) == true
 *     val d: List<Int>, // isElementOptional(3) == false
 *     val e: List<Int> = listOf(1), // isElementOptional(4) == true
 * )
 * ```
 * Returns `false` for valid indices of collections, maps, and enums.
 *
 * @throws IndexOutOfBoundsException for an illegal [index] values.
 * @throws IllegalStateException if the current descriptor does not support children elements (e.g. is a primitive).
 */
- (BOOL)isElementOptionalIndex:(int32_t)index __attribute__((swift_name("isElementOptional(index:)")));

/**
 * Returns serial annotations of the associated class.
 * Serial annotations can be used to specify additional metadata that may be used during serialization.
 * Only annotations marked with [SerialInfo] are added to the resulting list.
 *
 * Do not confuse with [getElementAnnotations]:
 * ```
 * @Serializable
 * @OnClassSerialAnnotation
 * class Nested(...)
 *
 * @Serializable
 * class Outer(@OnPropertySerialAnnotation val nested: Nested)
 *
 * val outerDescriptor = Outer.serializer().descriptor
 *
 * outerDescriptor.getElementAnnotations(0) // Returns [@OnPropertySerialAnnotation]
 * outerDescriptor.getElementDescriptor(0).annotations // Returns [@OnClassSerialAnnotation]
 * ```
 */
@property (readonly) NSArray<id<KmpCalendarKotlinAnnotation>> *annotations __attribute__((swift_name("annotations")));

/**
 * The number of elements this descriptor describes, besides from the class itself.
 * [elementsCount] describes the number of **semantic** elements, not the number
 * of actual fields/properties in the serialized form, even though they frequently match.
 *
 * For example, for the following class
 * `class Complex(val real: Long, val imaginary: Long)` the corresponding descriptor
 * and the serialized form both have two elements, while for `List<Int>`
 * the corresponding descriptor has a single element (`IntDescriptor`, the type of list element),
 * but from zero up to `Int.MAX_VALUE` values in the serialized form:
 *
 * ```
 * @Serializable
 * class Complex(val real: Long, val imaginary: Long)
 *
 * Complex.serializer().descriptor.elementsCount // Returns 2
 *
 * @Serializable
 * class OuterList(val list: List<Int>)
 *
 * OuterList.serializer().descriptor.getElementDescriptor(0).elementsCount // Returns 1
 * ```
 */
@property (readonly) int32_t elementsCount __attribute__((swift_name("elementsCount")));

/**
 * Returns `true` if this descriptor describes a serializable value class which underlying value
 * is serialized directly.
 *
 * This property is true for serializable `@JvmInline value` classes:
 * ```
 * @Serializable
 * class User(val name: Name)
 *
 * @Serializable
 * @JvmInline
 * value class Name(val value: String)
 *
 * User.serializer().descriptor.isInline // false
 * User.serializer().descriptor.getElementDescriptor(0).isInline // true
 * Name.serializer().descriptor.isInline // true
 * ```
 */
@property (readonly) BOOL isInline __attribute__((swift_name("isInline")));

/**
 * Whether the descriptor describes a nullable type.
 * Returns `true` if associated serializer can serialize/deserialize nullable elements of the described type.
 *
 * Example:
 *
 * ```
 * @Serializable
 * class User(val name: String, val alias: String?)
 *
 * val userDescriptor = User.serializer().descriptor
 *
 * userDescriptor.isNullable // Returns false
 * userDescriptor.getElementDescriptor(0).isNullable // Returns false
 * userDescriptor.getElementDescriptor(1).isNullable // Returns true
 * ```
 */
@property (readonly) BOOL isNullable __attribute__((swift_name("isNullable")));

/**
 * The kind of the serialized form that determines **the shape** of the serialized data.
 * Formats use serial kind to add and parse serializer-agnostic metadata to the result.
 *
 * For example, JSON format wraps [classes][StructureKind.CLASS] and [StructureKind.MAP] into
 * brackets, while ProtoBuf just serialize these types in separate ways.
 *
 * Kind should be consistent with the implementation, for example, if it is a [primitive][PrimitiveKind],
 * then its element count should be zero and vice versa.
 *
 * Example of introspecting kinds:
 *
 * ```
 * @Serializable
 * class User(val name: String)
 *
 * val userDescriptor = User.serializer().descriptor
 *
 * userDescriptor.kind // Returns StructureKind.CLASS
 * userDescriptor.getElementDescriptor(0).kind // Returns PrimitiveKind.STRING
 * ```
 */
@property (readonly) KmpCalendarKotlinx_serialization_coreSerialKind *kind __attribute__((swift_name("kind")));

/**
 * Serial name of the descriptor that identifies a pair of the associated serializer and target class.
 *
 * For generated and default serializers, the serial name is equal to the corresponding class's fully qualified name
 * or, if overridden, [SerialName].
 * Custom serializers should provide a unique serial name that identifies both the serializable class and
 * the serializer itself, ignoring type arguments if they are present, for example: `my.package.LongAsTrimmedString`.
 *
 * Do not confuse with [getElementName], which returns property name:
 *
 * ```
 * package my.app
 *
 * @Serializable
 * class User(val name: String)
 *
 * val userDescriptor = User.serializer().descriptor
 *
 * userDescriptor.serialName // Returns "my.app.User"
 * userDescriptor.getElementName(0) // Returns "name"
 * ```
 */
@property (readonly) NSString *serialName __attribute__((swift_name("serialName")));
@end


/**
 * Decoder is a core deserialization primitive that encapsulates the knowledge of the underlying
 * format and an underlying storage, exposing only structural methods to the deserializer, making it completely
 * format-agnostic. Deserialization process takes a decoder and asks him for a sequence of primitive elements,
 * defined by a deserializer serial form, while decoder knows how to retrieve these primitive elements from an actual format
 * representations.
 *
 * Decoder provides high-level API that operates with basic primitive types, collections
 * and nested structures. Internally, the decoder represents input storage, and operates with its state
 * and lower level format-specific details.
 *
 * To be more specific, serialization asks a decoder for a sequence of "give me an int, give me
 * a double, give me a list of strings and give me another object that is a nested int", while decoding
 * transforms this sequence into a format-specific commands such as "parse the part of the string until the next quotation mark
 * as an int to retrieve an int, parse everything within the next curly braces to retrieve elements of a nested object etc."
 *
 * The symmetric interface for the serialization process is [Encoder].
 *
 * ### Deserialization. Primitives
 *
 * If a class is represented as a single [primitive][PrimitiveKind] value in its serialized form,
 * then one of the `decode*` methods (e.g. [decodeInt]) can be used directly.
 *
 * ### Deserialization. Structured types
 *
 * If a class is represented as a structure or has multiple values in its serialized form,
 * `decode*` methods are not that helpful, because format may not require a strict order of data
 * (e.g. JSON or XML), do not allow working with collection types or establish structure boundaries.
 * All these capabilities are delegated to the [CompositeDecoder] interface with a more specific API surface.
 * To denote a structure start, [beginStructure] should be used.
 * ```
 * // Denote the structure start,
 * val composite = decoder.beginStructure(descriptor)
 * // Decode all elements within the structure using 'composite'
 * ...
 * // Denote the structure end
 * composite.endStructure(descriptor)
 * ```
 *
 * E.g. if the decoder belongs to JSON format, then [beginStructure] will parse an opening bracket
 * (`{` or `[`, depending on the descriptor kind), returning the [CompositeDecoder] that is aware of colon separator,
 * that should be read after each key-value pair, whilst [CompositeDecoder.endStructure] will parse a closing bracket.
 *
 * ### Exception guarantees
 *
 * For the regular exceptions, such as invalid input, missing control symbols or attributes, and unknown symbols,
 * [SerializationException] can be thrown by any decoder methods. It is recommended to declare a format-specific
 * subclass of [SerializationException] and throw it.
 *
 * ### Exception safety
 *
 * In general, catching [SerializationException] from any of `decode*` methods is not allowed and produces unspecified behavior.
 * After thrown exception, the current decoder is left in an arbitrary state, no longer suitable for further decoding.
 *
 * ### Format encapsulation
 *
 * For example, for the following deserializer:
 * ```
 * class StringHolder(val stringValue: String)
 *
 * object StringPairDeserializer : DeserializationStrategy<StringHolder> {
 *    override val descriptor = ...
 *
 *    override fun deserializer(decoder: Decoder): StringHolder {
 *        // Denotes start of the structure, StringHolder is not a "plain" data type
 *        val composite = decoder.beginStructure(descriptor)
 *        if (composite.decodeElementIndex(descriptor) != 0)
 *            throw MissingFieldException("Field 'stringValue' is missing")
 *        // Decode the nested string value
 *        val value = composite.decodeStringElement(descriptor, index = 0)
 *        // Denotes end of the structure
 *        composite.endStructure(descriptor)
 *    }
 * }
 * ```
 *
 * This deserializer does not know anything about the underlying data and will work with any properly-implemented decoder.
 * JSON, for example, parses an opening bracket `{` during the `beginStructure` call, checks that the next key
 * after this bracket is `stringValue` (using the descriptor), returns the value after the colon as string value
 * and parses closing bracket `}` during the `endStructure`.
 * XML would do roughly the same, but with different separators and parsing structures, while ProtoBuf
 * machinery could be completely different.
 * In any case, all these parsing details are encapsulated by a decoder.
 *
 * ### Decoder implementation
 *
 * While being strictly typed, an underlying format can transform actual types in the way it wants.
 * For example, a format can support only string types and encode/decode all primitives in a string form:
 * ```
 * StringFormatDecoder : Decoder {
 *
 *     ...
 *     override fun decodeDouble(): Double = decodeString().toDouble()
 *     override fun decodeInt(): Int = decodeString().toInt()
 *     ...
 * }
 * ```
 *
 * ### Not stable for inheritance
 *
 * `Decoder` interface is not stable for inheritance in 3rd-party libraries, as new methods
 * might be added to this interface or contracts of the existing methods can be changed.
 */
__attribute__((swift_name("Kotlinx_serialization_coreDecoder")))
@protocol KmpCalendarKotlinx_serialization_coreDecoder
@required

/**
 * Decodes the beginning of the nested structure in a serialized form
 * and returns [CompositeDecoder] responsible for decoding this very structure.
 *
 * Typically, classes, collections and maps are represented as a nested structure in a serialized form.
 * E.g. the following JSON
 * ```
 * {
 *     "a": 2,
 *     "b": { "nested": "c" }
 *     "c": [1, 2, 3],
 *     "d": null
 * }
 * ```
 * has three nested structures: the very beginning of the data, "b" value and "c" value.
 */
- (id<KmpCalendarKotlinx_serialization_coreCompositeDecoder>)beginStructureDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("beginStructure(descriptor:)")));

/**
 * Decodes a boolean value.
 * Corresponding kind is [PrimitiveKind.BOOLEAN].
 */
- (BOOL)decodeBoolean __attribute__((swift_name("decodeBoolean()")));

/**
 * Decodes a single byte value.
 * Corresponding kind is [PrimitiveKind.BYTE].
 */
- (int8_t)decodeByte __attribute__((swift_name("decodeByte()")));

/**
 * Decodes a 16-bit unicode character value.
 * Corresponding kind is [PrimitiveKind.CHAR].
 */
- (unichar)decodeChar __attribute__((swift_name("decodeChar()")));

/**
 * Decodes a 64-bit IEEE 754 floating point value.
 * Corresponding kind is [PrimitiveKind.DOUBLE].
 */
- (double)decodeDouble __attribute__((swift_name("decodeDouble()")));

/**
 * Decodes a enum value and returns its index in [enumDescriptor] elements collection.
 * Corresponding kind is [SerialKind.ENUM].
 *
 * E.g. for the enum `enum class Letters { A, B, C, D }` and
 * underlying input "C", [decodeEnum] method should return `2` as a result.
 *
 * This method does not imply any restrictions on the input format,
 * the format is free to store the enum by its name, index, ordinal or any other enum representation.
 */
- (int32_t)decodeEnumEnumDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)enumDescriptor __attribute__((swift_name("decodeEnum(enumDescriptor:)")));

/**
 * Decodes a 32-bit IEEE 754 floating point value.
 * Corresponding kind is [PrimitiveKind.FLOAT].
 */
- (float)decodeFloat __attribute__((swift_name("decodeFloat()")));

/**
 * Returns [Decoder] for decoding an underlying type of a value class in an inline manner.
 * [descriptor] describes a target value class.
 *
 * Namely, for the `@Serializable @JvmInline value class MyInt(val my: Int)`, the following sequence is used:
 * ```
 * thisDecoder.decodeInline(MyInt.serializer().descriptor).decodeInt()
 * ```
 *
 * Current decoder may return any other instance of [Decoder] class, depending on the provided [descriptor].
 * For example, when this function is called on `Json` decoder with
 * `UInt.serializer().descriptor`, the returned decoder is able to decode unsigned integers.
 *
 * Note that this function returns [Decoder] instead of the [CompositeDecoder]
 * because value classes always have the single property.
 *
 * Calling [Decoder.beginStructure] on returned instance leads to an unspecified behavior and, in general, is prohibited.
 */
- (id<KmpCalendarKotlinx_serialization_coreDecoder>)decodeInlineDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("decodeInline(descriptor:)")));

/**
 * Decodes a 32-bit integer value.
 * Corresponding kind is [PrimitiveKind.INT].
 */
- (int32_t)decodeInt __attribute__((swift_name("decodeInt()")));

/**
 * Decodes a 64-bit integer value.
 * Corresponding kind is [PrimitiveKind.LONG].
 */
- (int64_t)decodeLong __attribute__((swift_name("decodeLong()")));

/**
 * Returns `true` if the current value in decoder is not null, false otherwise.
 * This method is usually used to decode potentially nullable data:
 * ```
 * // Could be String? deserialize() method
 * public fun deserialize(decoder: Decoder): String? {
 *     if (decoder.decodeNotNullMark()) {
 *         return decoder.decodeString()
 *     } else {
 *         return decoder.decodeNull()
 *     }
 * }
 * ```
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (BOOL)decodeNotNullMark __attribute__((swift_name("decodeNotNullMark()")));

/**
 * Decodes the `null` value and returns it.
 *
 * It is expected that `decodeNotNullMark` was called
 * prior to `decodeNull` invocation and the case when it returned `true` was handled.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (KmpCalendarKotlinNothing * _Nullable)decodeNull __attribute__((swift_name("decodeNull()")));

/**
 * Decodes the nullable value of type [T] by delegating the decoding process to the given [deserializer].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (id _Nullable)decodeNullableSerializableValueDeserializer:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy>)deserializer __attribute__((swift_name("decodeNullableSerializableValue(deserializer:)")));

/**
 * Decodes the value of type [T] by delegating the decoding process to the given [deserializer].
 * For example, `decodeInt` call is equivalent to delegating integer decoding to [Int.serializer][Int.Companion.serializer]:
 * `decodeSerializableValue(Int.serializer())`
 */
- (id _Nullable)decodeSerializableValueDeserializer:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy>)deserializer __attribute__((swift_name("decodeSerializableValue(deserializer:)")));

/**
 * Decodes a 16-bit short value.
 * Corresponding kind is [PrimitiveKind.SHORT].
 */
- (int16_t)decodeShort __attribute__((swift_name("decodeShort()")));

/**
 * Decodes a string value.
 * Corresponding kind is [PrimitiveKind.STRING].
 */
- (NSString *)decodeString __attribute__((swift_name("decodeString()")));

/**
 * Context of the current serialization process, including contextual and polymorphic serialization and,
 * potentially, a format-specific configuration.
 */
@property (readonly) KmpCalendarKotlinx_serialization_coreSerializersModule *serializersModule __attribute__((swift_name("serializersModule")));
@end


/**
 * [CompositeEncoder] is a part of encoding process that is bound to a particular structured part of
 * the serialized form, described by the serial descriptor passed to [Encoder.beginStructure].
 *
 * All `encode*` methods have `index` and `serialDescriptor` parameters with a strict semantics and constraints:
 *   * `descriptor` is always the same as one used in [Encoder.beginStructure]. While this parameter may seem redundant,
 *      it is required for efficient serialization process to avoid excessive field spilling.
 *      If you are writing your own format, you can safely ignore this parameter and use one used in `beginStructure`
 *      for simplicity.
 *   * `index` of the element being encoded. This element at this index in the descriptor should be associated with
 *      the one being written.
 *
 * The symmetric interface for the deserialization process is [CompositeDecoder].
 *
 * ### Not stable for inheritance
 *
 * `CompositeEncoder` interface is not stable for inheritance in 3rd party libraries, as new methods
 * might be added to this interface or contracts of the existing methods can be changed.
 */
__attribute__((swift_name("Kotlinx_serialization_coreCompositeEncoder")))
@protocol KmpCalendarKotlinx_serialization_coreCompositeEncoder
@required

/**
 * Encodes a boolean [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.BOOLEAN] kind.
 */
- (void)encodeBooleanElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(BOOL)value __attribute__((swift_name("encodeBooleanElement(descriptor:index:value:)")));

/**
 * Encodes a single byte [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.BYTE] kind.
 */
- (void)encodeByteElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(int8_t)value __attribute__((swift_name("encodeByteElement(descriptor:index:value:)")));

/**
 * Encodes a 16-bit unicode character [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.CHAR] kind.
 */
- (void)encodeCharElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(unichar)value __attribute__((swift_name("encodeCharElement(descriptor:index:value:)")));

/**
 * Encodes a 64-bit IEEE 754 floating point [value] associated with an element
 * at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.DOUBLE] kind.
 */
- (void)encodeDoubleElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(double)value __attribute__((swift_name("encodeDoubleElement(descriptor:index:value:)")));

/**
 * Encodes a 32-bit IEEE 754 floating point [value] associated with an element
 * at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.FLOAT] kind.
 */
- (void)encodeFloatElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(float)value __attribute__((swift_name("encodeFloatElement(descriptor:index:value:)")));

/**
 * Returns [Encoder] for decoding an underlying type of a value class in an inline manner.
 * Serializable value class is described by the [child descriptor][SerialDescriptor.getElementDescriptor]
 * of given [descriptor] at [index].
 *
 * Namely, for the `@Serializable @JvmInline value class MyInt(val my: Int)`,
 * and `@Serializable class MyData(val myInt: MyInt)` the following sequence is used:
 * ```
 * thisEncoder.encodeInlineElement(MyData.serializer.descriptor, 0).encodeInt(my)
 * ```
 *
 * This method provides an opportunity for the optimization to avoid boxing of a carried value
 * and its invocation should be equivalent to the following:
 * ```
 * thisEncoder.encodeSerializableElement(MyData.serializer.descriptor, 0, MyInt.serializer(), myInt)
 * ```
 *
 * Current encoder may return any other instance of [Encoder] class, depending on provided descriptor.
 * For example, when this function is called on Json encoder with descriptor that has
 * `UInt.serializer().descriptor` at the given [index], the returned encoder is able
 * to encode unsigned integers.
 *
 * Note that this function returns [Encoder] instead of the [CompositeEncoder]
 * because value classes always have the single property.
 * Calling [Encoder.beginStructure] on returned instance leads to an unspecified behavior and, in general, is prohibited.
 *
 * @see Encoder.encodeInline
 * @see SerialDescriptor.getElementDescriptor
 */
- (id<KmpCalendarKotlinx_serialization_coreEncoder>)encodeInlineElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("encodeInlineElement(descriptor:index:)")));

/**
 * Encodes a 32-bit integer [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.INT] kind.
 */
- (void)encodeIntElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(int32_t)value __attribute__((swift_name("encodeIntElement(descriptor:index:value:)")));

/**
 * Encodes a 64-bit integer [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.LONG] kind.
 */
- (void)encodeLongElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(int64_t)value __attribute__((swift_name("encodeLongElement(descriptor:index:value:)")));

/**
 * Delegates nullable [value] encoding of the type [T] to the given [serializer].
 * [value] is associated with an element at the given [index] in [serial descriptor][descriptor].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (void)encodeNullableSerializableElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index serializer:(id<KmpCalendarKotlinx_serialization_coreSerializationStrategy>)serializer value:(id _Nullable)value __attribute__((swift_name("encodeNullableSerializableElement(descriptor:index:serializer:value:)")));

/**
 * Delegates [value] encoding of the type [T] to the given [serializer].
 * [value] is associated with an element at the given [index] in [serial descriptor][descriptor].
 */
- (void)encodeSerializableElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index serializer:(id<KmpCalendarKotlinx_serialization_coreSerializationStrategy>)serializer value:(id _Nullable)value __attribute__((swift_name("encodeSerializableElement(descriptor:index:serializer:value:)")));

/**
 * Encodes a 16-bit short [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.SHORT] kind.
 */
- (void)encodeShortElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(int16_t)value __attribute__((swift_name("encodeShortElement(descriptor:index:value:)")));

/**
 * Encodes a string [value] associated with an element at the given [index] in [serial descriptor][descriptor].
 * The element at the given [index] should have [PrimitiveKind.STRING] kind.
 */
- (void)encodeStringElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index value:(NSString *)value __attribute__((swift_name("encodeStringElement(descriptor:index:value:)")));

/**
 * Denotes the end of the structure associated with current encoder.
 * For example, composite encoder of JSON format will write
 * a closing bracket in the underlying input and reduce the number of nesting for pretty printing.
 */
- (void)endStructureDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("endStructure(descriptor:)")));

/**
 * Whether the format should encode values that are equal to the default values.
 * This method is used by plugin-generated serializers for properties with default values:
 * ```
 * @Serializable
 * class WithDefault(val int: Int = 42)
 * // serialize method
 * if (value.int != 42 || output.shouldEncodeElementDefault(serialDesc, 0)) {
 *    encoder.encodeIntElement(serialDesc, 0, value.int);
 * }
 * ```
 *
 * This method is never invoked for properties annotated with [EncodeDefault].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (BOOL)shouldEncodeElementDefaultDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("shouldEncodeElementDefault(descriptor:index:)")));

/**
 * Context of the current serialization process, including contextual and polymorphic serialization and,
 * potentially, a format-specific configuration.
 */
@property (readonly) KmpCalendarKotlinx_serialization_coreSerializersModule *serializersModule __attribute__((swift_name("serializersModule")));
@end


/**
 * [SerializersModule] is a collection of serializers used by [ContextualSerializer] and [PolymorphicSerializer]
 * to override or provide serializers at the runtime, whereas at the compile-time they provided by the serialization plugin.
 * It can be considered as a map where serializers can be found using their statically known KClasses.
 *
 * To enable runtime serializers resolution, one of the special annotations must be used on target types
 * ([Polymorphic] or [Contextual]), and a serial module with serializers should be used during construction of [SerialFormat].
 *
 * Serializers module can be built with `SerializersModule {}` builder function.
 * Empty module can be obtained with `EmptySerializersModule()` factory function.
 *
 * @see Contextual
 * @see Polymorphic
 */
__attribute__((swift_name("Kotlinx_serialization_coreSerializersModule")))
@interface KmpCalendarKotlinx_serialization_coreSerializersModule : KmpCalendarBase

/**
 * Copies contents of this module to the given [collector].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (void)dumpToCollector:(id<KmpCalendarKotlinx_serialization_coreSerializersModuleCollector>)collector __attribute__((swift_name("dumpTo(collector:)")));

/**
 * Returns a contextual serializer associated with a given [kClass].
 * If given class has generic parameters and module has provider for [kClass],
 * [typeArgumentsSerializers] are used to create serializer.
 * This method is used in context-sensitive operations on a property marked with [Contextual] by a [ContextualSerializer].
 *
 * @see SerializersModuleBuilder.contextual
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (id<KmpCalendarKotlinx_serialization_coreKSerializer> _Nullable)getContextualKClass:(id<KmpCalendarKotlinKClass>)kClass typeArgumentsSerializers:(NSArray<id<KmpCalendarKotlinx_serialization_coreKSerializer>> *)typeArgumentsSerializers __attribute__((swift_name("getContextual(kClass:typeArgumentsSerializers:)")));

/**
 * Returns a polymorphic serializer registered for a class of the given [value] in the scope of [baseClass].
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (id<KmpCalendarKotlinx_serialization_coreSerializationStrategy> _Nullable)getPolymorphicBaseClass:(id<KmpCalendarKotlinKClass>)baseClass value:(id)value __attribute__((swift_name("getPolymorphic(baseClass:value:)")));

/**
 * Returns a polymorphic deserializer registered for a [serializedClassName] in the scope of [baseClass]
 * or default value constructed from [serializedClassName] if a default serializer provider was registered.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy> _Nullable)getPolymorphicBaseClass:(id<KmpCalendarKotlinKClass>)baseClass serializedClassName:(NSString * _Nullable)serializedClassName __attribute__((swift_name("getPolymorphic(baseClass:serializedClassName:)")));
@end

__attribute__((swift_name("KotlinAnnotation")))
@protocol KmpCalendarKotlinAnnotation
@required
@end


/**
 * [CompositeDecoder] is a part of decoding process that is bound to a particular structured part of
 * the serialized form, described by the serial descriptor passed to [Decoder.beginStructure].
 *
 * Typically, for unordered data, [CompositeDecoder] is used by a serializer withing a [decodeElementIndex]-based
 * loop that decodes all the required data one-by-one in any order and then terminates by calling [endStructure].
 * Please refer to [decodeElementIndex] for example of such loop.
 *
 * All `decode*` methods have `index` and `serialDescriptor` parameters with a strict semantics and constraints:
 *   * `descriptor` argument is always the same as one used in [Decoder.beginStructure].
 *   * `index` of the element being decoded. For [sequential][decodeSequentially] decoding, it is always a monotonic
 *      sequence from `0` to `descriptor.elementsCount` and for indexing-loop it is always an index that [decodeElementIndex]
 *      has returned from the last call.
 *
 * The symmetric interface for the serialization process is [CompositeEncoder].
 *
 * ### Not stable for inheritance
 *
 * `CompositeDecoder` interface is not stable for inheritance in 3rd party libraries, as new methods
 * might be added to this interface or contracts of the existing methods can be changed.
 */
__attribute__((swift_name("Kotlinx_serialization_coreCompositeDecoder")))
@protocol KmpCalendarKotlinx_serialization_coreCompositeDecoder
@required

/**
 * Decodes a boolean value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.BOOLEAN] kind.
 */
- (BOOL)decodeBooleanElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeBooleanElement(descriptor:index:)")));

/**
 * Decodes a single byte value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.BYTE] kind.
 */
- (int8_t)decodeByteElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeByteElement(descriptor:index:)")));

/**
 * Decodes a 16-bit unicode character value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.CHAR] kind.
 */
- (unichar)decodeCharElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeCharElement(descriptor:index:)")));

/**
 * Method to decode collection size that may be called before the collection decoding.
 * Collection type includes [Collection], [Map] and [Array] (including primitive arrays).
 * Method can return `-1` if the size is not known in advance, though for [sequential decoding][decodeSequentially]
 * knowing precise size is a mandatory requirement.
 */
- (int32_t)decodeCollectionSizeDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("decodeCollectionSize(descriptor:)")));

/**
 * Decodes a 64-bit IEEE 754 floating point value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.DOUBLE] kind.
 */
- (double)decodeDoubleElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeDoubleElement(descriptor:index:)")));

/**
 *  Decodes the index of the next element to be decoded.
 *  Index represents a position of the current element in the serial descriptor element that can be found
 *  with [SerialDescriptor.getElementIndex].
 *
 *  If this method returns non-negative index, the caller should call one of the `decode*Element` methods
 *  with a resulting index.
 *  Apart from positive values, this method can return [DECODE_DONE] to indicate that no more elements
 *  are left or [UNKNOWN_NAME] to indicate that symbol with an unknown name was encountered.
 *
 * Example of usage:
 * ```
 * class MyPair(i: Int, d: Double)
 *
 * object MyPairSerializer : KSerializer<MyPair> {
 *     // ... other methods omitted
 *
 *    fun deserialize(decoder: Decoder): MyPair {
 *        val composite = decoder.beginStructure(descriptor)
 *        var i: Int? = null
 *        var d: Double? = null
 *        while (true) {
 *            when (val index = composite.decodeElementIndex(descriptor)) {
 *                0 -> i = composite.decodeIntElement(descriptor, 0)
 *                1 -> d = composite.decodeDoubleElement(descriptor, 1)
 *                DECODE_DONE -> break // Input is over
 *                else -> error("Unexpected index: $index)
 *            }
 *        }
 *        composite.endStructure(descriptor)
 *        require(i != null && d != null)
 *        return MyPair(i, d)
 *    }
 * }
 * ```
 * This example is a rough equivalent of what serialization plugin generates for serializable pair class.
 *
 * The need in such a loop comes from unstructured nature of most serialization formats.
 * For example, JSON for the following input `{"d": 2.0, "i": 1}`, will first read `d` key with index `1`
 * and only after `i` with the index `0`.
 *
 * A potential implementation of this method for JSON format can be the following:
 * ```
 * fun decodeElementIndex(descriptor: SerialDescriptor): Int {
 *     // Ignore arrays
 *     val nextKey: String? = myStringJsonParser.nextKey()
 *     if (nextKey == null) return DECODE_DONE
 *     return descriptor.getElementIndex(nextKey) // getElementIndex can return UNKNOWN_NAME
 * }
 * ```
 *
 * If [decodeSequentially] returns `true`, the caller might skip calling this method.
 */
- (int32_t)decodeElementIndexDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("decodeElementIndex(descriptor:)")));

/**
 * Decodes a 32-bit IEEE 754 floating point value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.FLOAT] kind.
 */
- (float)decodeFloatElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeFloatElement(descriptor:index:)")));

/**
 * Returns [Decoder] for decoding an underlying type of a value class in an inline manner.
 * Serializable value class is described by the [child descriptor][SerialDescriptor.getElementDescriptor]
 * of given [descriptor] at [index].
 *
 * Namely, for the `@Serializable @JvmInline value class MyInt(val my: Int)`,
 * and `@Serializable class MyData(val myInt: MyInt)` the following sequence is used:
 * ```
 * thisDecoder.decodeInlineElement(MyData.serializer().descriptor, 0).decodeInt()
 * ```
 *
 * This method provides an opportunity for the optimization to avoid boxing of a carried value
 * and its invocation should be equivalent to the following:
 * ```
 * thisDecoder.decodeSerializableElement(MyData.serializer.descriptor, 0, MyInt.serializer())
 * ```
 *
 * Current decoder may return any other instance of [Decoder] class, depending on the provided descriptor.
 * For example, when this function is called on `Json` decoder with descriptor that has
 * `UInt.serializer().descriptor` at the given [index], the returned decoder is able
 * to decode unsigned integers.
 *
 * Note that this function returns [Decoder] instead of the [CompositeDecoder]
 * because value classes always have the single property.
 * Calling [Decoder.beginStructure] on returned instance leads to an unspecified behavior and, in general, is prohibited.
 *
 * @see Decoder.decodeInline
 * @see SerialDescriptor.getElementDescriptor
 */
- (id<KmpCalendarKotlinx_serialization_coreDecoder>)decodeInlineElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeInlineElement(descriptor:index:)")));

/**
 * Decodes a 32-bit integer value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.INT] kind.
 */
- (int32_t)decodeIntElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeIntElement(descriptor:index:)")));

/**
 * Decodes a 64-bit integer value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.LONG] kind.
 */
- (int64_t)decodeLongElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeLongElement(descriptor:index:)")));

/**
 * Decodes nullable value of the type [T] with the given [deserializer].
 *
 * If value at given [index] was already decoded with previous [decodeSerializableElement] call with the same index,
 * [previousValue] would contain a previously decoded value.
 * This parameter can be used to aggregate multiple values of the given property to the only one.
 * Implementation can safely ignore it and return a new value, efficiently using 'the last one wins' strategy,
 * or apply format-specific aggregating strategies, e.g. appending scattered Protobuf lists to a single one.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (id _Nullable)decodeNullableSerializableElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index deserializer:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy>)deserializer previousValue:(id _Nullable)previousValue __attribute__((swift_name("decodeNullableSerializableElement(descriptor:index:deserializer:previousValue:)")));

/**
 * Checks whether the current decoder supports strictly ordered decoding of the data
 * without calling to [decodeElementIndex].
 * If the method returns `true`, the caller might skip [decodeElementIndex] calls
 * and start invoking `decode*Element` directly, incrementing the index of the element one by one.
 * This method can be called by serializers (either generated or user-defined) as a performance optimization,
 * but there is no guarantee that the method will be ever called. Practically, it means that implementations
 * that may benefit from sequential decoding should also support a regular [decodeElementIndex]-based decoding as well.
 *
 * Example of usage:
 * ```
 * class MyPair(i: Int, d: Double)
 *
 * object MyPairSerializer : KSerializer<MyPair> {
 *     // ... other methods omitted
 *
 *    fun deserialize(decoder: Decoder): MyPair {
 *        val composite = decoder.beginStructure(descriptor)
 *        if (composite.decodeSequentially()) {
 *            val i = composite.decodeIntElement(descriptor, index = 0) // Mind the sequential indexing
 *            val d = composite.decodeIntElement(descriptor, index = 1)
 *            composite.endStructure(descriptor)
 *            return MyPair(i, d)
 *        } else {
 *            // Fallback to `decodeElementIndex` loop, refer to its documentation for details
 *        }
 *    }
 * }
 * ```
 * This example is a rough equivalent of what serialization plugin generates for serializable pair class.
 *
 * Sequential decoding is a performance optimization for formats with strictly ordered schema,
 * usually binary ones. Regular formats such as JSON or ProtoBuf cannot use this optimization,
 * because e.g. in the latter example, the same data can be represented both as
 * `{"i": 1, "d": 1.0}` and `{"d": 1.0, "i": 1}` (thus, unordered).
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
- (BOOL)decodeSequentially __attribute__((swift_name("decodeSequentially()")));

/**
 * Decodes value of the type [T] with the given [deserializer].
 *
 * Implementations of [CompositeDecoder] may use their format-specific deserializers
 * for particular data types, e.g. handle [ByteArray] specifically if format is binary.
 *
 * If value at given [index] was already decoded with previous [decodeSerializableElement] call with the same index,
 * [previousValue] would contain a previously decoded value.
 * This parameter can be used to aggregate multiple values of the given property to the only one.
 * Implementation can safely ignore it and return a new value, effectively using 'the last one wins' strategy,
 * or apply format-specific aggregating strategies, e.g. appending scattered Protobuf lists to a single one.
 */
- (id _Nullable)decodeSerializableElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index deserializer:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy>)deserializer previousValue:(id _Nullable)previousValue __attribute__((swift_name("decodeSerializableElement(descriptor:index:deserializer:previousValue:)")));

/**
 * Decodes a 16-bit short value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.SHORT] kind.
 */
- (int16_t)decodeShortElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeShortElement(descriptor:index:)")));

/**
 * Decodes a string value from the underlying input.
 * The resulting value is associated with the [descriptor] element at the given [index].
 * The element at the given index should have [PrimitiveKind.STRING] kind.
 */
- (NSString *)decodeStringElementDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor index:(int32_t)index __attribute__((swift_name("decodeStringElement(descriptor:index:)")));

/**
 * Denotes the end of the structure associated with current decoder.
 * For example, composite decoder of JSON format will expect (and parse)
 * a closing bracket in the underlying input.
 */
- (void)endStructureDescriptor:(id<KmpCalendarKotlinx_serialization_coreSerialDescriptor>)descriptor __attribute__((swift_name("endStructure(descriptor:)")));

/**
 * Context of the current decoding process, including contextual and polymorphic serialization and,
 * potentially, a format-specific configuration.
 */
@property (readonly) KmpCalendarKotlinx_serialization_coreSerializersModule *serializersModule __attribute__((swift_name("serializersModule")));
@end

__attribute__((objc_subclassing_restricted))
__attribute__((swift_name("KotlinNothing")))
@interface KmpCalendarKotlinNothing : KmpCalendarBase
@end


/**
 * [SerializersModuleCollector] can introspect and accumulate content of any [SerializersModule] via [SerializersModule.dumpTo],
 * using a visitor-like pattern: [contextual] and [polymorphic] functions are invoked for each registered serializer.
 *
 * ### Not stable for inheritance
 *
 * `SerializersModuleCollector` interface is not stable for inheritance in 3rd party libraries, as new methods
 * might be added to this interface or contracts of the existing methods can be changed.
 *
 * @note annotations
 *   kotlinx.serialization.ExperimentalSerializationApi
*/
__attribute__((swift_name("Kotlinx_serialization_coreSerializersModuleCollector")))
@protocol KmpCalendarKotlinx_serialization_coreSerializersModuleCollector
@required

/**
 * Accept a provider, associated with generic [kClass] for contextual serialization.
 */
- (void)contextualKClass:(id<KmpCalendarKotlinKClass>)kClass provider:(id<KmpCalendarKotlinx_serialization_coreKSerializer> (^)(NSArray<id<KmpCalendarKotlinx_serialization_coreKSerializer>> *))provider __attribute__((swift_name("contextual(kClass:provider:)")));

/**
 * Accept a serializer, associated with [kClass] for contextual serialization.
 */
- (void)contextualKClass:(id<KmpCalendarKotlinKClass>)kClass serializer:(id<KmpCalendarKotlinx_serialization_coreKSerializer>)serializer __attribute__((swift_name("contextual(kClass:serializer:)")));

/**
 * Accept a serializer, associated with [actualClass] for polymorphic serialization.
 */
- (void)polymorphicBaseClass:(id<KmpCalendarKotlinKClass>)baseClass actualClass:(id<KmpCalendarKotlinKClass>)actualClass actualSerializer:(id<KmpCalendarKotlinx_serialization_coreKSerializer>)actualSerializer __attribute__((swift_name("polymorphic(baseClass:actualClass:actualSerializer:)")));

/**
 * Accept a default deserializer provider, associated with the [baseClass] for polymorphic deserialization.
 *
 * This function affect only deserialization process. To avoid confusion, it was deprecated and replaced with [polymorphicDefaultDeserializer].
 * To affect serialization process, use [SerializersModuleCollector.polymorphicDefaultSerializer].
 *
 * [defaultDeserializerProvider] is invoked when no polymorphic serializers associated with the `className`
 * in the scope of [baseClass] were found. `className` could be `null` for formats that support nullable class discriminators
 * (currently only `Json` with `useArrayPolymorphism` set to `false`).
 *
 * [defaultDeserializerProvider] can be stateful and lookup a serializer for the missing type dynamically.
 *
 * @see SerializersModuleCollector.polymorphicDefaultDeserializer
 * @see SerializersModuleCollector.polymorphicDefaultSerializer
 */
- (void)polymorphicDefaultBaseClass:(id<KmpCalendarKotlinKClass>)baseClass defaultDeserializerProvider:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy> _Nullable (^)(NSString * _Nullable))defaultDeserializerProvider __attribute__((swift_name("polymorphicDefault(baseClass:defaultDeserializerProvider:)"))) __attribute__((deprecated("Deprecated in favor of function with more precise name: polymorphicDefaultDeserializer")));

/**
 * Accept a default deserializer provider, associated with the [baseClass] for polymorphic deserialization.
 * [defaultDeserializerProvider] is invoked when no polymorphic serializers associated with the `className`
 * in the scope of [baseClass] were found. `className` could be `null` for formats that support nullable class discriminators
 * (currently only `Json` with `useArrayPolymorphism` set to `false`).
 *
 * Default deserializers provider affects only deserialization process. Serializers are accepted in the
 * [SerializersModuleCollector.polymorphicDefaultSerializer] method.
 *
 * [defaultDeserializerProvider] can be stateful and lookup a serializer for the missing type dynamically.
 */
- (void)polymorphicDefaultDeserializerBaseClass:(id<KmpCalendarKotlinKClass>)baseClass defaultDeserializerProvider:(id<KmpCalendarKotlinx_serialization_coreDeserializationStrategy> _Nullable (^)(NSString * _Nullable))defaultDeserializerProvider __attribute__((swift_name("polymorphicDefaultDeserializer(baseClass:defaultDeserializerProvider:)")));

/**
 * Accept a default serializer provider, associated with the [baseClass] for polymorphic serialization.
 * [defaultSerializerProvider] is invoked when no polymorphic serializers for `value` in the scope of [baseClass] were found.
 *
 * Default serializers provider affects only serialization process. Deserializers are accepted in the
 * [SerializersModuleCollector.polymorphicDefaultDeserializer] method.
 *
 * [defaultSerializerProvider] can be stateful and lookup a serializer for the missing type dynamically.
 */
- (void)polymorphicDefaultSerializerBaseClass:(id<KmpCalendarKotlinKClass>)baseClass defaultSerializerProvider:(id<KmpCalendarKotlinx_serialization_coreSerializationStrategy> _Nullable (^)(id))defaultSerializerProvider __attribute__((swift_name("polymorphicDefaultSerializer(baseClass:defaultSerializerProvider:)")));
@end

__attribute__((swift_name("KotlinKDeclarationContainer")))
@protocol KmpCalendarKotlinKDeclarationContainer
@required
@end

__attribute__((swift_name("KotlinKAnnotatedElement")))
@protocol KmpCalendarKotlinKAnnotatedElement
@required
@end


/**
 * @note annotations
 *   kotlin.SinceKotlin(version="1.1")
*/
__attribute__((swift_name("KotlinKClassifier")))
@protocol KmpCalendarKotlinKClassifier
@required
@end

__attribute__((swift_name("KotlinKClass")))
@protocol KmpCalendarKotlinKClass <KmpCalendarKotlinKDeclarationContainer, KmpCalendarKotlinKAnnotatedElement, KmpCalendarKotlinKClassifier>
@required

/**
 * @note annotations
 *   kotlin.SinceKotlin(version="1.1")
*/
- (BOOL)isInstanceValue:(id _Nullable)value __attribute__((swift_name("isInstance(value:)")));
@property (readonly) NSString * _Nullable qualifiedName __attribute__((swift_name("qualifiedName")));
@property (readonly) NSString * _Nullable simpleName __attribute__((swift_name("simpleName")));
@end

#pragma pop_macro("_Nullable_result")
#pragma clang diagnostic pop
NS_ASSUME_NONNULL_END
